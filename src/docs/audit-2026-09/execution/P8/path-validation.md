# Path validation

Nuke test-r6 compiled and ran17998 tests:17995 pass3fail. P8 absolute/relative exact-path and ProjectReference controls passed. Artifacts fixture failed its expected root because UseArtifactsOutput was configured after Microsoft.NET.Sdk props had already selected paths. Lead moved artifacts settings before SDK props; production unchanged. Old source-content regression still asserted prefix concatenation; updated to actual Path.GetFullPath authority while preserving existing test. Original failed log retained, affected validation pending. P5a third failure independent.

Nuke test-r7 passed all 17,998 generator,79 analyzer,888 runtime tests (one generator and one runtime skip). Path-only same-assembly golden control `path-golden-r1`: original HEAD targets exit1; corrected targets exit0; exact original current bytes restored in finally. Receipt pins both target sources and command. Three actual-target cases cover absolute, relative and artifacts; ProjectReference controls passed in full test-r7. Resource/package delivery remains separate pending work.

Final paired-review pass: search source identity now uses FullPath item metadata; second-project search query asserts raw exact source/wrapper paths and existence for abs/relative/artifacts. ProjectReference native manifest is also three modes. Full Nuke test-r12 passed18067generator+1skip,79analyzer,890runtime+1skip. Extended same-assembly golden path-golden-r2 ran nine tests across both target outputs and consumer boundary: old targets nonzero, corrected zero; exact current target bytes restored. This is final Medium/Low fix pass validation, no rereview. Resource readiness is still open separate scope.
