# 0.18.0 pre-release open items — every known defect, all repos

Date opened: 2026-07-26. **Status: SWEEP COMPLETE 2026-07-27 — all 11 SB gates green, zero
failures (see section A). RELEASE HELD by owner decision G4**: open items in this register get
worked/decided first, then a full downstream regression pass, then the cut. Nothing here is
autonomous next-work — the remaining items are owner-gated decisions and owner-sequenced runs.

Purpose: the owner asked for one durable register of every open defect and unfinished
verification across **all three repos**, so nothing depends on a chat session surviving. Scope
is deliberately wider than `roadmap.md` (intent + policy) and `not-planned.md` (trigger-gated
latents): this file is the pre-release punch list. Items here are expected to be **fixed or
consciously accepted before `release/sdk-0.18.0+apple-26.2.8` is cut**.

Repos:
- **SB** = `/Users/wojo/Dev/swift-bindings` (this repo)
- **PKG** = `/Users/wojo/Dev/swift-dotnet-packages`
- **IBT** = `/Users/wojo/Dev/internal-binding-testing`

Status legend: `OPEN` needs a fix · `VERIFY` needs a run to confirm · `ACCEPT?` owner call ·
`DONE` closed this cycle.

---

## START HERE — orientation for a fresh session

This file is written to be **self-sufficient**: a session handed only this doc should be able to
execute without recovering prior chat context.

**Goal.** Everything green across all three repos, then cut `release/sdk-0.18.0+apple-26.2.8`.
The owner's standing instruction is that nothing gets buried — every issue found is reported and
documented here, not silently worked around.

**Where the work stands (updated 2026-07-27, wave committed).** The fix wave is **COMMITTED to
SB `main` as five commits (owner-authorized 2026-07-27), still UNPUSHED** — `main` is now 11
commits ahead of `origin/main`. The five wave commits, in order:

1. **Generator fixes** (`f4e785d5`) — B9/B10 cross-module reference recording
   (`CrossModuleBindingReferences.cs`, `ResolvedReferenceRecorder.cs`, `BindingProjectEmitter.cs`),
   B11 dependency-closure fixpoint (`DependencyClosureResolver.cs`), B15 static-merge library
   naming, the A6 wrapper-library literal fix, and B3 keyword over-escaping — with all their tests.
2. **Harness trust fixes** (`bc592d7b`) — A15 per-attempt run token, A17 crash-recovery budget,
   A18 UDID routing, A6 async-wrapper hard-fail + `MainQueueSynchronizationContext` +
   tvOS/Mac runner threading + `SimCtl`/`DeviceCtl` collectors — with all their tests
   (unit floor **16169**, see A1/A18).
3. **Fixture** (`bb50ea0d`) — the rescued `AsyncGenericCollectionStructArrayReturn*` fixture.
4. **Baselines** (`d3929da7`) — validate reseed (G1-authorized) + the five raised runtime pass
   floors + the simulator identity block. **Deliberately isolated** so an A20 revert, if the owner
   rules that way, is one `git revert d3929da7`; committed under the owner's blanket
   commit authorization, with A20/A19 remaining formally open.
5. **Docs** — this file, `not-planned.md`, `c2-c3-triage-2026-07.md`.

`swift-dotnet-packages` is **deliberately uncommitted** — its csprojs pin
`SwiftBindings.Sdk/0.18.0`, which is not on NuGet yet. The B1/B2 injector fixes live there,
uncommitted, same reason.

**What a fresh session should NOT do:** push, cut the release (G4), reseed any baseline,
or start the A12/A13/A14 downstream runs without the owner asking. The open surface is:
owner decisions (A19, A20, B13, C5, B14/C2/D1 funding, G4 sequencing), and the held release
steps in section F step 3. Note B15 is fixed and committed (`f4e785d5`, regen-verified); its
residual is the downstream Mappedin repack, not new generator work.

**Execution order lives in section F.** Read it before starting. The short version: test the
step-0 hypothesis first, because it may collapse B9/B10/B11 into one fix.

**Hard constraints — these are not negotiable:**
- **Never `git stash`** — linter hooks detect reverted files and `stash pop` discards changes silently.
- **Do not commit unless the owner explicitly asks.**
- **Do not commit `swift-dotnet-packages`** until the referenced SDK version is published to NuGet.
- **No shortcuts.** Root-cause fixes only — never weaken an assertion, skip a failing test, or
  paper over a defect. If unsure whether a fix is root-cause, ask rather than proceed.
- **Prediction-gate freeze policy applies.** A new hand-coded emission-time prediction gate is
  justified *iff the failure it prevents would compile*. All of B9/B10/B11 end in compile errors,
  so by policy they belong to the verify-recover loop, **not** to new predictors. See section F step 0.
- **New work ships with tests** at the layer that actually exercises it.
- Long runs (`nuke validate`, ~5 min+) must be **daemonized** (double-fork + `setsid`, poll via a
  watcher) — the harness SIGKILLs ordinary background tasks at roughly 5.5 minutes.
- Generator runs from `bin/Debug/`; run `dotnet build -c Debug` after generator edits or a stale
  binary will silently mask your change.

---

## A. Verification gaps — things not yet proven green

| ID | Repo | Item | Status |
|---|---|---|---|
| A1 | SB | `nuke test` — unit/runtime/analyzer | **DONE 2026-07-27 (latest): 16158 / 0 / 1 unit, 35/0/0 analyzer, 730/0/1 runtime**, 46s, all six targets Succeeded. Floor raised 15868 → 15989 → **16158**. *(Superseded by the A18 re-verification: final count **16169/0/1**, the +11 delta closing exactly against `SharedUdidRoutingTests`' 11 TRX results.)* Measured twice independently (main loop and gate lane) with identical results, and the new classes confirmed *executed* via TRX enumeration rather than a log grep — `CrashRecoveryBudgetTests` 12, `SandboxResultProbeTests` 7, `AsyncWrapperGateTests` 8, `LaunchDiagnosticsTests` 18, `JsonlRunTokenTests` 20, `StaticNativeMergeLibraryNamingTests` 33, `DependencyClosureResolverTests` 19. (A `grep` of a `nuke test` log cannot prove a class ran: `--verbosity minimal` names only failing/skipped tests, so a fully-green class is invisible — see A16.) |
| A2 | SB | `nuke binding-tests --compile-only` | **DONE 2026-07-27 — re-run complete, PASS.** Exit 0, 3:25, no `--permissive`, against a tree carrying every patch of this cycle. All sub-gates green: wrapper-strip tripwire 0 blocks stripped (baseline 0); wrapper getter-parity 210 EveryProtocol witness getters identical across harness + generator wrappers; CSM recovery gate — `LibraryResponse.items` annotated with 2 recovering projections; compile-check 0 errors; artifact-parity **0 NEW violations** (pre-baseline residue unchanged: 2 fwd-missing / 6 reverse-orphan / 15 C#-only vtable); API-manifest 2844 current vs 2842 baselined = **2 added, 0 removed, additive-clean → no reseed**; resilience-kitchen PASSED; ingestion-kitchen PASSED on all four legs (closed graph binds; missing transitive fails early with structured SWIFTBIND119, no SWIFTBIND111, no artifacts; both quarantine legs degrade and compile). This covers both the dependency-closure (ingestion) and static-merge (emission) patches. The `[ERR]`-prefixed log lines are `swiftc` warnings Nuke routes to stderr, per A11 — not failures. *Superseded reason for the re-run:* the earlier green predated two generator patches applied later on 2026-07-27 (the dependency-closure `otool`/import-edge split, which touches the ingestion path, and the static-merge library-naming fix). Both must be covered before sign-off. Prior result — re-run 2026-07-27 after the new fixture landed: exit 0, 3:51. Parity 0 new; API-manifest additive-clean (no reseed needed); resilience-kitchen + ingestion-kitchen PASSED. |
| A3 | SB | `nuke validate` — full real-world compile sweep | **DONE 2026-07-27 — re-run complete, PASS.** **132/132 passed** — Compile (standalone) 107/107, dependencies 25/25, Swift wrapper 112/112 (20 ObjC/no wrapper). All four targets Succeeded, 10:56 (Validate 6:03, PackGate 3:03, BehaviorTier 1:42). **No regressions detected.** This closes the static-merge lane's one residual: the newly-judged wrapper-side `Tj` arm is confirmed by a full real-world sweep with zero regressions, as the lane predicted. Baseline kept per instruction (`git_sha` 52ac336a → ff08abd5, `dep_compile: ok` rows added), uncommitted. **Correction to the cleanup instruction below: validate dirtied NONE of the eight `-behaviortier` version-stamp files this run** — the only file it touched was `build/baselines/validation-baseline.json`, verified by mtime against the run start. A blind `git checkout` of that eight-file list would have been destructive, not merely unnecessary: `BindingProjectEmitter.cs` is on it and carried 49 lines of this cycle's new cross-module Apple sibling-package emission with zero stamp content. **Never blind-revert that list — verify each file's diff is purely the stamp first.** *Superseded reason for the re-run:* same as A2: this sweep predates the dependency-closure and static-merge patches. The re-run also closes the static-merge lane's one residual (it could not run validate from its worktree; it bounds the blast radius of the newly-judged wrapper-side `Tj` arm by two independent arguments and expects confirmation, not surprise). On re-run: restore the eight `-behaviortier` version-stamp files with `git checkout` but **KEEP** the baseline json; re-baselining beyond the authorized G1 seed is not in scope. Prior result — **DONE** 2026-07-27: **132/132 passed** — Compile (standalone) 107/107, Swift wrapper 112/112 (20 ObjC/no wrapper), dependency gate 25/25. All four targets Succeeded (Validate 6:07, PackGate 2:59, BehaviorTier 1:40; total 10:54). **Zero regressions** against the old baseline (which itself recorded 130/130 ok; the "12 failed / 26 regressions" figures were live-run-vs-stale-baseline deltas, not a red baseline). Corpus grew by 2 cells (`FirebaseSharedSwift`, `Promises`). Baseline **re-seeded** in place by the run, `git_sha 52ac336a` → `ff08abd5` — authorized by G1 (fully green). Note the `Lines` column is now incomparable to pre-B5 values by design; the nine cells that shrank are analysed in `not-planned.md` (mostly the wrapper-symbol withdrawal machinery removing would-be-dangling P/Invokes). This closes **D2**. |
| A4 | SB | `nuke binding-tests` (iOS Simulator, Mono JIT) | **DONE** 2026-07-27: exit 0, **3254 passed / 0 failed / 41 skipped** (floor was 3245; +4 from the new fixture). |
| A5 | SB | `nuke binding-tests --device` (NativeAOT) | **DONE 2026-07-27 — PASS, 4:31.** `JSONL: 3268 pass, 0 fail, 27 skip, 0 crash (done=True)`, 3295 records — **IMPROVEMENT, +4 over baseline 3264**. The four watch-items all clean: (i) **zero class exclusions** — completed on `JSONL results (run 1)`, first launch attempt, zero launcher aborts, so the blind-skip fallback never armed and the E1 CoreDeviceError 10002/EINVAL path did not fire at all; (ii) **run-token working** — JSONL line 1 is `{"run_token":"92f1f25f70a5444ca0533bdfda47620f"}`, accepted at recovery; (iii) **skip classification correct** — of 27 skips, 1 `Device:`-prefixed, **0 `Simulator:`-prefixed**, 26 free-text NativeAOT trim/reflection reasons, so no simulator-flavoured skip leaked in; (iv) **A15 confirmed retrospectively** — the old false-green artifacts `runtime-tests-device-run-1.jsonl` and `-5.jsonl` are byte-identical (md5 `cdfade341b353757a6f35a0b2f1a405e`), the six identical "recoveries" from the persistent-container defect, now proven rather than inferred; this run's file is distinct (`fb8f60da0d0f251f0b1debc707392b4a`). **Honest limitation, stated rather than glossed:** because the run had no launcher aborts, the token **accept** path was exercised live but the **reject** path was not, and the crash-budget extension never engaged. Those are covered at the unit layer (`JsonlRunTokenTests` 20 cases, `SandboxResultProbeTests` 7, `CrashRecoveryBudgetTests` 12) but have no live device repro — the environmental failure they guard against did not recur, and cannot be summoned on demand. *Prior state for the record:* The 2026-07-26 environmental read (devicectl 10002/EINVAL, `LaunchActionDeclaration` never invoked → app image never entered) was re-confirmed on 2026-07-27 — the device is healthy (paired, booted, developer mode on, unlocked, wired, tunnel up) and a *manual* launch succeeds in 0.304s, so the discriminator is the harness launching ~1s after `devicectl device install app`, six times in 16s: an install/LaunchServices settle race. **But that run also exposed a HIGH-severity harness defect that is NOT environmental — see A15.** Product signal recovered separately: a hand-launched full suite completed on device (`done:true, total 3295`) and the new `AsyncGenericCollectionStructArrayReturnTests` fixture passes **4/4 under NativeAOT**. That run mis-classified itself as Simulator (10 `Simulator:`-prefixed skips, 0 `Device:`) because the ad-hoc launch did not deliver `--platform device`; the *harness* delivers it correctly (its own JSONL carries `Device:`-prefixed skips), so there is **no arg-forwarding defect** and the one failure it reported (`MainActorTests.TestMainActorGuard_OffMainThread_Throws`, whose `[SkipOnDevice]` is correct as written) is an artifact of the ad-hoc launch, **not a device regression**. |
| A16 | — | **Process note: three commands that report success while doing nothing — verify the effect, never the return code** | A recurring failure family, hit three times in one session, each time producing a *confident false positive* rather than an error. (i) **`find -newermt`**: this machine's `find` is `bfs`, which rejects both `-newermt '-300 seconds'` and non-integer `-mmin`, failing with an error and empty stdout — piped through `\| wc -l` that reads as `0`, i.e. "tree is settled". Any "has editing stopped?" gate built on it agrees unconditionally. It cost a `nuke test` run (compiled a test file mid-write; three `CS8632`s against line numbers that no longer existed) and released a verification sweep against a tree still being written. Use integer `-mmin -N`, or epoch-diff: `newest=$(find … -exec stat -f '%m' {} \; \| sort -rn \| head -1); echo $(( $(date +%s) - newest ))`. (ii) **`pkill -f 'nuke <target>'` matches nothing**: `nuke` is a shell shim, so the string never appears in any process's command line — the real process is `dotnet run --project build/_build.csproj -- <target>`. `pkill` exits 0 having killed nothing while the run continues for another 15+s spawning fresh `xcodebuild`/generator children. Use `pkill -f '_build.csproj -- <target>'` and confirm by log-quiet, not exit status. (iii) **grepping a `nuke test` log for a test-class name proves nothing**: `--verbosity minimal` only names tests that *fail or skip*, so a fully-green class never appears — the grep returns 0 for a passing run and a nonexistent one alike. To prove new cases executed, enumerate them from the **TRX**, which lists every case regardless of outcome. General rule: when a control action gates something expensive, verify the *effect*, never the *return code*. |
| A17 | SB | **Paired Codex + Grok review of the whole uncommitted cycle** | **DONE 2026-07-27 — no High or Critical from either reviewer.** Both independently confirmed the two load-bearing claims: the static-merge redirect is **inert when `StaticallyMergedModules` is empty** on every path (Grok traced all five; Codex confirmed the `GetLibraryPath` choke point), and the false-green is closed — *"I did not find a path where a never-started launch can still score a previous run's green JSONL under the default (non-permissive) policy"* (Grok), *"no remaining stale-results acceptance path in the default policy"* (Codex). Both flagged the same honest caveat, already known: the `Tj-XM` change is **not** literally behaviour-identical to the old checker, because wrapper-bound `Tj` thunks previously short-circuited out of the rule entirely and are now judged — that is **new coverage, not a relaxation**. Two Mediums found, both in the harness rather than the generator, both **fixed and verified**: (1) *launcher aborts consumed crash-recovery retries* — found independently by both reviewers. The loops widened their bound to `maxRetries + launchAbortCount` but all six stop conditions inside compared the bare attempt index against `maxRetries`, so the comment promising separate budgets described behaviour the code did not implement; one abort followed by five recoverable crashes stopped recovery after four product attempts. Fixed by extracting the arithmetic to `build/Models/CrashRecoveryBudget.cs` so it exists exactly once (`grep "attempt == maxRetries\|attempt < maxRetries"` now returns nothing), with the "attempt N/Total" log lines corrected so the operator no longer reads "attempt 6/6" while the loop legitimately continues. (2) *`SimCtl.CopyResultsFromSandbox` abandoned the second candidate path* after a first-hit token mismatch, contradicting its own adjacent comment ("probe both shapes") — a stale `Documents/` artifact would mask the live tvOS result at `Library/Caches/Documents/`, discarding a complete correctly-tokened run. Fixed via `build/Models/SandboxResultProbe.cs`: a mismatch disqualifies *that candidate* only, fail-closed preserved (no matching token → null, never a fallback to the stale file), every rejected candidate still logged. Note both Mediums fail in the **safe direction** — they discard honest work rather than manufacture a green — but a device run left silently short of classes reads as a product regression, which is the expensive way to be wrong. Also fixed: the standalone `nuke BuildAsyncWrapper` target still discarded `RunBuildAsyncWrapper()`'s result and could exit 0 after a failed compile. Tests ship with all three (`CrashRecoveryBudgetTests`, `SandboxResultProbeTests`); the discriminating case is `LauncherAbortsDoNotConsumeCrashRecoveryRetries`, which asserts `IsExhausted(attempt: 5, maxRetries: 5, launchAbortCount: 1) == false` — `5 == 5` under the old arithmetic, so the assertion inverts against the buggy code. Unit floor **15989 → 16158**. One Grok finding deliberately **not** acted on: `BinaryDependencyAnalyzer.Analyze` discards import-only candidates when `otool` fails, but production routes through `DependencyClosureResolver`'s split `ScanLinkedDependencies` + `ResolveDetected` path, so on current evidence the one-shot API is reachable only from tests — a latent, and this repo's freeze policy sets a high bar for speculative generator edits. Sessions: Codex `019fa2e8-42d2-79b3-bc9c-005c03beb5ec`, Grok `019fa2e8-4d1e-7e40-a4f6-5aa5ec181c90`. |
| A18 | SB | **`--device-udid` is one parameter with two incompatible meanings, so `--mixed-pack --sim --device` can never run** | **Found during the gate-7 sweep 2026-07-27; FIXED and re-verified — the combined form now runs.** `Build.RuntimeTests.cs:44` declares a single `[Parameter("Pre-booted simulator or device UDID")] readonly string? DeviceUdid`, and four *simulator* branches trust it unconditionally: `Build.BindingTests.MixedPack.cs:432`, `Build.BindingTests.MixedDirect.cs:144`, `Build.RuntimeTests.cs:1548` and `:1799`. In a combined run both legs read the same value, so supplying the physical device's UDID hands it to `xcrun simctl install`, which dies instantly with `Invalid device: 559479FD-…`. **Both halves of that invocation are documented**, which is what makes it a trap rather than user error: the `CLAUDE.md` flag table advertises `--mixed-pack --sim --device` as the both-platforms form, and device runs are documented as requiring `--device-udid`. Follow both and the red accuses the simulator while the fault is argument routing — and it lands on a pre-release gate. Harness-only, pre-existing, no product impact; gate 7 was run as two invocations instead (7a sim, 7b device), which cost no coverage. Fix shape: the sim branches ignore `DeviceUdid` when `--device` is also set — `SimCtl.EnsureBootedDevice()` already resolves the simulator correctly, as gates 5 and 7a both demonstrated — and log the discard rather than doing it silently. A `--sim-udid` the caller must remember was rejected: a parameter that silently means the wrong thing is worse than one that is absent. **Landed as** `build/Models/SharedUdidRouting.cs` (pure statics, unit-tested by `SharedUdidRoutingTests`) plus one `Build.SimulatorUdidFor(legLabel)` helper that all four simulator sites now route through; the two device sites keep reading `DeviceUdid` directly because it is genuinely theirs. **Why no eight-gate re-sweep was needed:** `SharedUdidRouting.SimulatorUdid(udid, deviceLegRequested)` returns `udid` unchanged when `deviceLegRequested` is false, and `BelongsToDeviceLeg` is likewise false then, so nothing is even logged — every invocation without `--device` is behaviourally identical to the tree the eight gates were certified against. Re-verification is therefore scoped to `nuke test`, `--mixed-direct`, and the combined `--mixed-pack --sim --device --device-udid …` form itself. **The narrow keying on `Device` is load-bearing, not tidiness — CI would have broken without it.** Both `.github/workflows/ci.yml:127` and `.github/workflows/release.yml:222` pass a **simulator** UDID through `--device-udid` (`--device-udid "$SIM_UDID"`, forwarded by `build/scripts/ci/ci_ios_test.py:139-142` as `nuke binding-tests --sim --device-udid …`) and **neither passes `--device`**. The broader fix shape — "simulator branches ignore `DeviceUdid`" — would have made CI's tier-2 run and the release gate resolve an arbitrary booted simulator instead of the one the workflow had just booted and `simctl bootstatus`-waited on, and **none of the three re-verification gates would have caught it**, since none passes a simulator UDID without `--device`. Anyone revising this fix must keep the discard conditioned on `Device` specifically — and that cell is now protected by an assertion, `SharedUdidRoutingTests.WithoutTheDeviceLeg_TheSimulatorPinsTheSuppliedUdid`, rather than by anyone's reading of the truth table. **Re-verification results:** `nuke test` 16169/0/1 with the delta closing exactly (16169 − 16158 = 11, and `SharedUdidRoutingTests` contributes exactly 11 TRX results, so no other class moved); `--mixed-direct` PASS as the negative control, with **no** discard notice logged and the same simulator resolved as on the pre-fix tree; and the combined `--mixed-pack --sim --device --device-udid …` PASS, logging the discard, resolving its own booted simulator, and passing **both** legs in one invocation with zero `Invalid device` and zero duplicate-class registration. That combined run also **supersedes the split 7a/7b**: the structural gate ran once, produced a single 116,605-byte nupkg, and both the Mono-JIT simulator leg and the NativeAOT device leg consumed that one physical artifact — the "one nupkg instance executed on both runtimes" property the split could only infer is now observed. |
| A19 | SB | **Runtime-identity gate is inert on FOUR of five platforms — only the iOS Simulator is seeded — OWNER DECISION** | `build/baselines/runtime-identity-baseline.json` contains exactly one platform, `simulator` (verified at HEAD *and* in the working tree — the file's only working-tree change is `pass_count` 3250 → 3254 and the `git_sha` stamp). Every other leg logs `No runtime identity baseline for <platform> yet — identity gate inert until seeded`: **device, macos, maccatalyst and tvos_simulator all run with the identity ratchet switched off.** Device being unseeded matters most, because NativeAOT is the runtime where a test can silently stop existing rather than fail. On those four platforms only the *pass-count* floor is enforced, so a run that swaps *which* tests pass while holding the count constant is invisible. Deliberately **reported, not seeded**: seeding raises a ratchet, which is an owner-authorised act, not a gate-lane one — and seeding from a run this cycle would freeze in the current skip set, including the classified Catalyst leak flake. Decide before the cut whether to close this, and if so seed device first. |
| A20 | SB | **Five runtime pass-count baselines are already raised in the working tree — OWNER DECISION (keep or revert)** | The runtime baselines live in `build/baselines/validation-baseline.json` under `runtime_tests` — **not** `BindingTests/baselines.json`, which holds only coverage ratchets and was never touched (mtime still Jul 16). Comparing HEAD to the working tree: `macos` 2001 → **2592**, `maccatalyst` 1909 → **2587**, `tvos_simulator` 1532 → **2691**, `device` 3264 → **3268**, `simulator` 3250 → **3254**. All five were written automatically by the harness during this cycle's runs and are uncommitted. This supersedes the A6 note that the Mac improvements were "warn-only and not auto-written" — they were written. **Why it is safe:** a raised baseline is a ratchet, so it is only sound if the number came from a genuinely green run on the current tree — and gate 8 re-measured all three desktop platforms against the fully-patched tree at **exactly** the raised values (macOS 2592, Catalyst 2587, tvOS 2691), with device 3268 and simulator 3254 likewise measured this cycle. The Catalyst ratchet is demonstrably live: the flaked first attempt at 2586 was correctly rejected against the 2587 baseline. The A6 precondition — "reseed only from a run measured with *all* of this cycle's patches" — is therefore satisfied rather than outstanding. Owner's call is now simply whether to commit these five raised values, not whether to reseed. **UPDATE 2026-07-27: committed as isolated commit `d3929da7` under the owner's blanket wave-commit authorization; a revert, should the owner rule the other way, is a single `git revert d3929da7`.** |
| A15 | SB | **Device/sim harness reported a false green on a run that executed nothing** | **HIGH — root-caused 2026-07-27, fix in flight.** `DeviceCtl.CopyResultsFromSandbox` (and the simctl twin) pull `Documents/test-results.jsonl` from the app's **persistent** data container, which survives reinstall, with **no freshness check of any kind**. In the A5 run all six launch attempts failed outright (the process never started) yet each "recovered" results and logged `3264 pass, 0 fail, 27 skip, 0 crash (done=True)` — all six recovered files byte-identical (372149 bytes, one md5, `total:3291`). **The run went red only by luck**: the stale artifact predated the newly-added fixture, so the class-inventory diff (356 vs 357) kept the retry loop alive. Had the stale file covered every class, the gate would have certified a green device run that executed nothing. Compounding it, `LaunchFailure` shares a branch with `Crash`/`Timeout` (`Build.RuntimeTests.cs` ~:1560, ~:1845), so a launch that never happened drives class-**exclusion** recovery — and that crash-recovery path is precisely what reads the stale file. **Fix:** a per-launch-*attempt* opaque run token passed via `--run-token`, written+flushed as the JSONL's first line so it survives a genuine crash, and required at recovery on all five hosts; a token-less or mismatched file is discarded exactly like a failed copy, degrading to an honest red. Validation rather than pre-launch blanking is deliberate — a delete that silently fails is indistinguishable from stale data, whereas a token check also catches a file that simply never got overwritten. **DONE 2026-07-27.** Token generated per attempt as `Guid.NewGuid().ToString("N")`, threaded through all five hosts, written and flushed as JSONL line 1 (`TestResults.cs:45`); recovery validates it in `SandboxResultProbe` (`build/Models/SandboxResultProbe.cs`, 7 unit cases) which rejects **per candidate** rather than per probe, so a stale `Documents/` file left by an earlier iOS run can no longer hide a live tvOS result at the `Library/Caches/` path. Both reviewers independently searched for a surviving stale-acceptance path under the default (non-permissive) policy and found none. **Honest limitation:** the confirming device run had zero launcher aborts, so the token *reject* arm and the extended crash-recovery budget were exercised at the unit layer only, not live. Two further defects fell out of the same investigation and were fixed with it — the crash-recovery budget being consumed by launcher aborts (`build/Models/CrashRecoveryBudget.cs`, 12 unit cases, all ten call sites rewired off `attempt == maxRetries`) and the misleading per-candidate probe warning. |
| A6 | SB | `nuke binding-tests --macos` / `--catalyst` / `--tvos` | **DONE 2026-07-27 — all three GREEN: macOS 2592/0/32, Mac Catalyst 2587/0/37, tvOS 2691/0/37.** Two notes carried forward. (i) The macOS/Catalyst greens required one further harness fix beyond the generator root-cause below: both hosts are plain executables with no NSApplication/UIApplication, so nothing installs a `SynchronizationContext` and every `await` continuation resumed on a threadpool thread — tripping the runtime's main-actor guard *correctly*. A `MainQueueSynchronizationContext` posting to `DispatchQueue.MainQueue` (the same mechanism Swift's own main-actor hops use) fixes it; skip counts are unchanged, so this is a fix, not a suppression. (ii) The harness logged **IMPROVEMENT** on both Mac lanes (macOS 2001 → 2592, Catalyst 1909 → 2587); those are warn-only and not auto-written, so `BindingTests/baselines.json` wants reseeding from a run measured with *all* of this cycle's patches in the tree — deliberately not reseeded from the earlier numbers. **Open item handed back to the main loop:** `RunBuildAsyncWrapper`'s return value is ignored at four sites in `Build.RuntimeTests.cs` (sim/device, macOS, Catalyst, tvOS); only `Build.BindingTests.cs:1025` honours it. This is the same "a failure that doesn't fail" shape as A15 and it fired for real here — the Catalyst leg soft-failed its wrapper build, proceeded, and reported `121 passed / 1296 failed` pointing at the bindings instead of one honest build error pointing at the wrapper. Decision taken: honour it and hard-fail by default, with `--permissive` demoting to a warning, matching that flag's existing compile-gate semantics. Original diagnosis follows — all three were red for **three unrelated causes**, none introduced this cycle. **tvOS: GREEN** after two harness fixes — (1) the runner executed the whole suite on a threadpool thread (`Task.Run` in `RuntimeTestsApp.tvOS/Program.cs`) while iOS kicks it off on the main thread, so 17 tests failed on the runner's threading model rather than the binding: 13 `MainActorTests` + 1 `GenericMainActorDispatchTests` hit the runtime's main-actor guard and 2 `BridgeSimpleViewTests` hit UIKit's main-thread assertion — all firing *correctly*; now enqueued via `BeginInvokeOnMainThread` so `FinishedLaunching` still returns promptly. (2) `SimCtl.CopyResultsFromSandbox` probed only `<container>/Documents`, but tvOS has no persistent Documents dir so .NET maps `SpecialFolder.MyDocuments` to `<container>/Library/Caches/Documents` — the JSONL was written but never collected, and the Finding-27 gate then (correctly) refused to certify a green run on the console marker alone. Collector now probes both shapes. Result: **2691 passed / 0 failed / 37 skipped**, and the collector fix is verified end-to-end — the run certifies on a real artifact (`JSONL: 2691 pass, 0 fail, 37 skip, 0 crash (done=True)`), not on the console marker, so Finding 27 is satisfied rather than bypassed. Note `DeviceCtl.cs:176` is deliberately left on the `Documents/` path: iOS device has a real per-app Documents dir. **Catalyst: harness defect fixed, then revealed the SAME generator defect as macOS** — the fixture died in ~1s at `swiftc -emit-module`, before the generator ever ran, with `could not build Objective-C module 'StoreKit'` ← `'UIKit/UIKit.h' file not found`. Root cause is ours, not Apple SDK unavailability: a `-macabi` target builds against the macOS SDK but resolves iOS-flavoured frameworks out of `$SDKROOT/System/iOSSupport`, which was not on the search path, so StoreKit resolved to the *macOS* headers and died inside them. Proven by direct probe — a file containing only `import StoreKit` fails under `-target arm64-apple-ios15.0-macabi -sdk <MacOSX.sdk>` and compiles (exit 0) once `-Fsystem $SDKROOT/System/iOSSupport/System/Library/Frameworks` is added; the framework path alone is sufficient (`-I` alone is not). Wired into `CompileModuleSlice` (both the `swiftc` compile and the `swift-frontend` ABI dump, which re-parses the same imports) and `ExtractSymbolGraph`. With that fixed the fixture builds and generation runs — and lands on the **same generator defect as macOS**: `--platform maccatalyst` exits 1 emitting zero C# for the main module, with `12× SWIFTBIND093`, `3× SWIFTBIND024`, `1× SWIFTBIND022`, `1× SWIFTBIND111` across the *same three* site families as macOS (`GetOptionalTag` ×2 + `MockBook`/`MiniEntityProperty` getter/getSetter ×5 hashes) — a superset of macOS's 7. So macOS and Catalyst are **one root cause, not two**; the Catalyst reproduction is now part of the agent's mandate. Secondary observation (not changed — flag semantics are a product call): the macOS/Catalyst regen path treats a non-zero generator exit as a warning by default (`"This is expected if the test library includes features beyond current generator support"`) and proceeds to build the app against bindings that were never emitted, turning one real error into ~2000 downstream `CS0246`s. `--strict` makes it fail-closed, and `--compile-only` is fail-closed by default; consider whether the platform runtime gates should be too. **macOS: real generator defect, pre-existing** — generator exits 1 emitting zero C#: `SWIFTBIND111 RecoveryNonConvergence` at stage `SwiftCompile`, driven by 7× `SWIFTBIND093 (CC-003)` across 3 sites (`PInvoke_GetOptionalTag → SBW_GetOptionalTag_SwiftBindingsTestLib_{SafeDiv,RangedInt}`, plus a `MockBook`/`MiniEntityProperty` getter). Proven pre-existing by building the generator at HEAD in a separate worktree: all 7 diagnostic fingerprints identical (`2145B14F, 528B8CF1, 5CFA7E15, 6E53095F, 79EA8CDF, AA407EA1, CBFCE413`). **ROOT-CAUSED AND FIXED 2026-07-27.** Three hand-rolled wrapper P/Invokes wrote the string literal `"SwiftBindings"` as their bound library instead of resolving the wrapper name this run was configured with, so every `SBW_` entry point they declared was bound against a library that does not export it — exactly what CC-003 exists to catch. Sites: `WrapperEmitter.FailableFactory.cs:283` (the `PInvokeHelperContext` branch) and `:295` (the direct-writer branch), and `ConformerKeyPathInitFactoryEmitter.cs:401` (the `SBW_EPF_` trampoline). All three bypassed `PInvokeEmitter`, which resolves the library correctly at `PInvokeEmitter.cs:1256-1257`; only the hand-rolled sites were wrong. **It is not platform-specific — it is `--async-library`-specific.** The iOS/tvOS/device lanes pass `--async-library {WrapperModule}` where `WrapperModule = "SwiftBindings"` (`Build.BindingTests.cs:21,402`), so the literal *coincidentally matched* and the bug was invisible on the lane that runs constantly; the macOS/Catalyst lanes go through `RunRegenerateMacOSBindings` (`Build.RuntimeTests.cs:2804`), which deliberately omits the flag, so the wrapper auto-defaults to `{Module}SwiftBindings` (`BindingsGeneratorCommand.cs:681-686`) and the literal no longer matched. Confirmed by a 2×2 at HEAD: iOS *without* the flag also fails (12× 093, zero `.cs`); macOS *with* `--async-library SwiftBindings` passes (exit 0, 1293 `.cs`). The "7 vs 12" spread was deduplicated fingerprints vs raw log lines — the same 7 distinct sites in both. Fix: new single resolver `PInvokeEmitHelper.ResolveWrapperLibrary(ITypeDatabase)` (+ `DefaultWrapperLibraryName` for the no-wrapper-configured case, which preserves prior behaviour exactly); both emitters route through it. No CC-003 weakening, no new prediction gate, `AbiContractChecker` untouched — the fail-closed `Unattributable` path stays as designed (`AbiContractChecker.cs:396-402`), because the correct fix was to stop emitting the bad binding, not to teach the checker to tolerate it. Tests: `EmitterTests/WrapperLibraryBindingTests.cs`, 6 cases including one asserting cleanliness *through `AbiContractChecker` itself* and a source-level drift guard (`NoEmitterHardcodesAWrapperLibraryName`) that scans the whole `src/Emitter` tree and fails on any `LibraryImport("…SwiftBindings")` / `LibraryPath = "…SwiftBindings"` literal. Verified RED 4/6 → GREEN 6/6; iOS lane output byte-identical after the fix (1293 files, 479476 lines, zero differing `.cs`). **Blast radius beyond this repo:** every third-party binding auto-defaults its wrapper name, so any consumer library emitting a failable init on a frozen struct or a conformer KeyPath-init factory got *zero* C#. `build/Build.Validation.cs` does not pass `--async-library` either, so `nuke validate`'s 132/132 green run exercised the auto-default path — meaning no corpus library happens to hit those two emitter shapes, which is why the corpus never caught it. **Follow-up opportunity, deliberately NOT taken in this pass (cleanup, not a defect):** `RunRegenerateMacOSBindings` omits `--async-library` and then *textually rewrites* the emitted C# (`Build.RuntimeTests.cs:2862-2876`, `"{Module}SwiftBindings"` → `"SwiftBindings"`) to match the separately-compiled wrapper. That rewrite is why the hand-rolled literals were accidentally right for this lane's *final* state — but CC-003 runs inside the generator, long before the rewrite, so the generator failed closed and there was never any C# left to rewrite. The fix and the rewrite are consistent, so the lane needs no further change. However, the standing comment at `:2833-2836` claiming `--async-library` "cause[s] the generator to produce no C# output when combined with desktop-class platforms (generator limitation)" is **contradicted by measurement** — macOS *with* `--async-library SwiftBindings` exits 0 and emits 1293 `.cs`. If that holds up under a dedicated check, the flag can simply be passed and the whole string-rewrite post-process deleted. Left alone here to keep this pass's blast radius to the root-cause fix. |
| A7 | SB | `nuke binding-tests --partial-success-kitchen` | **DONE** 2026-07-27: **PASSED**. Exact frozen-baseline match against `git_sha 4ae2a813`, `ReviewCount == 0`, both positive controls (`KitchenOk`, `KitchenOkClass`) declared, emitted C# compiled, generator exit 0, no SWIFTBIND108. No reseed needed — the sharpest skip-surface detector we have reports zero drift from the Lane A fixes. |
| A8 | SB | `nuke binding-tests --mixed-pack` (sim + device) | **DONE** 2026-07-27 — **PASS on both legs**, run as two invocations (see A18 for why the combined form could not run). Sim (Mono JIT): structural OK — `source dropped, wrapper device slice carries _OBJC_CLASS_$_SbMixedPackProbe, companion embedded in lib/ios slice` — plus `consumer-run OK — ObjC type usable through single PackageReference, class registered once`. Device (NativeAOT, real `devicectl` tunnel to the iPhone): structural re-asserted identically, same consumer-run assertion. **Zero occurrences of "implemented in both" in either log** — the issue #40 shape stays closed on both runtimes. The device publish was confirmed genuine rather than an incremental no-op (`obj/` created fresh, 4,204,384-byte native binary stamped that run). |
| A9 | SB | `nuke binding-tests --mixed-direct` | **DONE** 2026-07-27 — **PASS**, 1:57. Both assertions fired: structural — `generated ObjC companion dropped its source NativeReference (Gap 2), so the static archive is linked exactly once` — and runtime on the iPhone 17 Pro Max simulator — `ObjC type usable in SDK-direct mode (companion referenced into the app's own compile), class registered once`. Consumer emitted `OBJC_GREETING:objc-mixed-ok` → `TEST SUCCESS`. Discounted noise, each checked: ~20× `MT7154` BundleResource `LogicalName` collisions on `Swift/*Database.xml` (known-benign dedup family, warnings only, pre-existing), 6× `MSB3026` retry-1 file locks on `Swift.Runtime.dll` during the parallel multi-TFM Apple pack (MSBuild retried, build exit 0), and `SWIFTBIND061: 2 of 7 Swift members were skipped` (the fixture's designed shape). |
| A10 | SB | `nuke binding-tests --appstore-hygiene` | **DONE** 2026-07-27 — **PASS**, 0:28, both legs on real artifacts. Structural nupkg leg: `framework xcframework preserved (device arm64, sim arm64+x86_64), no loose dylib, no injector script` with `lipo -archs` run on both extracted slices. Built-IPA leg (real `ios-arm64` Release publish, real `.ipa`, real `ditto` extraction — nothing simulated): `runtime embedded as a signed SwiftBindingsRuntime.framework (@rpath install_name), no loose dylib, zero embedded libswift*.dylib, no SwiftSupport/ folder, app signature verifies`. `codesign --verify --strict` ran twice and passed — once on `Frameworks/SwiftBindingsRuntime.framework`, once on the `.app`. |
| A11 | SB | `nuke binding-tests --skip-surface` | **DONE** 2026-07-27: **PASSED**, 3:26. Compile gate plus the skip-class trend ratchet against `build/baselines/skip-surface-baseline.json` — no class regressed. The `[ERR]`-prefixed lines in the log are `swiftc` warnings (existential-any, deprecated-declaration) that Nuke routes to stderr, not failures. |
| A12 | PKG | Downstream regression across all library + Apple-framework cells | **VERIFY** — last run `20260727-000956.281Z` was 13/13 non-device green, 9 device cells environmental. Needs a clean re-run against the committed generator. |
| A13 | IBT | `run-all-sim.sh` / `run-all-device.sh` over the 15 library cells | **VERIFY** — not run since the fix. |
| A14 | IBT | `corpus-sweep` (120 libraries) | **VERIFY** — last known 58/120 green; 6 compile-reds root-caused (see D1). |

---

## B. Confirmed defects — status per row (many are FIXED-IN-TREE in the uncommitted wave; read the leading status marker before starting any work)

| ID | Repo | Defect | Evidence / site |
|---|---|---|---|
| B1 | PKG | **[FIXED-IN-TREE, uncommitted — Lane B complete; commit gated on 0.18.0 reaching NuGet, see START HERE]** **Auto-detected `ProjectReference` injector deletes correct references.** `ApplyProjectRefs` strips the existing block — including hand-authored sibling refs — *before* deciding whether to re-emit, and `BuildPrBlock` returns `""` on a zero-hit scan. The scan is a literal `\bModule\.` token grep over generated wrapper C#, so any reference not surfacing as that exact text reads as zero hits and the ref is dropped, never replaced. **This is the root cause of the 25 missing managed references SWIFTBIND081 caught during the 0.18.0 run** — the generator was right, the fixtures were mis-wired. | `build/Helpers/CsprojRewriter.cs` (`ApplyProjectRefs`, `BuildPrBlock`, `MigrateSiblingProjectRefs`, `StripPrAutoBlock`); `GeneratedCsScanner.ContainsModuleReference`. Proven by commit `eb1b24cd`, which deleted StripeConnect's and StripeIdentity's `../StripeCore/…` refs with no replacement. **Still live**: untracked `libraries/Facebook/FBSDKCoreKit` and `FBAEMKit` already show SFD-without-PR. |
| B2 | PKG | **[FIXED-IN-TREE, uncommitted — Lane B complete; commit gated on 0.18.0 reaching NuGet, see START HERE]** **PR injection is all-or-nothing on freshness; SFD injection is not.** `InjectProjectRefsForLibrary` freshness-checks *every* product before any write, so one stale product silently aborts `ProjectReference` injection library-wide, while `InjectFrameworkDepsForLibrary` writes per-product regardless. That asymmetry is how the two marker blocks drift apart unnoticed. | `build/Build.Dependencies.cs` (freshness pass ~L194–203); contrast `InjectFrameworkDepsForLibrary` ~L72–153. |
| B4 | SB | **`nuke validate` never supplied dependency xcframeworks to the generator — FIXED 2026-07-26.** `GenerateTarget` (xcframework mode) invoked the generator with a bare `--xcframework` and no `--framework-dependency`, so the compile-import graph was never closed for any multi-module SDK. Since the ingestion-hardening closure gate landed (2026-07-21) that is a hard **SWIFTBIND119 / `InputClosureUnsatisfied`** at Parse, emitting **zero** C#. It took out 12 cells: FirebaseAuth, FirebaseCrashlytics, FirebaseDatabase, FirebaseFirestore, FirebaseFunctions, FirebaseMLModelDownloader, FirebaseRemoteConfig, FirebaseSessions, FirebaseStorage, GTMAppAuth, StripeCryptoOnramp, StripeIssuing — plus every `skipped (dependencies not resolved)` and a large share of the compile failures downstream of them. **The generator was correct throughout; the harness was stale.** Proof: GTMAppAuth imports `AppAuth` + `GTMSessionFetcher`, both already sitting beside it in `.libraries/Firebase/`; re-running with the two `--framework-dependency` args gives exit 0, 8 files, 4834 lines (baseline 4721) and no failure report. Fix: extracted the dep-collection already present in `CompileWrapper` into `CollectFrameworkDependencyArgs` and called it from both paths. | `build/Build.Validation.cs` — `GenerateTarget` (was L741), `CollectFrameworkDependencyArgs`. |
| B5 | SB | **`nuke validate` LINE DRIFT measured one arbitrary file — FIXED 2026-07-26.** `FindMainCsFile` returned `Directory.GetFiles(outdir, "*.cs").FirstOrDefault()` — **filesystem order, unsorted, not stable across runs**. That was ~accurate when the emitter produced a single monolithic `{Module}.cs`, but it now splits output per type (`{Module}.Types.{X}.cs`), so the metric measured a random fraction of the binding. Result: phantom 95–100% "drift" across essentially the whole corpus while emission was actually flat or *up* — **Alamofire reported `59665 -> 362` when it really emitted 62,284 lines across 97 files (an increase)**; `362` is literally `Alamofire.Types.RetryResult.cs`. `Quick: 137 -> 328 (139%)` is the same bug pointing the other way. This is worse than noise: it would equally well have hidden a genuine emission collapse. Fix: `CountEmittedCsLines` sums every emitted file; `EmittedCsFiles` sorts ordinally so the metric is deterministic. **Baseline `Lines` values are now incomparable to the old ones (per-file → total), so a re-baseline is mandatory once the run is green.** | `build/Build.Validation.cs` — `FindMainCsFile` / `EmittedCsFiles` / `CountEmittedCsLines` (was L2321). |
| B6 | SB | **The dependency-tier compile gate only ever compiled ONE file per binding — FIXED 2026-07-26.** `WriteDependencyCsproj` emitted `<Compile Include="{single filename}" />`, taking that filename from `FindMainCsFile` (see B5, arbitrary). Since the emitter splits output per type, every dependency-gate "OK" was reached with the compiler having seen a small fraction of the binding. Fix: mirror the generated csproj's real item set — `EnableDefaultCompileItems=false` plus `{Module}.cs`, `{Module}.Types.*.cs`, and the conditional `.Wrappers.cs` / `.SwiftUIBridge.cs`. Effect was immediate and large: the dependency gate went from `1/8 tested, 7 failed` to `13/13 tested, passed` once it was compiling whole bindings against real DLLs. | `build/Build.Validation.cs` — `WriteDependencyCsproj`. |
| B7 | SB | **Dependency DLL resolution was blind to pure-ObjC bindings — FIXED 2026-07-26.** `FindDependencyDll` searched only `<outdir>/bin` for `{Module}.dll` / `{Module}.Swift.{Platform}.dll`. A pure-ObjC binding emits `{Module}.ObjC.{Platform}.csproj` and builds to **`bin.objc/`**, so any Swift module depending on one (FirebaseAuth → FirebaseCore, FirebaseRemoteConfig → FirebaseCore) resolved nothing and the dependency gate parked it as `skipped (dependencies not resolved)` forever. 26 of the corpus's targets build this way. Fix: search `bin` and `bin.objc`, and add `{Module}.ObjC.{Platform}.dll` to the candidate names. | `build/Build.Validation.cs` — `FindDependencyDll`. |
| B8 | SB | **Import parser dropped dependencies to an attached comment — FIXED 2026-07-26.** The derived-dependency walk read module names by cutting the text after `import ` at the first `.`. FirebaseFirestore's interface declares `@_exported import FirebaseCore/*.Timestamp*/` — a block comment attached with no separating space — so the cut landed inside `/*.` and produced `FirebaseCore/*`, which matches no sibling. FirebaseCore was silently omitted and FirebaseFirestore failed to generate. Fix: take only leading identifier characters, and skip the declaration-kind token in `import struct Foundation.Data` forms. **Generic lesson: `.swiftinterface` import lines carry comments and declaration kinds — never parse them by delimiter-splitting.** | `build/Build.Validation.cs` — `ReadInterfaceImports` / `TakeIdentifier`. |
| B9 | SB | **[FIXED, COMMITTED `f4e785d5` — Lane A complete 2026-07-27, one fix with two recording arms shared with B10; verified by the 132/132 validate re-run (A3). See §F Lane A status.]** **Apple-supplement references are recorded by a hard-coded two-identity allowlist, so a binding that uses any *other* supplement type ships a csproj with no `SwiftBindings.Apple` `PackageReference` and cannot compile.** `AppleSupplementReferences.Record` is called from ~15 emitter sites, every one of them hard-coding `"Foundation.Data"` or `"Foundation.AnyError"`; the only data-driven caller is `TypeDatabase.TryResolveAppleSupplementArm`, which fires solely for identities present in `apple-types-manifest.json` (8 Foundation entries). But the **XML type databases** also project into supplement-owned namespaces — `FoundationDatabase.xml` maps `Foundation.{Data,URL,URLRequest,Measurement}` → `Swift.Foundation.*` and `ManagedSettingsDatabase.xml` maps 4 more → `Swift.ManagedSettings.*` — and **all nine of those managed types live only in `SwiftBindings.Apple`**, none are in the manifest. That path never records. Net: `URL`, `URLRequest`, `Measurement` and the ManagedSettings four are emitted as references to an assembly the csproj does not reference. **Takes out all 4 WeatherKit cells** (`Swift.Foundation.Measurement` ×262 → 131 × CS0234, `AppleSupplementReferences: []`). Alamofire is the near-miss that shows the shape: it emits `Swift.Foundation.URL` ×20, `URL` is absent from its recorded references, and it only links because `Data`/`AnyError` happened to be recorded too. **Latent since `9acf4bfa` (2026-04-18) moved these types out of `Swift.Runtime` into `SwiftBindings.Apple`; surfaced now because `b32401d1` (2026-07-19) started gating publication on whether the emitted C# compiles.** The baseline's `ok(0)` was false confidence, not a passing state. Fix shape: make the oracle the *resolved managed projection's owner* rather than a name allowlist — record whenever a resolved `TypeRecord`'s namespace is supplement-owned, so XML-DB entries and manifest entries record identically. | `src/Swift.Bindings/src/TypeDatabase/AppleSupplementReferences.cs`; `TypeDatabase.TryResolveAppleSupplementArm` (`TypeDatabase.cs:662`); `src/Swift.Runtime/src/Swift/FoundationDatabase.xml:4,7,19,439`, `ManagedSettingsDatabase.xml:7,12,16,20`; supplement sources under `src/Swift.Bindings.Apple/Sources/{Foundation,ManagedSettings}/`. |
| B10 | SB | **[FIXED, COMMITTED `f4e785d5` — Lane A complete 2026-07-27, shared fix with B9; RealityKit auto-emits the sibling `PackageReference` with no hand authoring (see B12). See §F Lane A status.]** **Cross-module dependency types emit no sibling reference.** ⚠️ **Diagnosis corrected a SECOND time, 2026-07-26 (step 0) — the namespace-resolution half of the previous row was wrong.** Reproduced at the exact validate arg shape with `RealityFoundationDatabase.xml` threaded via `--module-database`: namespace resolution is **correct** — zero `RealityKit.Entity`, and instead `RealityFoundation.Entity` ×59, `RealityFoundation.IEvent` ×20, `RealityFoundation.IHasCollision` ×17 plus 7 more `RealityFoundation.*` types. The empty `RealityKit.Types.Entity.cs` shell is likewise **correct**: `Entity` belongs to RealityFoundation, so RealityKit rightly emits nothing for it. The sole defect is that the generated csproj carries **no reference to the sibling RealityFoundation binding**, so every one of those correctly-qualified references fails to resolve → `CS0246 'RealityFoundation' could not be found` (65 errors; the prior row's "131 × CS0234" came from a different, unidentified condition). Root shared with **B9** and confirmed at step 0: `BindingProjectEmitter` has no dependency/sibling reference emission at all — its only conditional reference is the Apple supplement, gated on `AppleSupplementReferences.Any`. **Fix shape: derive the emitted csproj's reference set from the namespaces the emitted C# actually uses**, with one recording arm for supplement-owned namespaces (B9) and one for dependency-module namespaces (B10). **This is NOT a missing Apple workload binding** — an earlier revision of this row claimed `autoBridge` asserted a nonexistent `RealityKit` namespace in `Microsoft.iOS.dll`; that byte-scan was accurate but irrelevant, since the bridge target was never meant to come from the workload. The shipped `SwiftBindings.Apple.RealityKit` package works precisely because its csproj carries a **hand-authored** `<PackageReference Include="SwiftBindings.Apple.RealityFoundation">` whose comment records "Generated bindings reference RealityFoundation.Entity" — packages-repo regression cells for RealityKit pass sim + device. So the defect is that the generator neither resolves a dependency-module type to its owning namespace nor auto-emits the sibling reference the way the hand-authored csproj does. Likely shares a root with **B9** (cross-module type referenced without its package reference). Same 2026-07-19 verification gate surfaced it. | Repro `RealityKit.Types.Entity.cs` (empty shell) + 49 × `RealityKit.Entity` across generated output; contrast `swift-dotnet-packages/apple-frameworks/RealityKit/SwiftBindings.Apple.RealityKit.csproj` (hand-authored sibling `PackageReference`); config `build/validation-libraries.json:88–100`. |
| B11 | SB | **[FIXED, COMMITTED `f4e785d5` — Lane A complete 2026-07-27: closure iterated to a fixpoint (`DependencyClosureResolver`, 19 unit cases); ingestion-kitchen gate covers it (A2). One documented boundary: ObjC-only siblings are not traversed — fails safe, routed to not-planned. See §F Lane A status.]** **Mach-O-derived dependency auto-detection runs on the primary only, never to a fixpoint — so an auto-added dependency's own `@_exported` imports go unsatisfied and the closure gate hard-fails.** The generator seeds extra `-F` roots from the primary binary's `LC_LOAD_DYLIB` list (that is how `Stripe` and `StripeFinancialConnections` entered a `StripeCryptoOnramp` run despite no interface importing them and neither being a declared dependency). Once `Stripe` is an input, its `@_exported import StripeIssuing` becomes a public compile-import edge that must close — but the link scan is never re-run on auto-added deps, so `StripeIssuing` is never added, and the run dies **SWIFTBIND119 / `InputClosureUnsatisfied`** at Parse with zero C# emitted. `StripeIssuing.xcframework` is sitting in the same directory the whole time, and it is *not* in the searched-roots list the diagnostic prints (along with StripeCardScan and StripeConnect). **This is not a harness artifact** — a third-party consumer pointing at `StripeCryptoOnramp.xcframework` with siblings co-located reproduces it exactly. Fix shape: iterate dependency auto-detection to a fixpoint (re-scan each newly-added dep's public imports *and* link list against the sibling search roots) instead of one pass over the primary. | `src/Swift.Bindings/src/Ingestion/BindingInputClosurePreflight.cs`; `InputInventory.FromCliInvocation` (`InputInventory.cs:117`); `XCFrameworkResolver.ResolveSiblingFrameworkSearchPaths` (`XCFrameworkResolver.cs:653`). Evidence: `Stripe.xcframework/…/x86_64-apple-ios-simulator.swiftinterface:12`. |
| B13 | SB | **The verify-recover loop is absent from the apple-framework/direct generation mode.** Discovered at step 0 (2026-07-26). `verifyRecoverCompile` is constructed only when `resolution != null`, and `resolution` is assigned only in the `--xcframework` branch; direct mode therefore passes `compileWrapper: null` into `GenerateBindings`, which takes the ordinary single-render arm and never constructs `InEmissionDriver`. Net: every Apple-framework binding gets only the single-shot post-loop publication gate (`SWIFTBIND113`, fail-closed) with **no** withdrawal/re-emit safety net — a mode-shaped hole in a mechanism the rest of the repo treats as universal. Proven empirically: WeatherKit and RealityKit each run exactly one `C# verification build:` and log zero `verify-recover`/`SWIFTBIND114`/`withdraw`/`denylist` markers. **Not** on the 0.18.0 critical path — B9/B10 are fixed at the reference-emission root, so the net is not needed to make those cells green — but it is false confidence anywhere else in the Apple-framework catalog, and it should be an explicit owner call rather than an accident of where `resolution` happens to be assigned. | `src/Swift.Bindings/src/BindingsGeneratorCommand.cs:565` (`resolution` assignment), `:999` (`verifyRecoverCompile` gate), `:2850` (`CanVerifyCSharpInLoop`); `src/Swift.Bindings/src/Program.cs:979` (single-render arm). |
| B12 | PKG | **Hand-authored sibling `PackageReference`s may be masking B10 across the Apple-framework catalog.** `apple-frameworks/RealityKit/SwiftBindings.Apple.RealityKit.csproj` carries a manually-written `<PackageReference Include="SwiftBindings.Apple.RealityFoundation" Version="26.2.8" />` with the comment *"Generated bindings reference RealityFoundation.Entity, so consumers need the sibling package on restore. ProjectReference alone would not propagate to nuspec `<dependencies>`."* That line is exactly the work B10 says the generator should be doing itself — which means **any other multi-module Apple framework whose sibling reference was hand-added is green for the wrong reason**, and any that was *not* hand-added may be shipping broken. Commit history shows this has churned before (`ae9aaee` added explicit refs for RealityKit + BlinkIDUX; `438e992` reverted BlinkIDUX as "auto-emitted by ProjectReference"), so the auto-injection path already exists and is inconsistently effective. **Action: once B10 is fixed, sweep every `apple-frameworks/*/*.csproj` for hand-authored sibling `PackageReference`s, confirm the generator now emits them, and remove the manual ones.** Until then the packages-repo green signal for multi-module frameworks does not prove the generator is correct. **PRECONDITION MET 2026-07-27, removal deliberately NOT done — it is owner-sequenced.** The 132/132 validate run proves the generator now auto-emits it: `RealityKit.Swift.iOS.csproj:63` carries `<PackageReference Include="SwiftBindings.Apple.RealityFoundation" Version="[26.2.4,26.3.0)" />` with no hand authoring, and the reverse direction is correctly absent (RealityFoundation emits no RealityKit reference — the asymmetric umbrella filter). The two known hand-authored refs are `apple-frameworks/RealityKit/…csproj:21` → `SwiftBindings.Apple.RealityFoundation` and `apple-frameworks/MatterSupport/…csproj:26` → `SwiftBindings.Apple.Matter`. **They must NOT be removed yet**: `swift-dotnet-packages` still pins SDK 0.16.0, which lacks this fix, so deleting the manual refs before the pin moves would build genuinely broken packages. Correct sequencing is to remove them *in the same change that bumps the packages repo to the SDK version carrying the fix* — i.e. after 0.18.0 is on NuGet. | `swift-dotnet-packages/apple-frameworks/RealityKit/SwiftBindings.Apple.RealityKit.csproj`; sibling `apple-frameworks/RealityFoundation/`. |
| B14 | SB | **Skip-report fidelity: members of a suppressed parent type are invisible to `PublicSurfaceLost`.** When a whole type is suppressed (e.g. by the SwiftUI gate), its members are never enumerated — they are counted in `TotalTypes`/`TotalMembers` but recorded as neither emitted nor skipped, so the "lost public surface" metric under-counts exactly where suppression bites hardest. Cleanest illustration: **`AlertToast` — 20 members, 0 emitted, 5 skips recorded, 15 unaccounted.** The same mechanism is why the zero-surface `@_spi` modules (C5) read `PublicSurfaceLost: 0`. This is a **reporting** defect, not a binding defect — the bindings are correct; the accounting of what didn't bind is incomplete, which matters because C2-style triage (and any future "no silent skips" audit) relies on that report being exhaustive. Separable, unfunded. Full analysis: `src/docs/c2-c3-triage-2026-07.md` §1.5, §1.7 and the AlertToast section. |
| B15 | SB | **Static-merge library naming: the generator emits P/Invokes carrying the vendor module's name when the vendor native is a static archive that ships only merged into the wrapper — CONFIRMED generator defect; FIXED, COMMITTED `f4e785d5`, verified by fresh regen 2026-07-27.** This is the generator-side root cause of C4, registered per the owner's direction ("if we're doing something wrong which causes a wrong library name lookup, we should mark it as such for a future pass to fix"). When the source xcframework's native slice is a static `ar` archive, packaging force-loads it into the Swift wrapper and drops the source from every consumer reference (`NativePackagingPolicy.ShouldIncludeSourceXcframework` → false), so no loadable image by the vendor's name ever exists — yet the pre-fix generator baked the vendor module name into every emitted `LibraryImport` for that module's symbols (shipped Mappedin 6.4.2: all 403). That name resolves today only by runtime accident (Mono loader fallback / NativeAOT link-time resolution) and throws under CoreCLR (C4's host experiment). **The fix is in the working tree, uncommitted**: `TypeDatabase.StaticallyMergedModules` + the `GetLibraryPath` redirect (`TypeDatabase.cs:851-857`), populated from the SAME predicate that gates packaging (`BindingsGeneratorCommand.cs:907-912` → `Program.cs:91-96`) so emission and packaging cannot drift; unit tests in `StaticNativeMergeLibraryNamingTests.cs` (untracked, part of the wave). **Verified end-to-end 2026-07-27** by rebuilding the Debug generator and regenerating Mappedin from its real xcframework: **zero vendor-named imports remain on any reachable path** — all 4,034 primary imports name the wrapper. The 351 remaining `LibraryImport("Mappedin")` are ALL `PInvoke_getMetadata_fallback`, the *deliberate* recovery arm reached only from `catch (DllNotFoundException)` / `catch (EntryPointNotFoundException)` around the wrapper-bound primary (`MetadataWrapperEmitter.cs:107-156`); that arm intentionally keeps the declared name via `GetDeclaredLibraryPath`, because redirecting it too would make both arms carry the identical import and collapse the recovery path into a no-op (documented at `ITypeDatabase.cs:87-101`). An earlier same-day read of this regen ("the patch does NOT fix it") counted raw import strings without separating primary from fallback arms — wrong, corrected here. **Remaining work (the "future pass"):** (1) ~~commit the fix~~ done (`f4e785d5`); (2) Mappedin — and any other static-archive vendor library — must be regenerated and repacked with the ≥0.18.0 generator during the downstream pass (A12–A14) to purge the 403 from the shipped package; until then the *shipped* artifact remains exactly as C4 describes (green on Mono/NativeAOT, CoreCLR-broken). | `src/Swift.Bindings/src/TypeDatabase/TypeDatabase.cs:851-857` (redirect); `src/Swift.Bindings/src/TypeDatabase/ITypeDatabase.cs:72-124` (contract + fallback rationale); `src/Swift.Bindings/src/BindingsGeneratorCommand.cs:901-918`; `src/Swift.Bindings/src/Program.cs:91-96`; `src/Swift.Bindings/src/Emitter/StringEmitter/MetadataWrapperEmitter.cs:107-156`; tests `src/Swift.Bindings/tests/UnitTests/EmitterTests/StaticNativeMergeLibraryNamingTests.cs`. Regen evidence (scratchpad `mappedin-regen/`): exit 0, 262 files, C# verification passed; import census 4,034 wrapper-named / 351 vendor-named (all fallback) / 42 `libswiftCore`. |
| B3 | SB | **[FIXED, COMMITTED `f4e785d5` — Lane A complete 2026-07-27: over-escaped call labels 39 → 1; the survivor is a legitimate declaration-site backtick. See §F Lane A status.]** **Wrapper emitter over-escapes Swift keywords in argument labels.** Emits `` `operator`: `` / `` `class`: `` in call argument position, where escaping is unnecessary. Swift warns (`keyword 'operator' does not need to be escaped in argument list`). Cosmetic — compiles and runs — but it pollutes every wrapper build log with `[ERR]`-tagged noise that makes real errors harder to spot. | `BindingTests/output/.wrapper-build/SwiftBindingsTestLib.Wrapper.swift:60684`. Observed in the 2026-07-26 `--compile-only` log. |

---

## C. Product-surface problems — real, pre-existing, need an owner call

| ID | Repo | Item |
|---|---|---|
| C1 | PKG | **`SwiftBindings.Stripe.UICore` emitting no API is CORRECT — investigated 2026-07-26, closed.** The shipped `StripeUICore.xcframework`'s `.swiftinterface` is **15 lines containing zero declarations** — only `import` lines, no `public`, no `open`, not even `@_spi`. Stripe ships this module with its entire surface `@_spi(STP)`-gated and stripped from the public interface, so **there is literally nothing to bind**. The "0 of 96 types / 0 of 538 members" framing in `regression-0.18.0-analysis-2026-07.md` is misleading: those totals come from the **ABI JSON**, which enumerates internal + SPI declarations the `.swiftinterface` omits. The generator agrees — all 57 skips are `ModuleInternal` with disposition `ExpectedNonPublic`, `PublicSurfaceLost: 0`, `ReviewCount: 0` (the only Stripe module with a clean triage). The package is already correctly labelled `[Transitive] Support package … Not an entry point; do not install directly`, matching Stripe3DS2, and 7 sibling packages consume it as a native dependency carrier. **No action. Do not re-open without new evidence.** |
| C2 | PKG | **54 public members lost across the Stripe web** — `SkipTriage.PublicSurfaceLost`, measured on the 2026-07-27 post-fix reports: StripePaymentSheet **24**, StripePayments **19**, StripeCore **10**, Stripe **1**. Unlike C1 these are genuinely public API that did not bind. Dominant reasons across the web: `UnsupportedClosure` (12), `EveryProtocolConformanceSkipped` (13), `SuppressedProxyMemberDegraded` (8), `UnsupportedExistential` (6), `SwiftUIConstraint` (4), `DuplicateSignature` (3). **TRIAGED 2026-07-27** — full analysis in `src/docs/c2-c3-triage-2026-07.md`. Headline correction: the count is **53 rows, not 54, and rows are not members** — `SkipTriage.cs:142-147` subtracts only `ExpectedNonPublic` and `Recovered`, so by-design non-projections are tallied as "lost." Of the 53: **22 are misclassified** (the surface is actually reachable — the 8 `SuppressedProxyMemberDegraded` rows are emitted public API with consume-direction loss only, the 9 `EveryProtocolConformanceSkipped` rows still emit the `I{P}` interface, and all 5 SwiftUI rows have their bridge class generated), **23 are correct** (genuine language gaps: ~12 async-closure-value, 6 `@objc`-existential), **5 are recoverable**, **3 are genuine bugs**. Ranked recoverables and the deliberate exclusions are routed to `not-planned.md` with triggers; the top-ranked one (**registered Apple `valueTypes` get no value-type record**, `TypeDatabaseExtensions.cs:833-834`, 21 corpus rows over 10 type names) is surfaced to the owner as next work — deliberately NOT taken in this pass, since it is a surface *recovery* rather than a release blocker and a type-resolution change mid-verification would have invalidated a green gate matrix. |
| C3 | SB | **Which *shipped* packages carry Defect C's dangling P/Invokes? — OPEN QUESTION.** Defect C (RawRepresentable planner writes the Swift wrapper to a discarded `StringWriter` while the C# plane still emits `LibraryImport`s naming those symbols) produces members that throw `EntryPointNotFoundException` when touched. It has been latent since `3b3f4e97` (2026-04-01). **Correction (owner, 2026-07-26): no Facebook binding has ever been released** — verified, `libraries/Facebook/` is untracked in the packages repo and no FB nupkg exists, so the analysis doc's "every shipped FB binding since ~0.14" claim is **wrong and there is no consumer impact from FB**. The real question is whether any *other* shipped package contains a module-internal `enum X: String` and therefore shipped the same dead API. SWIFTBIND108 (the gate that catches this, landed `3c3de627` 2026-07-12) predates the 0.17.0 release, which is weak evidence that 0.17.0 is clean — but packages published **before** 2026-07-12 were never gated. **SCANNED 2026-07-27** — analysis in `src/docs/c2-c3-triage-2026-07.md`. Two corrections to this row's own premises: the SWIFTBIND108 gate commit `3c3de627` is **not an ancestor of `sdk-v0.17.0`**, so "0.17.0 was gated" was false; and the trigger shape is **broader than `enum X: String`** — any module-internal `RawRepresentable` conformance qualifies. Result of scanning the on-disk nupkgs: **no shipped package was found carrying a dangling P/Invoke**. But the scan can only clear what it can open: 11 of 14 published Stripe packages, plus Mappedin, BlinkIDUX and BlinkID 7.8.1/7.8.3/7.8.4, were cleared **at source level only** because their nupkgs are not on disk. Those are recorded in `not-planned.md` as **UNDETERMINED, explicitly not cleared**, with the trigger being: download them from nuget.org and repeat the binary diff. Capability was also separated from occurrence — a package having the trigger shape is not the same as having shipped the dead API. |
| C4 | PKG | **`SwiftBindings.Mappedin` declares 403 P/Invokes against a `Mappedin` library the package does not ship — RECLASSIFIED 2026-07-27: empirically green on BOTH runtimes; the residual is "works by unexplained mechanism", not "broken package".** Found during the C3 binary audit (not the C3 defect — same defect *class* on the vendor-module-name axis). The original row predicted `DllNotFoundException` on first touch; **two rounds of owner pushback overturned that prediction** — the finding's *facts* all stand, its predicted symptom does not. **Static facts (all verified):** the shipped 6.4.2 managed assembly's 403 P/Invokes carry ModuleRef `Mappedin` (354 metadata accessors, 29 methods, 14 statics, 3 getters, 2 thunks); the nupkg ships only `MappedinSwiftBindings.xcframework`, no `Mappedin` payload, no transitive supplier; all 403 symbols are statically merged into and **exported** from the wrapper binary (vendor slice is a static archive — no loadable `Mappedin` image can exist); `SwiftFrameworkResolver.ResolveSwiftFramework` finds nothing for `"Mappedin"` and defers to default resolution (`SwiftFrameworkResolver.cs:370`). **Runtime facts (2026-07-27):** (i) *Sim/Mono JIT* — the `libraries/Mappedin/tests/` suite exercises this exact group (`GetTypeMetadata()` on 40 types → the `…Ma` accessors; `Coordinate.Equals` → the audit's cited `==` operator) and passes; binary-verified that its DLL carries the same 403 `Mappedin`-ModuleRef P/Invokes (3987/403/42 vs shipped 3977/403/43) and that the test app is in consumer-equivalent shape — **zero** `$s8Mappedin` symbols in the executable, no vendor framework embedded, all 7,485 vendor symbols exported only from the linked wrapper framework. (ii) *Device/NativeAOT* — **Mappedin `ios-device` is PASS in every regression-validation run on record: twelve runs, 0.9.0 (2026-05-03) through 0.18.0 (2026-07-26)** (`swift-dotnet-packages/artifacts/regression-validate-*.json`), same test suite. So resolution succeeds on both runtimes — presumably the Mono loader fallback probing loaded images on sim, and link-time/direct symbol resolution on NativeAOT — **neither mechanism traced; works-by-runtime-behavior, not by design.** **Verdict (owner conversation, 2026-07-27): NOT broken today; it is a trigger-gated latent.** Runtime matrix, now measured rather than presumed: **Mono JIT resolves it** (loader fallback probes loaded images — 12 green regression runs), **NativeAOT resolves it** (build-time direct linking — same 12 runs), **CoreCLR THROWS** — proven by a minimal host experiment on this machine (a dylib exporting the symbol loaded in-process, a `[DllImport("Mappedin")]` naming a nonexistent library → `DllNotFoundException`; scratchpad `coreclr-fallback-test`). Mappedin is iOS-only, and iOS today is Mono/NativeAOT, so **no consumer-reachable runtime fails — nothing to fix or ship now**. The break becomes real exactly when iOS moves to CoreCLR, which is stated project intent for the .NET 11 timeframe. **UPDATE 2026-07-27 (owner direction): the generator-side root cause is now registered as B15 — confirmed defect, fixed and committed (`f4e785d5`), regen-verified.** A fresh Mappedin generation with the patched Debug generator emits zero reachable vendor-named imports (the only remaining `LibraryImport("Mappedin")` are the deliberate `PInvoke_getMetadata_fallback` recovery arms — see B15 for why those correctly keep the declared name). **Reopen trigger for the shipped artifact: the next Mappedin regen/repack (A12–A14 downstream pass), which retires this permanently; the CoreCLR-mobile migration is the deadline it must beat.** Lower-priority residual: every green leg consumes a ProjectReference build, never the literal published nupkg — a one-off literal-nupkg smoke would close that sliver. Route this row to `not-planned.md` with the trigger when the 0.18.0 register closes out. Full original analysis: `src/docs/c2-c3-triage-2026-07.md` §2.7. |
| C5 | PKG | **Two live packages have zero bound surface — product question for the owner: should a zero-public-surface module publish at all, or should generation fail loudly instead of emitting an empty package?** `SwiftBindings.Stripe.UICore` 26.0.0 and `SwiftBindings.Stripe.CameraCore` 26.0.0 are live on nuget.org shipping full native payloads and managed assemblies with **zero P/Invokes**. This is **not a generator bug and not C1 re-opened**: C1 closed the technical half (Stripe gates the entire API of these modules behind `@_spi(STP)`, the public `.swiftinterface` has literally zero declarations, so zero emission is correct — CameraCore shares UICore's cause exactly). What remains is purely the publish-policy call. Mitigating: both are `[Transitive]`-labelled support packages consumed as native dependency carriers by siblings, not entry points. Related but separable: B14 is why these read `PublicSurfaceLost: 0` in the skip report. (`StripeCryptoOnramp` shares the cause but is corpus-only, never published — no consumer impact.) Full analysis: `src/docs/c2-c3-triage-2026-07.md` §2.8. |

---

## D. Known-red corpora — scoped, not yet funded

| ID | Repo | Item |
|---|---|---|
| D1 | IBT | **6 compile-red corpus packages**, all root-caused with fix sites and sizes in `src/docs/corpus-compile-red-root-causes-2026-07.md`: malformed marshalling-body emission (undefined emitter locals `_handle`/`resultPtr`/`riveBox`/`s_init_*_Callback`; `.Payload` access on Apple-projected non-payload types) in DGCharts/rive-ios; CombineCocoa cross-module `Runtime` ref + incomplete `DelegateProxy` (missing `IDisposable.Dispose`); Moya blocked on sibling Alamofire missing `IParameterEncoding`; Macaw/SwiftDate operator-return CS0029. Six of seven underlying bugs are localized; fixing all is worth +7 greens (58 → 65/120). **Not funded.** |
| D2 | SB | **Validation baseline is 134+ commits stale** (`52ac336a`, 2026-06-28). A re-baseline is owed, but only *after* the reds it currently reports are triaged — see A3. Detailed disposition in `not-planned.md`. |

---

## E. Accepted / environmental — documented so they are not re-hunted

| ID | Item |
|---|---|
| E1 | **devicectl launch abort** (`CoreDeviceError 10002` + `NSPOSIXErrorDomain 22`) after a clean install is the *launcher* aborting before the app starts — no `LaunchActionDeclaration` in the unified log, app image never entered, zero product signal. Not a binding defect. `build/Models/LaunchDiagnostics.cs` encodes the discriminator; the mixed legs retry 3×. |
| E2 | **MT7155/MT7156 `BundleResource` dedup warnings** on macOS (`Swift/*Database.xml` LogicalName collision) — benign, pre-existing. |
| E3 | **LiveActivity `request()`-path tests** fail on a foreground-active precondition under the CLI simulator — environmental. |
| E4 | **StoreKit `products(for:)` returning 0** and `.storekit` config-file crashes are ASC/sandbox and Apple-side respectively; marshalling proven correct on sim + device. |

---

## F. Execution plan

**Status 2026-07-27:** steps 0–2 are **complete**; step 3 items 1–2 are complete, item 3's
precondition is met but the removal is owner-sequenced (B12), and items 4–6 are the remaining
work — **all of it held behind G4**. Item 5's triage half is done (C2/C3 rows); the section is
kept as written for the record.

Ordered. Steps 1 and 2 run **concurrently**; everything in step 3 is strictly downstream of step 1.

### Step 0 — test the collapsing hypothesis FIRST — **DONE 2026-07-26. Hypothesis FALSE.**

The hypothesis was: the verify-recover loop *declines* to recover B9/B10/B11, so one fix in the
recovery path collapses all three. **It does not decline — it never runs.** The entire
verify-recover mechanism is absent from the apple-framework/direct generation mode, which is where
B9 and B10 live.

**Evidence** — both cells reproduced end-to-end at the exact validate arg shape:

- **WeatherKit (B9):** exit 1, `SWIFTBIND113 … 150 error(s)`. The string `C# verification build:`
  appears **exactly once** in the generator log — the single-shot post-loop publication gate
  (`BindingsGeneratorCommand.cs:2166`). Zero occurrences of `verify-recover`, `SWIFTBIND114`,
  `withdraw`, `denylist`. Emitted C# uses `Swift.Foundation.Measurement` ×262; emitted csproj
  carries **no** `SwiftBindings.Apple` `PackageReference`.
- **RealityKit (B10):** same shape with `RealityFoundationDatabase.xml` threaded as validate does —
  exit 1, `SWIFTBIND113 … 65 error(s)`, all `CS0246 'RealityFoundation' could not be found`, one
  verification build, zero recovery markers.

**Why (wiring, not policy):** `verifyRecoverCompile` is built only when `resolution != null`
(`BindingsGeneratorCommand.cs:999`), and `resolution` is assigned only inside the `hasXcframework`
branch (`:565`). Direct mode leaves it null →  `verifyRecoverCompile == null` →
`CanVerifyCSharpInLoop(wrapperLoopActive: false, …)` is false (`:2850`) → `verifyRecoverCsharp == null`
→ `GenerateBindings` takes the `compileWrapper == null` arm (`Program.cs:979`), an ordinary single
render; `InEmissionDriver` is never constructed. Filed as **B13**.

**Consequences for the three defects:**

1. **B11 is structurally unreachable by recovery.** `BindingInputClosurePreflight.RunOrFail` runs
   *before* ABI parsing and returns false from `GenerateBindings` (`Program.cs:212`) — nothing is
   emitted, so there is no fragment to withdraw. Genuinely separate, as section F step 1 guessed.
2. **B9 and B10 DO share one root, but recovery is the wrong fix for it.** `BindingProjectEmitter`
   has no dependency/sibling reference emission at all; its only conditional reference is the Apple
   supplement, gated on `AppleSupplementReferences.Any`. The shared defect is that **the emitted
   csproj's reference set is not derived from the namespaces the emitted C# actually uses**. B9's
   missing arm is supplement-owned namespaces reached via the XML type databases; B10's is
   dependency-module namespaces reached via `--module-database`. → **one fix, two recording arms.**
3. **The freeze policy never blocked B9/B10.** Neither fix adds an emission-time skip predicate;
   both make the *reference set* correct so the already-emitted code compiles. The policy bars new
   predictors for compile-catchable failures — it does not bar fixing what made the code wrong.
   Recovery would also be strictly worse here: withdrawing every member touching
   `Swift.Foundation.Measurement` (262 sites) or `RealityFoundation.*` (~130 sites) guts two
   bindings that are fully bindable once the reference is emitted.

### Step 1 — Lane A: generator defects (main loop, sequential, NOT delegated)

Root-cause generator work where the freeze policy demands a judgment call per fix. Do **not** fan
this out to subagents.

1. **B9 + B10 together** — **confirmed at step 0 to be one mechanism**: the emitted csproj's
   reference set is not derived from the namespaces the emitted C# actually uses. One fix, two
   recording arms — supplement-owned namespaces (B9 → `SwiftBindings.Apple`) and dependency-module
   namespaces (B10 → the sibling `SwiftBindings.Apple.RealityFoundation`). B10's separate
   "namespace-resolution error" was disproven; do not chase it.
2. **B11** — dependency discovery is one-pass over the primary; iterate to a fixpoint. Probably
   genuinely separate from B9/B10.
3. **B3** — cosmetic keyword over-escaping; cheap, do it while in the area.

Each fix ships with tests. Gate with `nuke test` + `nuke binding-tests --compile-only`, then a sim run.

**Lane A status — COMPLETE 2026-07-27, uncommitted.** All four fixed with tests, paired-reviewed
across two rounds (Grok r2/r3 + Codex r1/r2 once its limit reset), **no High from either side in
any round**. Final gates: `nuke test` 16056/0/1 · analyzers 35/35 · runtime 730/0/1;
`nuke binding-tests --compile-only` exit 0 (3:28); sim 3254 passed / 41 skipped.

Six Mediums were raised across the review rounds and each was resolved on the merits, not by
severity triage:

- **Third un-recorded reference chokepoint** (Codex). `TypeProjectionFactory.TryProjectConcreteClassFallback`
  names a foreign module's class with no `TypeRecord`, so neither recorded chokepoint sees it —
  yet `RealityFoundation` carries *both* `concreteClassFallback: true` and a `packageId`, so a run
  without its module database shipped a binding naming `RealityFoundation.Entity` with no
  `PackageReference` (CS0246). Fixed via `ResolvedReferenceRecorder.RecordUnresolvedModuleReference`
  at the single chokepoint both callers route through. A real hole in B10 that nothing else caught.
- **Anchor-relative unresolved finality** (Codex). Sibling visibility is anchor-relative
  (`FindSiblingXCFramework` searches the scanned module's *own* directory first), so the first
  anchor to miss a name was vetoing a resolution a differently-located anchor could make.
- **Manual dependency not seeded into the closure** (Codex). An explicit `--framework-dependency`
  is now scanned, so the artifact actually in use contributes its imports instead of the shadowed one.
- **Framework name vs Swift module name** (Codex r2). A miss is reported under the framework name
  (install-name basename); a hit is recorded under the module the slice vends. Those are not
  required to agree, so a satisfied dependency could remain in the unresolved report under its other
  identity — a phantom degradation. Reconciliation now happens once, in `ReconcileUnresolved`,
  matching on both identities.
- **Unlabeled-`_` guard asymmetry** (Grok r3). The subscript call-label builder gained the
  recovered-`_` guard this round; its method-path sibling `BuildSwiftCallArgLabel` had not, so the
  same `ArgumentDecl` emitted `foo(_: x)`. Guard added; the two builders now agree.
- **Recording keys on the Swift module, not the remapped name** (Grok r3) — **not a defect, not
  changed.** A `typeRemaps` entry changes the emitted type's spelling, never which package ships it,
  and `apple-frameworks.json`'s `packageId` map is itself keyed on the Swift module. Rationale
  inlined so it is not "fixed" later.

One boundary was documented rather than closed and routed to `not-planned.md`: the closure fixpoint
does not traverse ObjC-only siblings (both scan channels are Swift-shaped, and the single-pass
analyzer it replaced had the identical blind spot). It fails safe — an undiscovered module surfaces
as a normal unsatisfied-closure diagnostic, never a wrong binding.

Earlier-round detail: one `StructVwtDestroyLeakTests.TestStructWithRefChurnNoLeak` failure on
the first attempt matched the recorded end-state-(a) flake signature exactly — survivor present,
finalizer count short by exactly 1, every skip/catch counter zero, wire-destroy entered==completed
— and the sanctioned single rerun was full-green. B3 verification: over-escaped call labels in
`SwiftBindingsTestLib.Wrapper.swift` went 39 → 1, and the survivor (`:6970`
`public var \`repeat\`:`) is a legitimate *declaration* that must keep its backticks; zero
`does not need to be escaped` warnings remain in the compile-gate log.

One review finding was deliberately **not** applied: propagating
`CdeclParamMapper.BuildSwiftCallArgLabel`'s underscore-strip recovery to the subscript call path.
The parser sets `OriginalSwiftName` only for C#-keyword collisions, so a genuine Swift label `_foo`
arrives as `("_foo", null)` and would be silently rewritten to `foo`; and because the declaration
sibling `GetSubscriptExternalLabel` does not strip, stripping only on the call side desynchronizes
`subscript(_foo x:)` from `obj[foo: x]` — a hard error in the emitted wrapper, strictly worse than
the mangled-but-matching label it would replace. Rationale is inlined at the call site so it is not
"fixed" later.

### Step 2 — Lane B: packages-repo build harness (parallel, delegable)

**B1 + B2** — the `CsprojRewriter` zero-hit deletion bug and the all-or-nothing freshness asymmetry.

This lane is safely parallel **because it is a build-harness bug that does not consume the
generator**. That is the whole reason it can run alongside Lane A.

**Anything in PKG that runs a regression is NOT parallel** — regressions consume the generator via a
local feed and go stale the moment Lane A lands a fix. Keep this lane scoped to the C# harness fix
and its tests. Good candidate for delegation (Grok CLI); Lane A is not.

### Step 3 — Lane C: verification and release (strictly after Lane A)

1. Unfiltered `nuke validate` → must be fully green → **then** it writes the baseline (see G1).
2. Remaining A-cells: `--macos` / `--catalyst` / `--tvos` (A6), `--device` after phone reboot (A5),
   `--partial-success-kitchen` (A7), `--mixed-pack` (A8), `--mixed-direct` (A9),
   `--appstore-hygiene` (A10), `--skip-surface` (A11).
3. **B12 sweep** — verify the generator now emits sibling references, remove the hand-authored ones.
4. PKG full downstream regression (A12); IBT `run-all-sim.sh` / `run-all-device.sh` (A13); corpus-sweep (A14).
5. C2 triage (54 lost public members); C3 scan of shipped packages.
6. Push `main`, then cut `release/sdk-0.18.0+apple-26.2.8`.

### Delegation guidance

- **Main loop:** step 0, Lane A, all root-cause generator reasoning.
- **Delegable (Grok CLI):** Lane B; the C3 shipped-package scan; the B12 csproj sweep.
- **Not agent work at all:** the verification matrix — those are long `nuke` invocations, daemonized.
- **Do not use an agent team.** Nothing here needs live back-and-forth, and a prior fan-out left
  worktree residue that had to be cleaned up manually.

---

## G. Decisions recorded

| ID | Decision | Date |
|---|---|---|
| G1 | **Re-baseline `validation-baseline.json` only once B9/B10/B11 are green, then cut.** The release waits on all three landing. Never re-baseline blind — B5 changed `Lines` semantics from per-file to total, so old and new values are incomparable and the gate is meaningless until a fully-green unfiltered run writes it. | 2026-07-27, owner |
| G2 | **RealityKit needs no scope decision — B10 is a plain bug.** An owner call was raised on whether to bind RealityKit or fail honestly; it was **withdrawn** as based on a wrong premise. RealityKit ships, works, and passes its packages-repo regression cells on sim and device. See the B10 row for the corrected diagnosis. | 2026-07-27 |
| G3 | **RuntimeContract floor stays at 16** for 0.18.0 per the signed decision record. Re-confirm only if this release breaks the module-init ↔ runtime dispatch contract. | prior cycle |
| G4 | **RELEASE HELD — do not cut `release/sdk-0.18.0+apple-26.2.8` yet.** The owner reviewed the completed sweep and directed: the open items in this register get worked/decided first, and a **full downstream regression pass** (A12 packages regression, A13 IBT runs, A14 corpus sweep — against the committed generator) is expected before the cut. The green SB gate matrix is necessary, not sufficient. **UPDATE 2026-07-27: the commit half is done** — the owner authorized committing the fix wave, landed as five commits (`f4e785d5`…docs) with timestamps per the owner's placement instruction. **Push still awaits an explicit ask**, as do the downstream runs and the cut. | 2026-07-27, owner |

---

## H. Housekeeping — noticed, not acted on

| ID | Repo | Item |
|---|---|---|
| H1 | SB | **98 stale local branches.** Roughly a dozen `release/*` have live remote tracking and must stay; the rest are dead streams (`backup-*`, `backup/*`, `session*worktree`, `ingestion/s0*`, `worktree-*`, several literally named `*-pre-squash`). Zero bearing on 0.18.0. Awaiting an owner call before any sweep — a prior branch-delete glob over-reached and had to be recovered, so enumerate and confirm before deleting. |
| H2 | PKG | **Agent worktrees under `.claude/worktrees/`** — at least four (`agent-a8e7be4395c90a492`, `agent-a34a938885b48dc6c`, `agent-a1883ee7e9575661c`, `agent-a9ba038284d333e5e`). Not investigated. Note the standing rule: worktrees must never live under a `~/.claude/` config dir, but a **repo-local** `.claude/worktrees/` is acceptable. Check for unmerged work before removing — in SB the equivalent sweep found all content already on main, but that was verified commit-by-commit, not assumed. |
| H3 | SB | Eight SB worktrees and six associated branches were removed 2026-07-27 after verifying every commit's content was already on `main` (three branches had been re-landed under rewritten SHAs; two `RELEASE-NOTES.md` commits are correctly release-branch-only and captured in tags). One orphaned fixture was rescued from them — see the changelog. **Done, recorded so it is not re-investigated.** |
| H4 | SB | **`BindingTests/output/.target-platform` currently reads `tvos`** (the last regen was the tvOS gate run). The next `nuke binding-tests --skip-regen` for any *other* platform will refuse to run until a full regen executes for that platform. This is the guard working as designed, not a defect — recorded so the refusal is not misread as a regression. First post-hold run on another platform: drop `--skip-regen` once. |

---

## Changelog

- **2026-07-27 (fix wave COMMITTED — owner-authorized)** — The owner authorized committing the
  wave; it landed on SB `main` as five logical commits: generator fixes (`f4e785d5`), harness
  trust fixes (`bc592d7b`), the rescued async fixture (`bb50ea0d`), baselines isolated for easy
  A20 revert (`d3929da7`), and this docs update. Commit timestamps follow the owner's placement
  instruction. `main` is now 11 commits ahead of `origin/main` — **push, downstream regression
  (A12–A14), and the cut all still await explicit asks (G4)**. The packages repo stays
  uncommitted (SDK 0.18.0 not on NuGet). Also this update: stamped B1/B2/B3/B9/B10/B11 rows
  with their fixed status so a fresh session cannot mistake them for open work, and recorded the
  owner-decision agenda discussion (A19/A20/B13/B14/C2/C5/D1 + the A6 fail-closed product call)
  as the remaining pre-sweep surface.
- **2026-07-27 (C4 fourth look — generator root cause registered as B15, fix regen-verified)** —
  The owner directed: "if we're doing something wrong which causes a wrong library name lookup, we
  should mark it as such for a future pass to fix." Registered as **B15**. Tracing settled the last
  open question: the uncommitted static-merge patch (`StaticallyMergedModules` + the
  `GetLibraryPath` redirect, keyed off the SAME predicate that gates packaging) **does** fix the
  emission — a fresh Mappedin regen with the rebuilt Debug generator produced zero reachable
  vendor-named imports (4,034 wrapper-named primaries). An initial regen read claimed the opposite
  ("still 351 vendor-named") — wrong: all 351 are `PInvoke_getMetadata_fallback`, the deliberate
  recovery arm behind `catch (DllNotFoundException)`, which keeps the declared name by documented
  design (`ITypeDatabase.cs:87-101`); counting raw import strings without separating primary from
  fallback arms repeated the same lesson as the C4 rounds — read the mechanism before reading the
  count. Remaining: the fix ships with the held wave; the shipped nupkg is purged at the next
  repack (A12–A14).
- **2026-07-27 (C4 second and third look — RECLASSIFIED, both runtimes green)** — The owner asked
  why the packages-repo Mappedin sim tests pass if the 403 vendor-named P/Invokes are unresolvable.
  Answer: they pass **while exercising that exact group** (40 metadata accessors + `Coordinate.==`),
  in a runtime shape equivalent to a nupkg consumer (no vendor framework, no vendor symbols in the
  executable) — so a runtime-level rescue path exists below our resolver. An intermediate framing
  ("device/NativeAOT is the unproven cell") was then **also corrected by the owner**: the regression
  artifacts show Mappedin `ios-device` PASS in all twelve runs from 0.9.0 through 0.18.0
  (2026-07-26). C4 is reclassified from "broken package, throw expected" to "works on both runtimes
  by untraced mechanisms; emitter naming still wrong-by-design; three low-priority residuals."
  Lesson for the register: the audit's static facts were all accurate — the failure was extrapolating
  a runtime symptom without checking the runtime evidence that already existed on disk.
- **2026-07-27 (final reconciliation — sweep complete, release held)** — All 11 SB gates green
  with zero failures (A1–A11); the owner reviewed the summary and **held the release (G4)**:
  open items first, then a full downstream regression pass (A12/A13/A14), then the cut. This file
  reconciled as the **single source of truth for a fresh session**: START HERE rewritten to the
  actual tree state (6 unpushed commits + the 73-file uncommitted fix wave, decomposed by lane);
  added **B14** (skip-report blind to suppressed-parent members — AlertToast's 15 unaccounted),
  **C4** (Mappedin's 403 P/Invokes against an unshipped library — static evidence only, one sim
  run settles it), **C5** (zero-surface publish-policy question for UICore/CameraCore), **H4**
  (`.target-platform` reads `tvos`), and **G4**. Owner's stated next step: discuss the broken
  items (starting with C4/Mappedin) before funding fixes.
- **2026-07-26 (step 0)** — **Collapsing hypothesis tested and FALSE.** The verify-recover loop does
  not decline to recover B9/B10/B11 — it never runs on the apple-framework/direct path at all
  (`resolution` is xcframework-only, so `compileWrapper` reaches `GenerateBindings` as null and
  `InEmissionDriver` is never constructed). Filed as **B13**. Consequences: B11 is structurally
  unreachable by recovery (fails at Parse, nothing emitted to withdraw); B9 and B10 *do* share one
  root — the emitted csproj's reference set is not derived from the namespaces the emitted C# uses —
  but recovery is the wrong fix for it, since it would gut two fully-bindable bindings (262 and ~130
  reference sites) that a correct reference makes compile. The prediction-gate freeze policy never
  applied: neither fix adds a skip predicate. **B10 diagnosis corrected a second time** — its
  namespace resolution is correct (`RealityFoundation.Entity` ×59, not `RealityKit.Entity` ×49) and
  the empty `RealityKit.Types.Entity.cs` shell is correct behavior; only the sibling reference is
  missing, and the error signature is `CS0246` ×65, not `CS0234` ×131.
- **2026-07-26** — File created during the full green sweep. Sections A–E seeded from the
  0.18.0 regression program, the packages-repo injector investigation, and the standing
  registers. A3/A4–A14 pending; B1–B3 open; C1–C3 await owner calls.
- **2026-07-27** — Validate run 4: **126/132, 0 compile failures, dependency gate 24/24**
  (from 102/130 with `1/8 tested, 7 failed` before the B4–B8 harness fixes). The six
  remaining regressions are all *generation* failures and all three of their root causes
  are now product defects, filed as **B9** (WeatherKit ×4 — supplement reference recorded
  by name allowlist, not by owner), **B10** (RealityKit — a dependency-module type
  (`RealityFoundation.Entity`) resolves to the wrong namespace and emits no sibling
  reference; corrected 2026-07-27, see the row) and **B11** (StripeCryptoOnramp — dependency
  auto-detection is one-pass, not fixpoint). None is a harness artifact and none is caused
  by the B4–B8 fixes: five of the six failed identically in run 1, before any change.
  All three were latent behind the pre-2026-07-19 absence of an emitted-C#-compiles gate;
  the baseline's `ok` for these cells was false confidence, not a passing state.
- **2026-07-27 (later)** — **B10 diagnosis corrected.** The prior row claimed RealityKit was flagged
  `autoBridge` against a .NET workload namespace that does not exist, citing a byte-scan showing zero
  `RealityKit` in `Microsoft.iOS.dll`. The scan was accurate but **irrelevant** — the bridge target
  was never meant to come from the workload. `SwiftBindings.Apple.RealityKit` ships, works, and passes
  its packages-repo regression cells. The real defect is that a dependency-module type
  (`RealityFoundation.Entity`) resolves to the wrong namespace and no sibling reference is emitted.
  Caught by the owner asking why a shipping package was being described as broken. Filed the
  consequence as **B12** (hand-authored sibling `PackageReference`s may be masking this across the
  catalog).
- **2026-07-27** — Added **START HERE** (fresh-session orientation + hard constraints), **section F**
  (execution plan, incl. the step-0 verify-recover hypothesis that may collapse B9/B10/B11 into one
  fix), **section G** (decisions recorded), **section H** (housekeeping). A2 re-run green; **A4 now
  DONE** — sim run 3254 passed / 0 failed / 41 skipped.
- **2026-07-27** — Worktree sweep: 8 SB worktrees removed, all content verified already on `main`.
  One genuinely orphaned fixture rescued and landed —
  `BindingTests/{Sources/SwiftBindingsTestLib,RuntimeTestsApp}/Async/AsyncGenericCollectionStructArrayReturn*`,
  covering `static func f<C>(for:) async throws -> [ResilientStruct] where C: Collection, C.Element ==
  String` with heap-allocated string fields. Compile-gated and runtime-green (4/4). Renamed from its
  original StoreKit-branded name to match the repo convention: a fixture name carries a framework name
  only when it imports that framework; mirrored shapes get a shape-name plus a provenance comment.
