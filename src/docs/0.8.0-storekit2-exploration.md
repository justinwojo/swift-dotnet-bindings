# StoreKit 2 — Phase 1 Exploration Findings

**Date**: 2026-04-07
**Scope**: First exploration session for the 0.8.0 Apple-frameworks pivot. Goal was narrow — build a StoreKit 2 wrapper Swift package, run our existing generator against it, capture observations. Not strategy, not implementation, not generator changes.

**Sibling**: `0.8.0-plan.md` (the broader release plan; this is exploration input that feeds the future `Future/apple-framework-binding-strategy.md` deliverable).

---

## TL;DR

The wrapper-package strategy in `0.8.0-plan.md` § "Working assumption" **does not work as written**. Two distinct blockers, both architectural rather than cosmetic:

1. **`@_exported import StoreKit` does not inline the imported module's API surface into the wrapper's `.swiftinterface` or `.abi.json`.** The wrapper xcframework's abi.json contains exactly one entry — the `Import` declaration itself — and zero StoreKit type declarations. Generator runs cleanly on it and produces 0 types / 0 members. Wrapper packaging by itself is empty.
2. **Even bypassing the wrapper and feeding the generator an abi.json produced directly from Apple's SDK (via `swift-api-digester`), the generator skips StoreKit entirely.** Reason: `Program.cs:65` pre-loads `StoreKitDatabase.xml` as a built-in dependency database, which registers the "StoreKit" module in the type database. When the generator parses an abi.json whose module name is also "StoreKit", `IsModuleProcessed("StoreKit")` returns `true` and emission is skipped (warning: `"Bindings generation already completed"`). No `.cs` files are written.

Neither blocker is a generator bug — they're consequences of design assumptions that pre-date the Apple-frameworks pivot. The generator was built around "Apple frameworks are dependencies provided by Microsoft.iOS, never targets." The Phase 1 pivot inverts that.

**Independent review** (Codex + Grok, same day): Both claims confirmed. No P0 contradictions. Codex ran two hands-on validations the original session did not — (a) a fresh Swift 6.2.3 build of a one-line `@_exported import StoreKit` module with and without library evolution, both producing only the bare `@_exported import` line in the swiftinterface (no inlined declarations); (b) the rename probe for Claim 2 — renamed the `swift-api-digester` dump's module from `StoreKit` to `StoreKit2`, re-ran the generator, got real `.cs` output (`StoreKit2.cs`, `StoreKit2.Wrapper.swift`). That rules out a `swift-api-digester` format mismatch as a confounder. **Codex also surfaced a real new constraint about how the fix must be shaped — see "The generator treats Apple frameworks as dependencies" below.**

---

## What was tried

### 1. Wrapper Swift package — built and consumed via `--xcframework`

- Created `/Users/wojo/Dev/swift-dotnet-packages/apple-frameworks/StoreKit2Wrapper/`
  - `Package.swift` declaring a single dynamic library, platforms iOS 15+ / macOS 12+ / Mac Catalyst 15+ / tvOS 15+
  - `Sources/StoreKit2Wrapper/StoreKit2Wrapper.swift` containing only `@_exported import StoreKit`
- Built into xcframework via `spm-to-xcframework . --min-ios 15.0 -o ./xcframeworks` — succeeded cleanly, ~220 KB output, both `ios-arm64` and `ios-arm64_x86_64-simulator` slices.
- Inspected the produced `.swiftmodule`:
  - `arm64-apple-ios.swiftinterface`: **9 lines**, just the module header + `@_exported import StoreKit` line + a few default `import` statements. Zero StoreKit type declarations.
  - `arm64-apple-ios.abi.json`: **21 lines**. `ABIRoot.children` contains exactly one element — an `Import` node for StoreKit with `declAttributes: ["Exported"]`. No types, no members, no functions.
- Ran the generator: `dotnet run --project src/Swift.Bindings/src -- --xcframework <wrapper xcframework> --platform ios --platform-target simulator -o /tmp/storekit2-bindings/`
  - Completed with **no errors and no warnings**.
  - Summary: `Types: 0 bound, 0 skipped (100.0% coverage)`. `Members: 0 bound, 0 skipped, 0 synthesized`.
  - Generated `StoreKit2Wrapper.cs` is **36 lines** — module-initializer scaffold and an empty `StoreKit2Wrapper.SwiftInterop` namespace. No StoreKit surface.
- Did not attempt to compile the generated C# (no point — there is nothing to compile).

### 2. Manual mode — fed the generator Apple's SDK files directly

Hypothesis after the wrapper failure: skip the wrapper, dump StoreKit's ABI from the SDK with `swift-api-digester`, feed that directly to the generator.

- Apple ships StoreKit's `.swiftinterface` and `.swiftdoc` in the iPhoneOS SDK at `/Applications/Xcode.app/.../iPhoneOS26.2.sdk/System/Library/Frameworks/StoreKit.framework/Modules/StoreKit.swiftmodule/arm64e-apple-ios.swiftinterface`.
- Apple does **not** ship a `.abi.json` for system frameworks. But `xcrun swift-api-digester -dump-sdk -module StoreKit -target arm64-apple-ios15.0 -sdk <iphoneos sdk> -o storekit-dump.json` produces a 1.4 MB abi.json with the same `ABIRoot.children` structure the generator expects.
- The TBD at `.../StoreKit.framework/StoreKit.tbd` contains all the Swift symbols (`_$s8StoreKit...`) with `install-name: '/System/Library/Frameworks/StoreKit.framework/StoreKit'`. That's the on-device path P/Invokes would resolve against.
- Ran the generator in manual mode:
  ```
  dotnet run --project src/Swift.Bindings/src -- \
    -a /tmp/storekit-dump.json \
    -d <SDK path>/StoreKit.framework/StoreKit.tbd \
    -t <SDK path>/StoreKit.framework/StoreKit.tbd \
    -s <SDK path>/StoreKit.framework/Modules/StoreKit.swiftmodule/arm64e-apple-ios.swiftinterface \
    -l '\@rpath/StoreKit.framework/StoreKit' \
    -o /tmp/storekit2-direct
  ```
- Generator parsed the swiftinterface fully. Loaded:
  - **78** public type names
  - **452** public member names
  - **100** parameter name entries
  - **359** availability annotation entries
  - **13** enum case label entries
  - **9** `@MainActor` actor-isolated member keys (1 `@MainActor` type)
  - **5** marker protocol conformance entries
  - **5** default parameter value entries
  - **36** internal member keys
- Then logged `warn: Bindings generation already completed for /tmp/storekit-dump.json.` and exited with **no output files at all**. `/tmp/storekit2-direct/` is empty.

The "already completed" log comes from `Program.cs:496`. The cause is `TypeDatabase.IsModuleProcessed("StoreKit")` returning `true` because `Program.cs:65` pre-loaded `StoreKitDatabase.xml` (a 17-line stub providing four StoreKit 1 ObjC enums for downstream library bindings). The pre-load registers the "StoreKit" module name in the database; the parse-and-emit branch is gated on the module name being absent.

---

## What this means

### The wrapper package is the wrong shape

The `0.8.0-plan.md` working assumption — *"the xcframework's `.swiftinterface` describes the Apple framework's Swift API surface"* — is wrong. `@_exported import` is a re-export *for downstream consumers of the wrapper module*, not an inlining of declarations into the wrapper's own interface file. The wrapper's `.swiftinterface` lists the import; consumers of the wrapper see StoreKit symbols by transitive resolution at compile time. **Nothing about that helps a tool like our generator that reads the wrapper's interface in isolation.**

This is current Swift compiler / module-interface emission behavior, not a hard language guarantee — but it's been stable across the toolchain and is consistent with how [SE-0409](https://github.com/swiftlang/swift-evolution/blob/main/proposals/0409-access-level-on-imports.md) frames re-export as visibility semantics rather than declaration cloning. Codex independently rebuilt the same one-line wrapper on Swift 6.2.3 with and without `-enable-library-evolution`; both produced the same bare `@_exported import StoreKit` line in the swiftinterface with no inlined declarations. Codex also checked `swiftc -help-hidden` and `PackageDescription.SwiftSetting` and found no flag, package setting, or experimental feature that changes this behavior. The wrapper-as-metadata-source approach is therefore not salvageable on the current toolchain.

There may still be a role for a wrapper Swift package — e.g., to emit hand-written `@_cdecl` thunks bridging StoreKit calls into a C ABI we can P/Invoke against, the way our generator already does for third-party libraries. But that role is *additive Swift code we author*, not "hosting StoreKit's surface for reflection." The naming `StoreKit2Wrapper` is also misleading if its only job becomes thunk hosting; something like `StoreKit2Bridge` or `StoreKit2Thunks` would scan more honestly. **Decision deferred** — Phase 1 strategy doc territory.

### The generator treats Apple frameworks as dependencies, not targets

The Apple framework registry (`apple-frameworks.json` + the `*Database.xml` files in `Swift.Runtime/src/Swift/`) was built for the world where:
- Generator output (third-party Swift libs) **references** Apple framework types, and
- Microsoft.iOS already provides the C# binding for those types as ObjC bindings, and
- The generator just needs a stub database to do type resolution + name remapping when emitting references to those types.

That model has zero room for "this Apple framework IS the generation target." The pre-load conflict isn't a switch we can flip; the generator's whole orchestration assumes Apple framework module names are read-only dependency entries.

To make StoreKit 2 (or any Apple Swift-first framework) generatable, the generator needs at minimum:
- A way to declare "the abi.json I'm parsing replaces (rather than collides with) the pre-loaded database for module X" — possibly a `--apple-framework-target StoreKit` flag, or detection that the input module name matches a pre-loaded database and unloading it.
- A way to suppress the type-stub side of the registry for the target module so the generator doesn't accidentally treat real declarations as "use the existing ObjC binding instead."
- Possibly a way to declare the runtime library path so the emitted DllImport library name is `/System/Library/Frameworks/StoreKit.framework/StoreKit` rather than a wrapper dylib name. (The `-l` flag accepts arbitrary library names, so this may already be sufficient — needs validation once the pre-load conflict is past.)

These are real generator changes. They are not large changes — the pre-load logic is a single block in `Program.cs:65–79` and the gate is a single `IsModuleProcessed` call — but they need to be designed deliberately because they crack open an assumption that's been load-bearing since the project started.

#### Constraint surfaced by review (added 2026-04-07): the fix must keep the module name as `StoreKit`

Codex ran the rename probe — modified the `swift-api-digester` dump's module name from `StoreKit` to `StoreKit2` and re-ran the generator. It bypassed the `IsModuleProcessed` gate cleanly and emitted real `.cs` and `.swift` files. That validates the diagnosis: the pre-loaded `StoreKitDatabase.xml` is the sole reason manual mode produced no output, and renaming around it works at the parse-and-emit layer.

**But renaming is NOT a viable fix.** Codex flagged a downstream constraint: metadata/conformance resolution keys in `DemanglingResults.cs:107` and `DemanglingResults.cs:133` are joined on module-qualified names. The TBD's exported symbols are mangled with `_$s8StoreKit...` (the leading `8StoreKit` is the module-name length-prefix in Swift mangling). If we generate with module name `StoreKit2`, the bindings will look for `_$s9StoreKit2...` symbols and find nothing — the resulting C# would compile but the P/Invokes would fail to bind at runtime. The Swift module name is load-bearing across the abi.json, the swiftinterface, the TBD, and the demangling-key joins; it cannot be changed in isolation.

So the actual fix must:
- **Keep the module name as `StoreKit`** in the abi.json fed to the generator (no rename workaround).
- **Drop or replace the pre-loaded `StoreKitDatabase.xml` entry** before the parse-and-emit branch is gated, so `_modules` no longer contains "StoreKit" when the generator checks.
- **Preserve the dependency-database semantics for OTHER libraries that reference the legacy SK* enums** until they migrate to the real generated bindings (or until those libraries are validation-only, in which case the migration is a non-issue).

#### Architectural framing: `IsModuleLoaded` and `IsModuleProcessed` should be different predicates

Today, both `TypeDatabase.IsModuleLoaded` (line 71) and `TypeDatabase.IsModuleProcessed` (line 298) are literally the same expression: `_modules.ContainsKey(moduleName)`. They are aliased because the project never needed to distinguish "we have a stub for this module from a dependency database" from "we have actually parsed and emitted bindings for this module." The Apple-frameworks pivot makes that distinction load-bearing for the first time.

The minimal narrow-spike fix can stay as "drop the matching entry from `_modules` before parsing the target abi.json" — implementable in a few lines of `Program.cs`. The correct long-term refactor (per Codex's read) is to split the predicates: a module can be `Loaded` (we have a dependency database) without being `Processed` (we have generated real bindings for it as a target). The gate at `Program.cs:344` should be checking `Processed`, not `Loaded`, and they should be tracked separately in `TypeDatabase`.

Both can land in the same change, but the spike should not block on the predicate split — get the Apple-framework target mode working first, then tighten the type-database semantics in a follow-up.

### The system framework runtime-loading question is still unanswered

Per `0.8.0-plan.md` strategy question #3 (`SystemFrameworkResolver`), we don't yet know whether the existing `SwiftFrameworkResolver` correctly `dlopen`s system frameworks via the `/System/Library/Frameworks/<X>.framework/<X>` path. **We did not get far enough in this session to test this.** The blocker chain is: fix generator → produce real bindings → install on a device or simulator → see if P/Invokes resolve. Each link is a separate piece of work.

### Plan section that needs revision

`0.8.0-plan.md` § "Working assumption" steps 1–3 should be rewritten. The current text reads:

> 1. Wrapper Swift package does `@_exported import <SystemFramework>` and ships an xcframework via `spm-to-xcframework`
> 2. The xcframework's `.swiftinterface` describes the Apple framework's Swift API surface
> 3. Our existing bindings generator consumes that xcframework like any other

Step 2 is false. Step 3 cannot work for any Apple framework currently in `apple-frameworks.json` (which is most of them). Suggested replacement direction:

1. ABI source comes from `swift-api-digester -dump-sdk -module <X> -sdk <iphoneos sdk>`, not from a wrapper xcframework.
2. The generator gains an "Apple framework target mode" that removes the target module from the pre-loaded dependency set before parsing.
3. P/Invokes are emitted with the system framework's on-device path as the library name; runtime loading uses the existing resolver if it already handles that path, or a small extension if not (TBD).
4. A wrapper Swift package may still be useful for hand-written thunks (e.g., bridging async/await or AsyncSequence into something P/Invokeable), but it is not the source of binding metadata.

That's a strategy refinement, not a strategy doc — the strategy doc still has to answer all 12 questions in `0.8.0-plan.md` § "Strategy questions". This exploration just rules out one branch and points at a different one.

---

## What the StoreKit 2 surface looks like through the generator

We never got to a real binding pass, but the swiftinterface load gives us a count-only view:

- **78 public types** in StoreKit (Swift surface). For comparison, Nuke is ~30 types; Lottie is ~50. StoreKit 2 is meaningfully larger than the third-party libs we currently ship, but well within the order of magnitude the generator handles.
- **452 public member names** — this is the "method/property names that need underscore-suppression" set, not the total method count. Order-of-magnitude indicator only.
- **359 availability annotations** is striking. StoreKit 2 has dense `@available(iOS 15, *)`, `@available(iOS 16, *)`, etc. annotations across its surface. This makes strategy question #6 (iOS-version availability) **very real** — the surface has version gates everywhere, and how we surface them in C# (TFM, `[SupportedOSPlatform]`, runtime guards) will affect every consumer.
- **9 `@MainActor` member entries** under **1 `@MainActor` type** — one of `AppStore`, `Storefront`, etc. is main-actor-isolated. We already handle `@MainActor` types in the generator, so this is observation, not blocker.
- **5 marker protocol conformances** — `Sendable` and friends. Already handled.
- **0 typed throws entries** — interesting, given StoreKit 2's `Result<Transaction, VerificationError>` is famous. Either typed throws aren't used in StoreKit 2 (it uses `Result` types instead), or `swift-api-digester` doesn't surface them in the same way `swiftinterface` ABI parsing does.

Notable surprises:
- **Apple's StoreKit `.swiftinterface` is shipped at `arm64e-apple-ios.swiftinterface`, not `arm64-apple-ios.swiftinterface`.** Our generator's `--xcframework` mode auto-resolves the swiftinterface based on the slice path. For Apple SDK frameworks, the only available slice is `arm64e` on device (no simulator slice exists in the system framework — the simulator uses the simulator SDK's separate framework copy). This will matter when wiring xcframework-mode auto-resolution against system framework paths.
- **The TBD's exported symbol list is the most complete inventory of the Swift surface.** It contains every mangled Swift symbol with `_$s8StoreKit` prefix. Cross-referencing this against the abi.json after generator parsing would let us measure real coverage rather than relying on the swift-api-digester dump alone.

---

## Side observations worth keeping

- **`spm-to-xcframework` works against local paths cleanly.** It accepts a local directory as a positional arg and produces a working xcframework. Good news for any future wrapper-package work.
- **The generator's CLI `-o /tmp/foo/` (with trailing slash) is rejected as "Valid output directory is required" if the directory does not pre-exist.** Without trailing slash and with `mkdir -p` first, it works. Minor papercut, not a bug worth filing — reproduces only on first-run cleanup paths.
- **The generator's `--xcframework` mode silently succeeds on an empty wrapper xcframework**, producing the resolver scaffold and a `.csproj` for an empty module. There is no warning that the abi.json contained zero declarations. Adding a warning ("Module X has 0 type declarations — likely indicates an incorrect ABI source") would have caught this exploration's first blocker in seconds. Cheap quality-of-life improvement; flag for future grooming.
- **`apple-frameworks.json:583` already lists StoreKit with `objcPrefixes: ["SK"]` and four enum value-type entries.** That's the StoreKit 1 surface. When the registry is reworked for Apple-framework targets, the StoreKit entry will need to either grow to cover the StoreKit 2 surface OR be split into `StoreKit` (legacy SK*) and `StoreKit2` (Product, Transaction, etc.) entries. This is open design space.

---

## Open questions this exploration created

These are NEW questions, distinct from the 12 in `0.8.0-plan.md`:

A. **Do we replace the wrapper-package strategy with an `swift-api-digester`-driven approach, or do we keep the wrapper as a host for hand-written thunks?** Both are compatible — the wrapper just stops being the binding-source. Decision affects how `apple-frameworks/StoreKit2Wrapper/` evolves.

B. **What's the right shape for the generator's "Apple framework target mode"?** Options:
   - A new CLI flag (`--apple-framework-target StoreKit`) that drops the matching pre-loaded database before parsing.
   - Auto-detection: if the parsed abi.json's module name matches a pre-loaded database, replace it.
   - A separate code path entirely (`apple-framework-mode` in `Program.cs` that bypasses the registry pre-load + uses different emission defaults).

   I lean toward auto-detection with an opt-out — least surprising for callers, smallest CLI surface — but this is design space.

C. **Is `StoreKitDatabase.xml`'s existing four-enum stub still correct after StoreKit 2 generation?** When we ship a real StoreKit 2 binding, the pre-loaded database is no longer a stub — it's the binding itself. Other libraries that today reference `SKError.Code` or `SKPaymentTransactionState` (validation libs etc.) would presumably switch to referencing the generated bindings. That's a migration question, not a blocker.

D. **Does our generator's existing async/AsyncSequence handling cover StoreKit 2 cleanly?** We never got far enough to find out. Once the pre-load gate is past, this becomes the next thing to discover.

E. **The `arm64e` swiftinterface slice issue** (above) — does our `--xcframework` mode know how to find it when the xcframework points at a system framework? Probably needs a smaller patch than the database pre-load issue.

F. **Will Apple's `iPhoneOS26.2` SDK be the right thing to dump from for a StoreKit 2 binding targeting iOS 15+ deployment?** The dump captures the API surface as of the SDK version, which includes everything added in iOS 16/17/18/26. Availability annotations distinguish them, so a binding compiled against the SDK is forward-compatible with older deployment targets — but this needs verification, and it's a model we haven't used before.

---

## Artifacts left behind

- **Wrapper Swift package**: `/Users/wojo/Dev/swift-dotnet-packages/apple-frameworks/StoreKit2Wrapper/` (uncommitted; `swift-dotnet-packages` is checked in but per memory we don't commit it until the referenced SDK ships).
  - `Package.swift`, `Sources/StoreKit2Wrapper/StoreKit2Wrapper.swift`
  - `xcframeworks/StoreKit2Wrapper.xcframework/` — built artifact, ~220 KB
  - **Recommended fate**: keep in place as a reproducible negative-result artifact for future sessions. If the Apple-framework strategy moves to a different repo location, delete this and rebuild fresh under the new convention.
- **`/tmp/storekit-dump.json`** — 1.4 MB swift-api-digester ABI dump for StoreKit. Useful for the next exploration session; ephemeral.
- **`/tmp/storekit2-bindings/`** — generator output from the wrapper-mode run. 0-binding scaffold; ephemeral.
- **`/tmp/storekit2-direct/`** — empty directory from the manual-mode run. Ephemeral.
- **Build logs**: `/tmp/storekit2-spm-build.txt`, `/tmp/storekit2-gen-wrapper.txt`, `/tmp/storekit2-gen-direct.txt`. Ephemeral.

No changes to the swift-bindings repo itself. No commits.

---

## Recommended next session

Run in this order. Each step is its own session — do not merge them. Steps 2 and 3 only become useful after step 1 produces real output.

### Session 1: Generator narrow spike — Apple-framework target mode  ✅ **COMPLETE — commit `108f4f12` (2026-04-08)**

**Goal**: Make `manual mode pointed at Apple's SDK + a `swift-api-digester` dump` produce real, non-empty `.cs` output for StoreKit. Stop the moment that happens.

**Constraints baked in from the review:**
- Module name in the abi.json must stay as `StoreKit` — do NOT use the rename workaround (it breaks the demangling-key joins in `DemanglingResults.cs:107,133`).
- The fix is to drop or replace the pre-loaded `StoreKitDatabase.xml` entry in `TypeDatabase._modules` before the parse-and-emit gate runs.
- Mechanism: either a CLI flag (`--apple-framework-target StoreKit` or similar) or auto-detection (if the parsed abi.json's module name matches a pre-loaded database, unload it). Lean toward auto-detection with an opt-out flag — least surprising for callers, smallest CLI surface.

**Explicit success criteria** (the spike is done when ALL of these hold):
- `dotnet run --project src/Swift.Bindings/src -- -a /tmp/storekit-dump.json -d <SDK>/StoreKit.tbd -t <SDK>/StoreKit.tbd -s <SDK>/.../arm64e-apple-ios.swiftinterface -l '\@rpath/StoreKit.framework/StoreKit' -o /tmp/storekit2-direct` produces a non-empty output directory.
- The output contains a `StoreKit.cs` (or similarly-named) file with real type bindings — at least the major StoreKit 2 types (`Product`, `Transaction`, `AppStore`, `Storefront`, etc.) appear as C# class/struct declarations.
- The output `.csproj` references the Swift wrapper compilation correctly.
- The unit test suite (`nuke test`) still passes — the spike must not regress existing third-party library bindings.
- `nuke validate` baseline is unchanged for non-StoreKit libraries (StoreKit may or may not appear in the validation pool — the spike doesn't need to wire it up).

**Explicit non-goals for this spike**:
- Do NOT attempt to compile the generated C# bindings into a working assembly. Generation success is enough; compilation is the next session.
- Do NOT attempt to wire runtime loading or test on a simulator/device.
- Do NOT refactor `IsModuleLoaded` vs `IsModuleProcessed` into separate predicates yet — note it as a follow-up but the narrow spike doesn't need it.
- Do NOT touch the `apple-frameworks.json` `StoreKit` entry yet.
- Do NOT update `0.8.0-plan.md` yet.
- Do NOT remove or rewrite `StoreKitDatabase.xml`.

If the spike succeeds, write the strategy doc immediately (Session 3) while the evidence is fresh. If the spike hits a new blocker, capture it as an exploration update to THIS doc and stop — do not improvise around the new blocker in the same session.

#### Session 1 outcome (2026-04-08, commit `ae44d0b8`)

**Status**: Complete. The spike's *spirit* met (StoreKit's `swift-api-digester` ABI dump now produces real, non-empty `.cs` output) with **scope expansion beyond the original "narrow spike" framing**, authorized mid-session by the user ("do whatever is necessary").

**What landed** (4 src files, +224/-37):
- **Apple-framework target mode** (`Program.cs`, `CliOptions.cs`, `BindingsGeneratorCommand.cs`): auto-skip a built-in `*Database.xml` whose `moduleName` matches the input abi.json's module name, via `PeekModuleNameFromXml` before parse-and-emit. Opt-out flag `--keep-builtin-database` for collisions. This is the original narrow spike — it's the smallest possible diff that lets the generator target a Swift framework whose name is also a pre-loaded dependency-database stub.
- **Parameter-position opaque type lowering** (`Parser/SwiftABIParser.cs`): swift-api-digester emits `some P` parameters as `TypeNominal/GenericTypeParam` with `printedName="some Foo"` and no children. The existing `OpaqueTypeArchetype` branch only caught return-position opaques. New `_opaqueParamCapture` list installed around `CreateMethodDecl`'s param loop; matching nodes are routed through `SynthesizeOpaqueParameter`, which produces a `τ_opaque_N` synthetic `TypeName` + `GenericArgumentDecl` with the constraint protocol attached. The capture is merged into `MethodDecl.GenericParameters` after the loop, mirroring Swift's own `some P` → `<T: P>` desugaring. The `τ_` prefix routes the emitted name through `NameProvider`'s positional `T{i}` branch, so the synthetic param's emitted C# name is `T<position>` and pre-existing generics keep their sugared names. **This is a real Swift language feature, not a defensive skip.**
- **`\@` CLI escape strip** (`BindingsGeneratorCommand.StripCliAtEscape`): the documented `\@rpath/...` escape (which bypasses System.CommandLine's response-file handling) was flowing through into the C# string literal emitted in `DllImport`, producing `CS1009: Unrecognized escape sequence`. Strip applied to `--library` and `--async-library` after CLI parsing.
- **Direct-mode `AsyncLibraryName` default**: when direct/manual mode is used without `--async-library`, default `AsyncLibraryName` to `"{Module}SwiftBindings"`, matching the convention used by xcframework mode (e.g. `NukeSwiftBindings`). The binding now correctly expresses the intent to call into a wrapper dylib in direct mode.

**StoreKit evidence**:
- 68/68 types bound, 406 members bound, 30 legitimate skips (`UnsupportedSignature`, `ModuleInternal`, `AnyTypeFallback`, etc.).
- Concrete example: `updateAll(_: some Collection<PromotionInfo>)` → `public static Task UpdateAllAsync<T0>(T0 promotions, ...) where T0 : ISwiftObject`.
- `nuke test` 9987/9987 passing (Unit 9441 + Analyzer 20 + Runtime 526).
- `nuke validate` 95/95 passing, "No regressions detected."

**Scope deviations from the original spec** (all authorized mid-session):
- The spec said "stop the moment non-empty output is produced" and "if the spike hits a new blocker, capture it and stop — do not improvise." After the auto-detect mechanism worked but uncovered a downstream parser blocker (parameter-position opaques), the user explicitly removed effort/time as constraints. The session pushed through to fix the opaque-parameter case properly (parser lowering, not a defensive skip), surfaced and dispositioned two further warnings (SWIFTBIND093, SWIFTBIND094), and added two further small fixes (`\@` strip, `AsyncLibraryName` default). This goes beyond the original narrow framing but preserves the spike's spirit.
- `IsModuleLoaded` vs `IsModuleProcessed` predicate split: deferred per the spec (still a follow-up).
- `apple-frameworks.json` `StoreKit` entry: untouched per the spec.
- `0.8.0-plan.md`: untouched per the spec.
- `StoreKitDatabase.xml`: retained per the spec.
- Co-Authored-By trailer reads `Claude Opus 4.6` rather than `Claude Opus 4.6 (1M context)` — minor, not amended.

**Deferred for Session 2** (handoff brief at `/tmp/swiftbind093-session2-handoff.md`):
- **SWIFTBIND093 (CC-003)**: SBW_ wrapper-helper P/Invokes (`SBW_GetErrorDescription`, `SBW_ReleaseError`, `SBW_Free`, `SBW_CancelTask` + one property accessor) target the system framework instead of a wrapper dylib in direct mode, because direct mode emits `StoreKit.Wrapper.swift` (84 KB) but doesn't compile it. Root cause precisely identified at `WrapperEmitter.Marshalling.cs:648`. The `AsyncLibraryName` default landed in this commit silences the validator (binding now correctly *expresses intent* to call into `StoreKitSwiftBindings`), but the dylib doesn't exist at runtime — Session 2 needs to pick a wrapper-deployment strategy and produce the dylib. Three candidate fixes (compile wrapper dylib in direct mode / move helpers into `Swift.Runtime` / hand-written thunks) characterized in the handoff brief with trade-offs.
- **31 throwing/async StoreKit methods** will throw `EntryPointNotFoundException` at runtime until Session 2 lands the wrapper dylib. List in the handoff brief tells Session 2 what NOT to call first.

**Deferred to roadmap** (handoff brief at `/tmp/swiftbind094-followup.md`):
- **SWIFTBIND094 (CC-004)**: `CallConvCdecl` + mangled `$s` symbol on metadata accessors and synthesized equality operators. Pre-existing on Nuke main (4 instances on unmodified `main`); StoreKit shows 6 of the same shape after this commit. **Not introduced by this session.** BindingTests runtime exercises this pattern without crashing — the validator is over-conservative on this rule. Warrants its own follow-up session as a generator cleanup, separate from the Apple-frameworks pivot.

### Session 2: Compile generated bindings + runtime smoke test  ✅ **PARTIAL — commit `eafe252d` (2026-04-08)**

Only after Session 1 produces real `.cs` output. Goal: get the generated `StoreKit.Swift.iOS.csproj` to compile, install on a simulator, and confirm at least one P/Invoke resolves against the system framework (e.g., reading `AppStore.deviceVerificationID` or instantiating a static type).

This is where we discover whether `SwiftFrameworkResolver` correctly handles `/System/Library/Frameworks/` paths or whether we need a `SystemFrameworkResolver` extension. It's also where async/AsyncSequence handling gets its first real test against StoreKit 2's `Transaction.updates`.

This session can grow to 2–3 sessions if compilation surfaces a chain of generator bugs (it usually does for new surface area). Budget loosely.

#### Session 2 outcome (2026-04-08, commit `eafe252d`)

**Status**: Partial. The Swift wrapper side of the StoreKit binding now compiles cleanly (486 KB dylib, was 406 errors → 0). The C# side does not — surfaced four unrelated emit bugs that block compiling the generated `StoreKit.cs` into a `.dll`. Per design-doc escalation criteria (>5 unrelated generator bugs in one framework's surface area = reassess), the user directed to **go all-in on StoreKit 2**: land what's done, document the four bugs, and address them in dedicated follow-up sessions before returning to the runtime smoke test.

**What landed** (15 files, +161/-27):
- **`MergeAvailabilityFromAncestors` helper** in `WrapperEmitterHelpers.cs`: walks parent decls and merges per-platform `@available` constraints into a single annotation. Wired through five emitter sites (`MetadataWrapperEmitter`, `WrapperEmitter.Async.cs`'s async wrapper templates, `PropertyHandler`'s async-property accessor path, `ClassHandler.cs`'s `==` operator wrapper, `EnumCaseWrapperEmitter.cs`).
- **Per-case enum `@available` propagation** (parser-side): `SwiftABIParser` now calls `ApplyMemberAvailability` on enum cases; `SwiftInterfaceContextTracker` recognizes `case` lines and yields the bare case name (matching the ABI JSON `Var` node `printedName`). Concrete trigger: `StoreKit.ExternalPurchase.NoticeResult.continuedWithExternalPurchaseToken` (iOS 17.4).
- **`Unmanaged<value-type>` fix in `ForeignTypeExtensionEmitter`** (earlier in the session): bogus `Unmanaged<StoreKit.Struct>` references in foreign extension wrappers replaced with the legitimate `Unmanaged<AnyObject>` / `Unmanaged.passRetained(... as AnyObject)` form.
- **`AvailabilityAttributeEmitterTests.cs`**: new unit test. `MergeAvailability_NoParentDecl_ReturnsMemberOnly` switched from `Assert.Same` to value equality (refactored helper now returns a fresh list).
- **`CLAUDE.md` cleanup**: removed a stale pointer to the deleted `src/docs/Completed/` directory.

**StoreKit evidence**:
- `StoreKit.Wrapper.swift` compiles to `StoreKitSwiftBindings.dylib` (486 KB) cleanly via `xcrun --sdk iphonesimulator swiftc`.
- `StoreKit.cs` compile attempt: 215 errors / 118 unique CS codes, ALL traceable to four unrelated bug families.
- `nuke test` 9441/0/1, `nuke validate` 95/95, `nuke binding-tests` pass, `nuke runtime-tests-simulator --skip-regen` 1312/0/23. Zero regressions.

**The four C# emit bugs** (all subsequently fixed; see commit history for details):

1. **Nested type qualification stripped** (52× CS0234). Generator emits `StoreKit.PurchaseIntents` instead of `StoreKit.PurchaseIntent.PurchaseIntents`. Affects every modern Apple framework that uses nested AsyncSequences. Worker noted that the `intents_Get` accessor routes through `ObjCRuntime.Runtime.GetNSObject<>` for a Swift type — type misclassification, not just name mangling.
2. **Constrained-extension member duplication** (34× CS0102) on `VerificationResult<TSignedType>`. Three specialized `where SignedType == AppTransaction | Transaction | Product.SubscriptionInfo.RenewalInfo` extensions all merge into one C# class and re-emit `SignatureData`/`SignedData`/etc. per-extension. Needs extension dedup keyed on member identity. **Highest-leverage** of the four — generalizes to any constrained-extension-heavy library.
3. **Property/nested-type collision**: `Transaction` emits both an `OfferType` property and a nested class `OfferType`. Likely needs the same suffix-disambiguation pass already used for instance/static name collisions.
4. **Foundation projection gaps**: `DateComponentsFormatStyle`, `DecimalFormatStyleCurrency` not in `AppleFrameworkRegistry` / Foundation XML; both referenced by StoreKit's formatter APIs.

**Scope deviations from the original spec**:
- Original spec said "compile + install + smoke test." Compilation surfaced a chain of generator bugs; the session fixed availability propagation but stopped at the four C# emit bugs per the >5-bug escalation gate.
- `/ai-pair-programming` review was NOT run before commit. The worker hit autocompaction three times (200k window) and the review step was missed under context pressure. Acceptable post-hoc because all gates were green and the commit is a focused availability propagation diff with new test coverage; should not be skipped on the bug-fix follow-up sessions.
- Wiring wrapper compilation INTO direct mode: NOT done. The wrapper now compiles manually via `swiftc`, which is the prerequisite. Wiring the build step into the direct-mode CLI pipeline is deferred until after the C# binding compiles (no point automating wrapper compilation when the consumer .csproj can't yet build).
- `SwiftFrameworkResolver` system-framework path verification: NOT done. Same reason — blocked by C# binding compilation.
- Simulator smoke test: NOT done. Blocked by C# binding compilation.

**Deferred for follow-up sessions** (all subsequently completed):
- Bug #2 (constrained-extension dedup) — work first, highest leverage
- Bug #1 (nested type qualification) — work second
- Bug #3 (property/nested-type collision) — narrower
- Bug #4 (Foundation registry gaps) — narrowest
- After all four land: re-run the full Session 2 chain (compile `StoreKit.Swift.iOS.csproj`, wire wrapper compilation into direct mode, verify resolver, simulator smoke test, then `Transaction.updates` AsyncSequence test).

### Session 3: Strategy doc — DEFERRED until StoreKit 2 ships

Originally scoped to write `Future/apple-framework-binding-strategy.md` after Session 2 produced compile + smoke-test evidence. Per `0.8.0-plan.md` Phase 1 step 8, the strategy doc is intended to capture "the strategy answers above so framework #2 onward is mechanical" — which is most useful **after** the first framework actually ships, not before.

Combined with the user's 2026-04-08 directive to go all-in on StoreKit 2 (rather than pivoting to a smaller framework after the bug discovery), Session 3 is **deferred until StoreKit 2 is genuinely shipping**: bindings compile, wrapper compilation wired into direct mode, simulator smoke test green, async/AsyncSequence handling validated against `Transaction.updates`, NuGet package built. Only then does the strategy doc have enough evidence to capture a reusable pattern.

The `Future/apple-framework-binding-strategy.md` deliverable lives in `0.8.0-plan.md` Phase 1 step 8, not as its own session in this exploration doc.

### Why this order

The dominant uncertainty in Phase 1 is **feasibility**, not design elegance. Writing the strategy doc before the generator spike risks producing a document full of assumptions that the spike will falsify in minutes — exactly the way this exploration session falsified the wrapper assumption in 30 seconds. The spike is a small, scoped, high-leverage piece of work that unblocks every Apple framework simultaneously. Front-loading it means the strategy doc gets written against real evidence rather than speculation, and the time spent on the strategy doc is time spent capturing what we know rather than guessing what might work.

Sessions 1–2 confirmed that the basic chain works (parse → emit Swift wrapper → compile wrapper) and surfaced the next layer of generator bugs that affect every Apple framework, not just StoreKit. The follow-up bug fixes are also high-leverage: each one generalizes beyond StoreKit. The strategy doc benefits from waiting until those land too.

The dead-end branch (forcing `@_exported import` to inline declarations via some Swift toolchain trick) was previously listed as a session candidate. After review, both Codex and Grok independently confirmed there is no such trick on the current toolchain. **Removed from the next-session list.** Don't spend time here unless a future Swift compiler release surfaces something new.

---

## Tracking: Sessions 4–7 (path to StoreKit 2 shipping)

This section is the live tracking doc for the remaining work between **Session 2 + the direct-mode wiring (commit `ab3f25a8`, 2026-04-08)** and **a published StoreKit 2 NuGet package consumed by a real iOS sample app**. Each session is a single Claude-sized chunk with explicit done criteria so progress is verifiable per-session, not just at the end.

### State after commit `ab3f25a8` (2026-04-08)

Direct-mode wiring is in place: `--xcframework`-less generator runs (manual `-a/-d/-t` with `-l @rpath/...`) now compile the Swift wrapper against the requested simulator/device slice, emit a buildable `.csproj`, and emit consumer `.targets` files. Behavior is gated on `-l @rpath/...` or `-l /System/Library/...` so non-system manual workflows are unchanged. `nuke test` is green; `BindingProjectDirectModeTests` covers the emitter-level changes.

**End-to-end status against the live StoreKit 2 SDK** (iPhone OS 26.2 / Simulator):

| Step | Status | Notes |
| --- | --- | --- |
| Generate `StoreKit.cs` from system framework abi.json | ✅ | Direct-mode CLI runs to completion |
| Generate `StoreKit.Wrapper.swift` | ✅ | 84+ KB, 369 wrapper strategies emitted |
| Compile `StoreKit.Wrapper.swift` → `StoreKitSwiftBindings.xcframework` | ✅ | Fixed Session 4 sub-task A — 525592-byte real dylib |
| Emit `StoreKit.Swift.iOS.csproj` + `.targets` | ✅ | Direct-mode emitter wired in `ab3f25a8` |
| Compile `StoreKit.Swift.iOS.csproj` → `StoreKit.Swift.iOS.dll` | ✅ | Fixed Session 4 sub-task B — 0 errors with `-p:SwiftBindingsRepoRoot=...` |
| Resolver + dyld @rpath verification on simulator | ✅ | Session 5 — double-prefix bug fixed in `SwiftFrameworkResolver`, 15 new unit tests |
| Trivial accessor smoke test on simulator | ✅ | Session 5 — `StoreKit.AppStore.CanMakePayments` = True, 48ms, zero fallback warnings |
| `Transaction.updates` AsyncSequence test | ⏸️ | Session 6 (sub-task A unblocks the iterator wrapper) |
| Published NuGet package + sample app | ✅ | Session 7 — `StoreKit.Swift.iOS.0.8.0-preview.1.nupkg` packs cleanly; fresh out-of-repo consumer prints `[STOREKIT-SMOKE] PASS` on Simulator |

### Session 4 — Unblock the direct-mode build (two independent sub-tasks) — **DONE 2026-04-08**

**Status**: complete. **Outcome**: Both sub-tasks landed in a single session along with two small follow-on fixes that surfaced once the end-to-end build was actually being exercised. The StoreKit direct-mode pipeline now produces a real wrapper dylib AND a building `.dll` against in-tree `Swift.Runtime`. All four gates green; Session 5 is unblocked.

**What landed (summary — see commit and Resolution sections below for detail)**:
- Sub-task A: `WrapperEmitter.Async.cs` now binds `__self` to a typed mutable pointer for `mutating async` receivers and routes the call through `__self.pointee` (uses `unsafeMutableAddress`, no exclusive borrow across await).
- Sub-task B: `BindingProjectEmitter` emits a `<ProjectReference>` to the in-tree `Swift.Runtime.csproj` for the `0.0.0-dev` sentinel, gated on `$(SwiftBindingsRepoRoot)`. The ProjectReference form (NOT a bare `<Reference>`+`<HintPath>`) is load-bearing — it pulls in `Swift.Runtime`'s native-dylib `<Content Include>` items so `libSwiftBindingsRuntime.dylib` actually lands in the consumer's output (and in `Frameworks/` on iOS/tvOS/maccatalyst). A first attempt used a raw HintPath and was caught in code review (Codex P2 #1): the build was clean but the consumer would have shipped without the concurrency runtime. Falls back to an exact-version `[0.0.0-dev]` `PackageReference` (NU1102 — clean failure mode) when the property is unset. Additionally, dev-sentinel projects emit `<IsPackable>false</IsPackable>` (Codex P2 #2): a `dotnet pack` against the in-tree `ProjectReference` would otherwise produce an unusable nupkg with a phantom `SwiftBindings.Runtime 0.0.0-dev` dependency that doesn't exist on any feed. Refusing to pack is the loud failure mode — to publish, the caller passes `--swift-runtime-version <published>` so the `PackageReference` path is taken and `SwiftBindings.Runtime`'s `buildTransitive` targets carry the dylib copy logic into the published consumer flow. The third Codex review pass (P2 #3) caught that the IsPackable=false rule was being applied universally because neither `BindingProjectEmitter.Emit()` call site (xcframework mode at `BindingsGeneratorCommand.cs:763`, direct mode at `:843`) actually plumbed `SwiftRuntimeVersion` through — every emitted non-SDK project defaulted to `0.0.0-dev` and silently became unpackable, broadly disabling `dotnet pack` instead of only blocking the local-dev case. Fixed by adding `--swift-runtime-version` as a first-class CLI option in `CliOptions.cs` and threading it through both call sites; default still resolves to the dev sentinel, but real packaging flows now pass `--swift-runtime-version 0.8.0` and get a normal packable PackageReference. Verified end-to-end against StoreKit direct mode: with the flag the emitted csproj has `<IsPackable>true</IsPackable>` + `Version="0.8.0"`, without it the dev-sentinel ProjectReference branch fires.
- Follow-on (uncovered while running the final gate): `SwiftWrapperPostProcessor` was leaving dangling `@available` / `// wrapper preamble` comments after stripping broken wrappers — fixed via a narrow `RemoveTrailingWrapperPreamble` walk-back wired into Patterns 2, 2b, and 4. The post-processor now has 4 new tests (`SwiftWrapperPostProcessorTests` region M).
- Follow-on: the direct-mode csproj template was missing `<EnableDefaultCompileItems>false</EnableDefaultCompileItems>`, so a consumer running `dotnet build` on the emitted csproj tripped NETSDK1022 (duplicate `Compile` items because the SDK wildcard pulled in the explicitly-listed `.cs` files a second time). Fixed in `BindingProjectEmitter.cs` with the property scoped inside the csproj (NOT on the command line — that would propagate to `Swift.Runtime`, which DOES rely on default Compile items). New `Emit_DisablesDefaultCompileItems` test asserts the property is present.
- Coverage: new BindingTests fixture `AsyncMutatingCounter` + new runtime test `TestAsyncMutatingCounterAdvancesAcrossCalls` lock in the mutating-async behavior. CLI integration test for the system-framework gate moved into `BindingsGeneratorCommandTests.cs` covering `@rpath/...`, `/System/Library/...`, plain dylibs, and the `hasXcframework` win.

**Final gate (2026-04-08, this session)**:
- `nuke test`: 526 pass, 1 skip
- `nuke validate`: 95/95 pass (81/81 cs_compile, 61/61 swift_compile, no regressions)
- `nuke binding-tests`: full pipeline green
- `nuke runtime-tests-simulator`: 1318 pass, 0 fail, 23 skip
- StoreKit direct-mode end-to-end: `StoreKitSwiftBindings.xcframework` ships a 525592-byte arm64 simulator dylib; `StoreKit.Swift.iOS.csproj` builds with 0 errors using `-p:SwiftBindingsRepoRoot=/Users/wojo/Dev/swift-bindings`; without the property the build fails with a clean NU1102 (the intended fallback signal).

**Reproducer command** (kept here for Session 5+ smoke-test re-runs):
```
SDK="$(xcrun --sdk iphonesimulator --show-sdk-path)"
SWIFTINTERFACE="$SDK/System/Library/Frameworks/StoreKit.framework/Modules/StoreKit.swiftmodule/arm64-apple-ios-simulator.swiftinterface"
DYLIB="$SDK/System/Library/Frameworks/StoreKit.framework/StoreKit.tbd"

dotnet run --project src/Swift.Bindings/src -- \
  -a /tmp/storekit2-direct-fresh/StoreKit.abi.json \
  -d "$DYLIB" -t "$DYLIB" -s "$SWIFTINTERFACE" \
  -l '\@rpath/StoreKit.framework/StoreKit' \
  --platform ios --platform-target simulator \
  -o /tmp/storekit2-session4

cd /tmp/storekit2-session4 && \
  dotnet build StoreKit.Swift.iOS.csproj \
    -p:SwiftBindingsRepoRoot=/Users/wojo/Dev/swift-bindings
```
Note: `-s/--swiftinterface` is REQUIRED for `@available` annotations on the wrapper functions — they're sourced from the swiftinterface, not the ABI JSON. Without it, the wrapper compiles fail against `InvalidRequestError` and similar iOS 18.4-only types.

The historical pre-Session-4 plan and risk notes are preserved below for context.

#### Sub-task A — Async iterator wrapper: handle mutating receivers

**Goal**: Make the wrapper emitter's async path detect mutating receivers and bind `__self` such that `AsyncIteratorProtocol.next()` and any other `mutating async` method compiles cleanly. This is a generator emitter bug, not a StoreKit-specific patch — every Apple framework that surfaces an async iterator hits it, and `Transaction.updates` is the headline StoreKit case.

**Concrete reproducer** (current commit, fresh direct-mode run):
```
/tmp/storekit2-direct-fresh/StoreKit.Wrapper.swift:3247:
    @_cdecl("SBW_StoreKit_AsyncIterator_next_675F1A37_async")
    public func PInvoke_next_B6747691(...) {
        let __self = _self.assumingMemoryBound(to: ...AsyncIterator.self).pointee
        ...
        let resultnext = await __self.next()   // ← error: cannot use mutating member on immutable value
    }
```

**Done criteria**:
1. `xcrun swiftc` compiles `StoreKit.Wrapper.swift` cleanly (no `cannot use mutating member` errors).
2. New BindingTests Swift fixture exercising at least one `mutating async` method (e.g., a custom `AsyncIteratorProtocol` conformance).
3. New runtime test in `BindingTests/RuntimeTestsApp/` that successfully iterates the async iterator once and reads at least one value.
4. `nuke binding-tests` + `nuke runtime-tests-simulator` green.

**Suggested first moves**:
- Find the async wrapper emission site (`WrapperEmitter.Async.cs` or similar) and grep for the `let __self` template literal.
- Decide between: (a) flip to `var __self` when the receiver method is mutating, or (b) bind through `_self.assumingMemoryBound(...).pointee` directly inside the call expression. Option (a) is smaller; option (b) avoids a stack-local copy.
- Check whether the wrapper emitter already tracks `IsMutating` on method decls — if not, plumb it from the parser.

**Risks / unknowns**:
- Mutating async on a value type may also need a `withExtendedLifetime` to prevent the rebound pointee from being released mid-await. Check SIL output of a hand-written reference implementation before committing to a fix shape.
- May surface a related class of bugs around mutating sync methods that haven't been exercised by BindingTests yet.

#### Sub-task B — Resolve `SwiftBindings.Runtime` 0.0.0-dev for direct-mode consumers

**Goal**: Make `dotnet build StoreKit.Swift.iOS.csproj` resolve `SwiftBindings.Runtime` against current source rather than the stale 0.7.0 nupkg cached in `~/.nuget/packages/`. Right now the direct-mode csproj template hard-codes `<PackageReference Include="SwiftBindings.Runtime" Version="0.0.0-dev" />`, but no `0.0.0-dev` package exists locally — silent fallback produces 190 errors against types that exist in current `Swift.Runtime` source (`IExistentialBoxable` from `8c74b688`, `ISwiftStruct` from `7552abba`).

**Done criteria**:
1. `dotnet build /tmp/storekit2-direct-fresh/StoreKit.Swift.iOS.csproj` returns 0 errors against current `Swift.Runtime` source (after sub-task A fixes the wrapper compilation).
2. Approach is documented inline in the direct-mode csproj template so future Apple-framework consumers don't hit the same trap.
3. `nuke validate` (xcframework mode) is unaffected.

**Two candidate approaches** (pick one in the session):

- **(a) Auto-pack `0.0.0-dev` convention**: Add a `nuke` target — or extend `nuke pack` — that publishes `SwiftBindings.Runtime.0.0.0-dev.nupkg` to a known local feed whenever `Swift.Runtime` source changes. Direct-mode consumers add that feed once. Pro: keeps the csproj template uniform across local and published flows. Con: more moving parts; users must remember to repack after Runtime source changes.
- **(b) `HintPath` fallback in the emitter**: Detect `SwiftRuntimeVersion == "0.0.0-dev"` in `BindingProjectEmitter` and emit a `<Reference Include="Swift.Runtime"><HintPath>{repo-root}/src/Swift.Runtime/src/bin/{Configuration}/{TFM}/Swift.Runtime.dll</HintPath></Reference>` instead of the `PackageReference`. Pro: no extra workflow step; direct-mode "just works" against current source. Con: needs an absolute repo-root path or a `$(SwiftBindingsRepoRoot)` MSBuild property the user sets; fragile if the build hasn't run yet.

**Recommendation**: start with (b) gated on a `SwiftBindingsRepoRoot` env var or MSBuild property, fall back to PackageReference when the property isn't set. Cleanest local-dev experience without breaking the published-NuGet path.

**Risks / unknowns**:
- The `Test.csproj` pattern from session start (manual `HintPath`) worked, so the underlying mechanism is proven. The unknown is which path the project owner prefers as the default convention.
- If approach (a), the cache invalidation story matters: is the local feed in `/tmp/swift-nuget/`, in a stable `~/.local/...` path, or somewhere else?

#### Session 4 final gate

After both sub-tasks: `nuke test` + `nuke validate` + `nuke binding-tests` + `nuke runtime-tests-simulator` all green. Re-run the full StoreKit direct-mode pipeline end-to-end and confirm both `StoreKitSwiftBindings.xcframework` (real dylib, not empty) and `StoreKit.Swift.iOS.dll` build with 0 errors.

### Session 5 — `SwiftFrameworkResolver` `/System/Library/Frameworks/` verification + smoke test — **DONE 2026-04-09**

**Status**: complete. **Outcome**: `SwiftFrameworkResolver` had a latent double-prefix bug against dyld-style library names emitted by the Apple-framework direct-mode generator. Fixed; paired with a real Simulator smoke test against `StoreKit.AppStore.canMakePayments`. The whole chain (`DllImport` → resolver → wrapper dylib → system framework via dyld transitive load → `@_cdecl` thunk → real StoreKit API) is now proven end-to-end on iOS Simulator.

**What the resolver bug actually was**: Apple-framework target bindings emit `[LibraryImport("@rpath/StoreKit.framework/StoreKit")]` for direct metadata accessors (the 78 StoreKit entries that hit the system framework directly, as opposed to the 408 routed through the wrapper). `SwiftFrameworkResolver.ResolveSwiftFramework` unconditionally prepended `@rpath/{name}.framework/{name}` on every incoming library name, so it was trying to load `@rpath/@rpath/StoreKit.framework/StoreKit.framework/@rpath/StoreKit.framework/StoreKit` — pure nonsense that always failed, then fell through to the other prefix variants (also nonsense). dyld's default resolver was picking up the slack by sheer coincidence (the wrapper dylib links `/System/Library/Frameworks/StoreKit.framework/StoreKit` into its own load commands at build time, so StoreKit is already resident in the process by the time any P/Invoke hits), but any future framework without that happy path would have exploded.

**Fix** (`src/Swift.Runtime/src/Swift/Runtime/SwiftFrameworkResolver.cs`): Added `IsDyldStylePath` predicate that recognizes `@rpath/`, `@executable_path/`, `@loader_path/`, and absolute filesystem paths as "already dyld-resolvable — pass verbatim to `NativeLibrary.TryLoad` and return `IntPtr.Zero` on failure so the default .NET resolver still runs." Deliberately strict: a malformed input like `@foo/bar` or `@rpathtypo` is NOT matched and falls through to the normal prefix-based search, so typos fail loudly rather than silently bypassing the standard resolver. `DiagnoseResolution()` was mirrored for symmetry.

**Smoke test** (`BindingTests/RuntimeTestsApp/SmokeTests/StoreKitSmokeTests.cs`, gated on `STOREKIT_SMOKE`): `TestAppStoreCanMakePayments` reads `StoreKitSwift::StoreKit.AppStore.CanMakePayments` — routed through `[LibraryImport("StoreKitSwiftBindings", EntryPoint = "thunk_StoreKit_af177bce")]` → wrapper dylib → real `StoreKit.AppStore.canMakePayments` → primitive `bool` return. Result: `CanMakePayments = True`, 48ms, zero `EntryPointNotFoundException` / `DllNotFoundException` / resolver fallback warnings. The assertion is intentionally "the call completed" (the Simulator with no StoreKit configuration legitimately reports either value).

**Why not `AppStore.deviceVerificationID`** (the Session 2 candidate): it returns `SwiftOptional<System.Guid>`, whose cctor chains through `TypeMetadata.GetTypeMetadataOrThrow<System.Guid>()`. `System.Guid ↔ Foundation.UUID` is mapped only at generator time (in `FoundationDatabase.xml`); there's no corresponding runtime `RegisterMetadata` call. Selecting that accessor as the smoke target would have conflated "does the resolver work" with "does runtime type metadata cover all ObjC-bridged value types" — two orthogonal questions. A `bool`-returning accessor isolates the resolver concern. The `Guid ↔ UUID` runtime gap is a separate follow-up.

> **Found and deferred — foreign value-type metadata gap.** Filed as a Post-1.0 entry in `src/docs/roadmap.md` ("Foreign value-type metadata registration gap"). Same root cause applies to `System.DateTime ↔ Foundation.Date` and `System.Decimal ↔ Foundation.Decimal`. Generator-level fix, generalizes across all Apple frameworks. **Session 6 is the next place this will bite** — see the blocker note at the top of the Session 6 section.

**Smoke test wiring is zero-regression**: `RuntimeTestsApp.csproj` gates the entire `<Reference>` + `<NativeReference>` block behind `$(StoreKitSmokeEnabled)`, which is only `true` on `iossimulator-arm64` when both the `StoreKit.Swift.iOS.dll` and the `StoreKitSwiftBindings.xcframework/ios-arm64-simulator` slice exist at `$(StoreKitSmokeRoot)` (default `/tmp/storekit2-session4`, overridable via env/CLI). The test class itself is wrapped in `#if STOREKIT_SMOKE`. Fresh checkouts, CI runs, and device builds silently skip the test — no artifacts needed, no new baseline file to keep in sync. Regenerate artifacts via the Session 4 reproducer command above when re-running this on a fresh machine.

**Extern alias rationale**: Microsoft.iOS's ObjC bindings expose `StoreKit.AppStore` too, so a default-aliased `<Reference>` collides (CS0433). We use `<Aliases>StoreKitSwift</Aliases>` and `extern alias StoreKitSwift` in the smoke test. An earlier attempt at `<Aliases>global,StoreKitSwift</Aliases>` (suggested by the pair-programming review as a "don't surprise future consumers" hedge) was rejected: it caused a Mono-side startup SIGABRT inside `xamarin_bridge_initialize → mini_init → load_aot_module` before `Main()` ran. The presumption is the Microsoft.iOS ObjC type registrar walks all globally-visible types at init and blows up on the `StoreKit.AppStore` collision. Extern-alias-only hiding is exactly the escape hatch — there's a warning comment in the csproj pinning this down so the next reviewer doesn't re-try it.

**Pair-programming review (OpenAI gpt-5.4) findings and disposition**:
1. ✅ *Tighten `IsDyldStylePath` from "any `@`-prefix"* — applied. Explicit whitelist + unit coverage (`IsDyldStylePath_MatchesOnlyDocumentedTokens`).
2. ❌ *Use the 4-arg `NativeLibrary.TryLoad(name, assembly, DllImportSearchPath?, out handle)` overload for parity with standard .NET resolution* — rejected. That overload applies `DllImportSearchPath.AssemblyDirectory` / `.UseDllDirectoryForDependencies` semantics, which look in the managed assembly's directory. For a dyld-style path we want dyld's own `@rpath`/`@executable_path` resolution, not .NET's assembly-directory search. The 2-arg overload is the correct choice for this branch.
3. ✅ *Test the resolver path itself, not just `DiagnoseResolution()`* — applied. `ResolveSwiftFramework` and `GetSearchPaths` tightened from `private` to `internal` (existing `InternalsVisibleTo("Swift.Runtime.Tests")` in `SwiftExitGuard.cs` covers it). New tests: `ResolveSwiftFramework_DyldStylePath_DoesNotPrefix` (4 inputs) and `ResolveSwiftFramework_BareName_DoesNotThrowAndFailsClean`.
4. ✅ *Parameterize `/tmp/storekit2-session4` via an MSBuild property* — applied. `$(StoreKitSmokeRoot)` with the current hardcode as default.
5. ❌ *Also include `global` in `<Aliases>`* — rejected for the reason above (Mono startup crash).

**Stale-AOT footgun caught during this session** (pinning it down so the next session doesn't re-hit it): the incremental simulator build re-AOT-compiles `Swift.Runtime.aotdata.arm64` when its source `.dll` changes, but does NOT re-AOT `StoreKit.Swift.iOS.aotdata.arm64` (the external `.dll` is unchanged). If the referenced symbol layout inside `Swift.Runtime.dll` shifts between runs, `StoreKit.Swift.iOS.aotdata.arm64` holds stale metadata-token references, and Mono aborts inside `mono_runtime_init_checked → create_exception_two_strings → load_aot_module` BEFORE `Main()` runs — the managed stacktrace is empty (`<unknown> <0xffffffff>`) because no user code has loaded yet. Fix: `rm -rf BindingTests/RuntimeTestsApp/{bin,obj}` and full rebuild whenever you rebuild `Swift.Runtime.dll` between StoreKit smoke test runs. This is a property of "AOT data for an external pinned `.dll` that references a locally-rebuilt `.dll`", not something unique to StoreKit.

**Files touched**:
- `src/Swift.Runtime/src/Swift/Runtime/SwiftFrameworkResolver.cs` (+47 / −3)
- `src/Swift.Runtime/tests/MetadataTests/SwiftFrameworkResolverTests.cs` (+68 / 0) — 15 new tests
- `BindingTests/RuntimeTestsApp/RuntimeTestsApp.csproj` (+42 / 0) — conditional StoreKit wiring
- `BindingTests/RuntimeTestsApp/SmokeTests/StoreKitSmokeTests.cs` (new, 78 lines)

**Done criteria** (all met):
1. `SwiftFrameworkResolver` read end-to-end + extended: the double-prefix bug is documented in the XML doc on `IsDyldStylePath`.
2. `RuntimeTestsApp` extension deploys to iOS Simulator, calls `StoreKit.AppStore.CanMakePayments` through the direct-mode pipeline, prints the result, passes.
3. Zero `EntryPointNotFoundException`, zero `DllNotFoundException`, zero resolver fallback warnings.

---

#### Historical planning notes (kept for context)

**Goal**: Verify that the runtime correctly resolves a `@rpath/StoreKit.framework/StoreKit` library name against the on-device system framework via dyld, then prove end-to-end with a single trivial accessor call from a real iOS app.

**Done criteria**:
1. `SwiftFrameworkResolver` is read end-to-end and either confirmed-correct for `/System/Library/Frameworks/` paths or extended to handle them. Decision documented in this section.
2. A minimal MAUI / .NET iOS sample app (or `RuntimeTestsApp` extension) that consumes the `StoreKit.Swift.iOS.dll` from Session 4, deploys to the iOS Simulator, and successfully calls **one** non-throwing, non-async, non-heap-returning StoreKit accessor (e.g., `AppStore.deviceVerificationID`, or instantiating a static type), prints the result, and exits cleanly.
3. No `EntryPointNotFoundException`, no `DllNotFoundException`, no resolver fallback warnings.

**Suggested first moves**:
- `Grep` for `SwiftFrameworkResolver` and read the resolution path. Look for any explicit handling of `@rpath/`, `/System/Library/`, or framework-name extraction.
- If the resolver needs no changes, verify by reading the dyld behavior expected for `@rpath/StoreKit.framework/StoreKit` from a `.dylib` linked into a simulator app (the simulator runtime resolves system frameworks under `/Library/Developer/CoreSimulator/...`, not `/System/Library/Frameworks/`).
- Pick the simplest possible StoreKit accessor for the smoke test. The Session 2 outcome notes already brainstormed candidates — `AppStore.deviceVerificationID` was the leading one.

**Risks / unknowns**:
- Simulator vs device path divergence: simulator system frameworks live under the `CoreSimulator` runtime path, not `/System/Library/Frameworks/`. dyld handles this transparently for normal apps, but a P/Invoke loading a wrapper dylib that links the system framework is a slightly different code path.
- The 31 throwing/async StoreKit methods listed in the Session 1 handoff brief will still throw `EntryPointNotFoundException` until Session 6 verifies async wiring — pick a synchronous accessor for the smoke test.

### Session 6 — `Transaction.updates` AsyncSequence test — **DONE 2026-04-09**

**Status**: complete. **Outcome**: validated the generator's async/AsyncSequence pipeline end-to-end on iOS Simulator. Could not use `Transaction.updates` directly (NEW generator bug — see below); pivoted to `Transaction.unfinished`, a drop-in proxy with identical return type, iterator shape, element type, and SBW entry point. Three iteration passes (early-terminate, full empty-complete, memory-tracked empty-complete) all green; managed-memory delta on the empty-complete pass is 656 bytes against a 256 KB ceiling — three orders of magnitude of headroom. Two new follow-ups filed for Session 7+: an orphan-PInvoke generator bug, and confirmation that the foreign value-type metadata gap surfaces as predicted.

**The Path A pivot — orphan `[LibraryImport]` generator bug** (filed in `roadmap.md`): the generator emits the `[LibraryImport]` declaration for `SBW_Get_StoreKit_Transaction_updates` (and the symbol IS exported from `StoreKitSwiftBindings.dylib` — confirmed via `nm -g`), but DROPS both the private wrapper getter method AND the public `Updates` C# property. There is literally no `StoreKit.Transaction.Updates` symbol to call. The same orphan pattern hits `Storefront.updates` and `Product.SubscriptionInfo.Status.updates` — all three are static properties returning a custom-named AsyncSequence (`Transactions`, `Storefront.Updates`, `Product.SubscriptionInfo.Status.Statuses`). Diagnosis is in progress in a background subagent; root cause is in `PropertyHandler.cs` somewhere between the preflight skip path and the P/Invoke emitter, but the precise gate has not yet been pinned down. Filing as Session 7+ follow-up rather than gating Session 6 — `Transaction.unfinished` exercises the same generator code paths and is the right call until the bug is fixed at the source.

**Why `Transaction.unfinished` is a faithful proxy** (defended in the test's XML doc): same return type (`Transaction.Transactions`), same `MakeAsyncIterator()`, same `NextAsync(CancellationToken) → Task<VerificationResult<Transaction>?>`, same `VerificationResult<Transaction>` element type, same `SBW_StoreKit_AsyncIterator_next_675F1A37_async` entry point in the generated wrapper. Anything that fails for `Transaction.unfinished` would also fail for `Transaction.updates`; anything that succeeds proves the wrapper code path. The only difference is the source of the iteration values (sandbox-account-driven vs unfinished-purchase queue), and an empty stream is the expected result on a fresh Simulator either way.

**Foreign value-type metadata gap — confirmed reachable, deliberately dodged**: `VerificationResult<Transaction>` exposes `UUID` and `Date` fields whose runtime `RegisterMetadata` calls are missing — same gap Session 5 hit and pivoted around. The smoke test sidesteps it by NEVER dereferencing any property on yielded `VerificationResult<Transaction>` instances; passes 1, 2, and 3 only count iterations and check for the terminal-nil signal. The static initializers for `Transactions`, `AsyncIterator`, and `VerificationResult<TSignedType>` do NOT touch `SwiftOptional<Guid>` / `SwiftOptional<DateTime>` cctors, so loading and iterating the sequence is safe — only field access on the yielded result would crash. Roadmap entry updated to reflect this is now a confirmed reachable hazard, not just a hypothetical one.

**Test design** (`BindingTests/RuntimeTestsApp/SmokeTests/StoreKitSmokeTests.cs::TestTransactionUnfinishedAsyncSequenceEnumerates`):
- **Pass 1 — early-terminate**: one `NextAsync` call, then dispose. Exercises the partial-iterator dispose path. If `SafeHandle` release / Swift ARC release double-frees on early termination, this pass crashes inside `Dispose()`.
- **Pass 2 — full empty-complete**: while loop until terminal nil, asserts `reachedTerminalNil` so hitting the iteration ceiling is treated as a failure (the terminal-completion path is half the point of the test). Single `CancellationTokenSource` reused across all iterations within the pass, not per-call.
- **Pass 3 — managed-memory delta**: re-runs the full empty-complete loop inside `ForceGC()` + `GC.GetTotalMemory(forceFullCollection: true)` bracket. Asserts `memoryDelta < 256 KB` (observed: 656 bytes). The 1000x headroom catches per-iteration `SafeHandle`/`GCHandle` leaks while tolerating Mono GC heuristic noise.
- 16-iteration safety ceiling on every pass to bound runaway streams.
- Native ARC ref-count inspection isn't surfaced through `SwiftSafeHandle` in this repo, so the success bar matches Session 5: "no managed exception, no Mono abort, managed memory delta is bounded."

**Sync-over-async deadlock caught mid-session**: the original draft used the existing `TestBase.TrackMemory(Action)` helper to bracket pass 3, but `TrackMemory` is sync-only — wrapping the async enumeration in a lambda forced `.GetAwaiter().GetResult()` on the iOS main thread, sync-over-async-deadlocked the test runner, and SIGABRT'd the process. Replaced with inline `ForceGC()` + `GC.GetTotalMemory(forceFullCollection: true)` measurement around the `await`. Pinning this down so the next reviewer doesn't re-try `TrackMemory` for an async test: the helper needs an async-friendly sibling (`TrackMemoryAsync(Func<Task>)`) before any other test can use it across an `await`. Filed mentally as a TestBase improvement, not urgent enough for a roadmap entry.

**Pair-programming review (OpenAI gpt-5.4) findings and disposition**:
1. ✅ *`memoryDelta` was computed but never validated* — applied. Added `MemoryDeltaCeilingBytes = 256 * 1024` const and `AssertTrue(memoryDelta < ceiling, ...)`. Without this the leak signal was log-only.
2. ✅ *`pass3Count` discarded by helper invocation* — applied. Captured into a local and logged. Still doesn't assert on the count (empty-complete is a valid result).
3. ✅ *`CancellationTokenSource` constructed per `NextAsync` call polluted the pass-3 memory measurement* — applied. Single `CTS` per pass via a `passTimeout` variable; gives a coherent deadline for the entire pass instead of resetting on each iteration.
4. ✅ *Hitting the 16-iteration ceiling was logged but not asserted, so a runaway stream would silently "succeed"* — applied. Pass 2 now sets `reachedTerminalNil = true` only on the nil branch and asserts it after the loop. Hitting the ceiling is now a hard failure.
5. ❌ *`EnumerateUnfinishedToCompletionAsync` should also assert ceiling-vs-terminal* — rejected as overengineering. The helper is called from exactly one place (pass 3) under a constant ceiling that pass 2 already validated against. Duplicating the assertion adds maintenance cost without changing behavior.

**Smoke test results** (iOS Simulator, fresh sandbox state):
- `TestAppStoreCanMakePayments`: PASS (43ms) — unchanged from Session 5.
- `TestTransactionUnfinishedAsyncSequenceEnumerates`: PASS (254ms). Pass 1 first `NextAsync` returned null (empty stream); pass 2 enumerated 0 entries before terminal completion; pass 3 enumerated 0 entries; managed memory before=2,842,264, after=2,842,920, delta=**656 bytes** (vs 256 KB ceiling).
- Targeted gate: 2 pass / 0 fail / 0 skip / 0 crash, done=True.
- Full simulator baseline: 1320 pass / 0 fail / 23 skip / 0 crash, done=True (+1 vs prior 1319 = the new test, zero regressions).

**Files touched**:
- `BindingTests/RuntimeTestsApp/SmokeTests/StoreKitSmokeTests.cs` (+~165 lines: new test method + helper, no changes to `TestAppStoreCanMakePayments`)
- `src/docs/0.8.0-storekit2-exploration.md` (this Session 6 outcome)
- `src/docs/roadmap.md` (foreign value-type entry tightened from "likely surfaces" to "confirmed reachable"; new orphan-PInvoke entry filed for Session 7+)

**Done criteria** (all met):
1. ✅ The simulator sample app from Session 5 is extended with an async-iterator loop over a `Transaction.Transactions` AsyncSequence. (`Transaction.unfinished` instead of `Transaction.updates` per the orphan-PInvoke pivot — same code path, defended above and in the test XML doc.)
2. ✅ The loop empty-completes cleanly (zero crashes, zero `EntryPointNotFoundException`, zero `SwiftRuntimeException`, zero Mono aborts) on a fresh Simulator with no sandbox account. Reading an actual transaction is gravy and not required for the success bar.
3. ✅ ARC behavior verified to the extent the test framework can: no managed exception across early-termination + full empty-complete + memory-tracked passes; managed memory delta bounded at 656 bytes. Native ARC ref-count inspection is a follow-up for whenever `SwiftSafeHandle` exposes it.
4. ✅ Two new generator follow-ups filed in `roadmap.md` (orphan-PInvoke for `Transaction.updates`/`Storefront.updates`/`Product.SubscriptionInfo.Status.updates`; foreign value-type metadata gap upgraded from "likely" to "confirmed reachable").

---

#### Historical planning notes (kept for context)

> **⚠️ Likely blocker discovered in Session 5 — foreign value-type metadata gap.** `Transaction` and `VerificationResult<Transaction>` almost certainly expose `UUID` / `Date` (and possibly `Decimal`) fields through `Optional`, which will trip `SwiftOptional<System.Guid>` / `SwiftOptional<System.DateTime>` cctor chaining through `TypeMetadata.GetTypeMetadataOrThrow<T>()` with no matching runtime `RegisterMetadata` call. Before burning a debug cycle: (a) read `Transaction`'s abi.json to enumerate which foreign value types it exposes, then (b) decide between two paths — either **pivot accessors** to avoid foreign value-type Optionals (same move as Session 5's `deviceVerificationID → canMakePayments` pivot), or **fix the metadata gap** upfront by adding the runtime `RegisterMetadata` calls for the generator-level `FoundationDatabase.xml` mappings. Fixing the gap is the more general solution and unblocks every future Apple framework that exposes UUIDs/Dates, but is a wider scope than a single async-sequence test. Pick deliberately. See the roadmap entry ("Foreign value-type metadata registration gap") for the full description.

**Goal**: Validate that the generator's async/AsyncSequence handling works against StoreKit 2's headline API, end-to-end on a real iOS Simulator, by consuming `Transaction.updates` from the C# side at least once.

**Done criteria**:
1. The simulator sample app from Session 5 is extended with an `await foreach` (or equivalent) loop over `Transaction.updates`.
2. The loop either reads at least one transaction from a sandbox account or empty-completes cleanly (no crash, no `EntryPointNotFoundException`, no marshalling failure).
3. ARC behavior is verified — no leaks across the await boundary, no double-frees on early termination.
4. If any new generator bug surfaces here, file it as a follow-up and document it in this section before marking the session done.

**Suggested first moves**:
- Confirm Session 4 sub-task A fixed the AsyncIterator wrapper (re-grep `__self` in `StoreKit.Wrapper.swift` for any remaining `let` bindings on async mutating paths).
- Read the `Transaction.updates` Swift signature in the abi.json to confirm what the generator emits — `AsyncStream<VerificationResult<Transaction>>`? a custom `Transaction.Transactions` AsyncSequence?
- Set up a sandbox StoreKit account in the Simulator's StoreKit configuration file before running, otherwise the `updates` stream stays empty and the test is unverifiable.

**Risks / unknowns**:
- **Foreign value-type metadata gap** (filed in `roadmap.md` as "Foreign value-type metadata registration gap"): `System.Guid`/`UUID`, and likely `DateTime`/`Date`, `Decimal`/`Decimal`, lack runtime `RegisterMetadata` calls despite being mapped in `FoundationDatabase.xml` at generator time. `Transaction` and `VerificationResult<Transaction>` very likely surface UUIDs and Dates — Session 6 will hit this the moment it instantiates anything that allocates `SwiftOptional<Guid>` or similar, throwing `SwiftRuntimeException: Unable to get type metadata for type T` out of the `SwiftOptional<T>` cctor. If hit, either pivot accessors to avoid foreign value-type Optionals OR fix the gap first by wiring runtime `RegisterMetadata` calls for each generator-level mapping. Do not silently work around it. Reproducer: `StoreKit.AppStore.deviceVerificationID` (already verified to throw — hence Session 5's pivot to `canMakePayments`).
- This is the first real exercise of the C#-side AsyncSequence consumer pattern against a non-trivial Swift AsyncSequence. The generator may emit the right Swift wrapper but the C# IAsyncEnumerable adapter may have gaps.
- `VerificationResult<Transaction>` is a constrained generic — the Session 2 fix list mentioned a `VerificationResult<TSignedType>` constraint bug that was reported as fixed. Verify it actually is at the AsyncSequence-element level, not just at standalone uses.

### Session 7 — Publish StoreKit 2 NuGet + write the strategy doc — **DONE 2026-04-09**

**Status**: complete. **Outcome**: A real `StoreKit.Swift.iOS.0.8.0-preview.1.nupkg` packs cleanly from the generator-emitted csproj, an out-of-repo consumer app builds against it via `<PackageReference>` + extern alias and prints `[STOREKIT-SMOKE] AppStore.CanMakePayments = True / [STOREKIT-SMOKE] PASS` on the iOS Simulator, and `Future/apple-framework-binding-strategy.md` captures the reusable pattern + framework-#2 checklist grounded in StoreKit evidence. The pack flow surfaced one generator bug (NU1012 on `buildTransitive/<tfm>/`) which is fixed at the source rather than worked around in the emitted csproj. Two follow-ups filed for 0.9.0+: dynamic `$(TargetPlatformVersion)` resolution in the generator-emitted csproj (so the pack TFM tracks the workload), and a `dotnet pack` regression gate (so the next NU1012-class bug fails CI instead of a downstream consumer).

**The pack-flow generator fix — `PlatformInfo.LibTfm`** (committed alongside this session): NuGet rejects `<None Pack="true" PackagePath="buildTransitive/{tfm}/" />` items with **NU1012** when `{tfm}` lacks a platform version (e.g. `net10.0-ios` is rejected; `net10.0-ios26.0` is accepted). The first pack attempt against the StoreKit binding tripped this immediately. The fix adds a new required `LibTfm` field to `PlatformInfo` (e.g. `"net10.0-ios26.0"`), updates `GetBuildTransitivePath()` to return `buildTransitive/{LibTfm}/`, and bumps the assertions in `PlatformInfoTests` and `BindingProjectEmitterTests`. Pair-programming review (P0) caught a third stale assertion in `PlatformInfoTests.BindingProjectEmitterPlatformTests.Emit_CorrectTfmAndPackageId_PerPlatform` that the initial diff missed — fixed before commit. The same review filed two P1 follow-ups (now in `roadmap.md`): the SwiftBindings.Sdk pack target already resolves `$(TargetPlatformVersion)` dynamically at pack time (`Sdk.targets:929-933`), so the right end state for the generator-emitted csproj is to compute the pack TFM the same way and delete `LibTfm`; and there is currently no test that runs `dotnet pack` against a generated csproj, so an NU1012-class regression cannot be caught by `nuke test` or `nuke validate` until a downstream consumer hits it.

**Pack invocation that actually works** (recorded for the framework-#2 checklist):
```bash
# 1. Regenerate with --swift-runtime-version set to a published version (so IsPackable=true and the
#    PackageReference branch fires instead of the dev-sentinel ProjectReference). Direct-mode CLI for
#    a system framework, with the @ in @rpath escaped to dodge System.CommandLine's response-file token.
dotnet run --project src/Swift.Bindings/src -- \
  -a StoreKit.abi.json -d StoreKit -t ios \
  -l '\@rpath/StoreKit.framework/StoreKit' \
  --swift-runtime-version 0.8.0 \
  -o /tmp/storekit2-session7-fresh

# 2. Build Release (NU5026 demands the Release dll exists on disk before pack).
cd /tmp/storekit2-session7-fresh && dotnet build -c Release StoreKit.Swift.iOS.csproj

# 3. Pack. Description must avoid em-dashes/parens/spaces or MSBuild misparses -p:Description= as a switch.
dotnet pack -c Release --no-build StoreKit.Swift.iOS.csproj \
  -p:PackageVersion=0.8.0-preview.1 \
  -p:Description=StoreKit2_Swift_bindings_for_dotnet_preview \
  -o ./nupkg
```
Result: `StoreKit.Swift.iOS.0.8.0-preview.1.nupkg`, 982,992 B, 9 entries. Verified layout via `unzip -l`:
- `lib/net10.0-ios26.0/StoreKit.Swift.iOS.dll` (449,536 B)
- `buildTransitive/net10.0-ios26.0/StoreKit.Swift.iOS.targets` (3,336 B)
- `runtimes/ios-arm64/native/StoreKitSwiftBindings.xcframework/Info.plist` + simulator slice
- `runtimes/ios-arm64/native/StoreKitSwiftBindings.xcframework/ios-arm64-simulator/StoreKitSwiftBindings.framework/StoreKitSwiftBindings` (525,592 B real dylib)

Nuspec dependency: `SwiftBindings.Runtime 0.7.0` against `net10.0-ios26.0` — clean.

**Fresh out-of-repo consumer reproducer** at `/tmp/storekit2-consumer-fresh/`:
- Bootstrapped via `dotnet new ios -n StoreKitConsumer` (no SwiftBindings tooling involved).
- `nuget.config` adds a single local feed pointing at `/tmp/storekit2-session7-fresh/nupkg` so the published nupkg resolves alongside `nuget.org`.
- `<PackageReference Include="StoreKit.Swift.iOS" Version="0.8.0-preview.1">` carries `<Aliases>StoreKitSwift</Aliases>` — load-bearing because Microsoft.iOS already exposes a `StoreKit` namespace, and any attempt to use `global,StoreKitSwift` SIGABRTs Mono inside `xamarin_bridge_initialize` (do NOT add `global` back). `AppDelegate.cs` declares `extern alias StoreKitSwift;` at the top and routes the call as `StoreKitSwift::StoreKit.AppStore.CanMakePayments`.
- Build: 0 errors, 45 cosmetic warnings (CA1416 platform-availability + MT7155 duplicate XML database BundleResources between `StoreKit.Swift.iOS.dll` and `Swift.Runtime.dll`; both pre-existing).
- Simulator launch (iPhone 17 Pro, `5B23DA8D-A2E6-4DEB-9A2C-78E34271E5CB`):
  ```
  [STOREKIT-SMOKE] AppStore.CanMakePayments = True
  [STOREKIT-SMOKE] PASS
  ```
- The deployed `.app` bundle contains `Frameworks/StoreKitSwiftBindings.framework/StoreKitSwiftBindings` and `Frameworks/libSwiftBindingsRuntime.dylib`, dyld resolves `@rpath/StoreKit.framework/StoreKit` against the simulator runtime, and the resolver fires zero fallback warnings — exactly the same wire-up that worked in `RuntimeTestsApp` in Session 5, but now reaching it via the published nupkg path instead of an in-repo `ProjectReference`.

**Strategy doc** (`src/docs/Future/apple-framework-binding-strategy.md`, ~290 lines): TL;DR ("what ships" vs "what does NOT work"), answers to the 12 Phase-1 strategy questions from `0.8.0-plan.md` each grounded in a specific Session 1–7 evidence point, the framework-#2 checklist (pre-flight / generator+binding / smoke test wiring / pack+publish / fresh consumer reproducer / validation gates / documentation), a future-questions appendix for everything that StoreKit didn't actually exercise (system frameworks with non-trivial dependency graphs, subscript chains crossing module boundaries, value-type marshalling on hot paths, etc.), and cross-references to `0.8.0-plan.md`, `0.8.0-storekit2-exploration.md`, the new `roadmap.md` follow-ups, `BindingTests/RuntimeTestsApp/SmokeTests/StoreKitSmokeTests.cs`, `PlatformInfo.cs`, and `SwiftFrameworkResolver.cs`. Wrapper-package was explicitly falsified in the doc — direct-mode + `swift-api-digester` + extern alias is the pattern that holds up.

**Final gate (this session)**:
- `nuke test`: 9506 unit + 20 analyzer + 545 runtime tests pass / 0 fail / 2 skip; unit-test count up by 0 vs Session 6 baseline (the LibTfm fix only updated existing assertions, no new tests).
- `nuke validate`: 95/95 overall pass (81/81 cs_compile, 61/61 swift_compile, 14/14 dependency gate), no regressions vs baseline.
- BindingTests / runtime simulator tests: not re-run this session — the generator change is `PlatformInfo.LibTfm` only, which affects emitted-csproj pack paths and is fully covered by the updated unit-test assertions; nothing in the closure/marshalling/runtime path is touched. The Session 6 baseline (1320 pass / 0 fail / 23 skip) stands.

**Done criteria** (all met):
1. ✅ `dotnet pack StoreKit.Swift.iOS.csproj` produces a valid `.nupkg` with the wrapper xcframework, source binding `.dll`, and consumer `.targets`.
2. ✅ Fresh out-of-repo consumer app prints `[STOREKIT-SMOKE] PASS` on the iOS Simulator using `<PackageReference>` against the local-feed nupkg.
3. ✅ `Future/apple-framework-binding-strategy.md` written, all 12 Phase-1 strategy questions answered against StoreKit evidence.
4. ✅ Strategy doc includes a framework-#2 checklist short enough to follow without reading the StoreKit history.

**Files touched**:
- `src/Swift.Bindings/src/Configuration/PlatformInfo.cs` (new `LibTfm` field, updated `GetBuildTransitivePath()`)
- `src/Swift.Bindings/src/Configuration/PlatformInfoFactory.cs` (`LibTfm` set for all 4 platforms)
- `src/Swift.Bindings/tests/UnitTests/ConfigurationTests/PlatformInfoTests.cs` (3 assertions updated — the third one was a pair-programming-review P0 catch)
- `src/Swift.Bindings/tests/UnitTests/EmitterTests/BindingProjectEmitterTests.cs` (2 assertions updated)
- `src/docs/Future/apple-framework-binding-strategy.md` (NEW)
- `src/docs/0.8.0-storekit2-exploration.md` (this Session 7 outcome + status row at line 349)
- `src/docs/roadmap.md` (new entry: "Generator-emitted csproj should compute pack TFM dynamically" + companion `dotnet pack` regression gap)

### Session-this follow-ups (small; fold into another session)

None of these warrant their own session — list them so they don't get lost:

- ~~**Re-run `nuke validate`**: was last run on the initial direct-mode implementation (before the Codex review fixes); the post-fix verification was `nuke test` only. Fold into the Session 4 final gate.~~ **Done — Session 4 final gate.**
- ~~**CLI integration test for the system-framework gate**: `BindingProjectDirectModeTests` covers the emitter, but no test asserts the `isSystemFrameworkTarget` gate fires from the CLI based on `-l @rpath/...`. Fold into Session 4 sub-task B if the emitter is touched.~~ **Done — `BindingsGeneratorCommandTests.IsSystemFrameworkTargetTests`.**

### Dependency graph

```
Session 4 ─→ Session 5 ─→ Session 6 ─→ Session 7
(A + B)
```

Session 4 bundles two independent sub-tasks (A and B) that both gate Session 5; if A explodes, B can ship standalone and A respawns as its own session. Sessions 5, 6, 7 are strictly sequential. Once Session 6 is green, Session 7 closes out Phase 1's StoreKit 2 deliverable and unblocks framework #2.
