# Remediation Gameplan — Fixing the Audit Backlog

> The execution plan for fixing the ~104 confirmed defects in `STATE-OF-THE-CODEBASE.md`. Ten sessions, sequential, each grouped by **fix-shape** so related bugs are knocked out together. Claude drives; Grok pairs on plan/design, Codex pairs on code review; every fix is fixture-first TDD.
>
> Read `STATE-OF-THE-CODEBASE.md` for the per-bug mechanism and `file:line`. This doc is the *queue and the rules* — it references backlog IDs (P0-NN / P1-NN) rather than re-deriving them.

---

## 0. Phase model — Phase 2 is a FUTURE discussion, not in scope here

> **For a future session to raise with the owner — do not start Phase 2 work off this note.**

This plan is **Phase 1 only**: the ten sessions that fix the **~104 *confirmed*** defects (the ones that passed the audit's adversarial verify gate).

Beyond the 104 sits a **~280 deferred-candidate pool** plus three **un-run tracks** (L1 docs-drift, L2 ObjC interop, L3 perf) and the audit's own ~40–60% per-run recall ceiling. **These are unverified leads, not confirmed bugs** — direct evidence (the refuted tier, e.g. the `nonmutating`/`indirect enum` false alarms in Track A8) shows a real fraction would wash out as unreachable, inert, or duplicate on inspection. See `STATE-OF-THE-CODEBASE.md` §6.

**If we choose to pursue the tail, it is a separate Phase 2 with its own discipline — never an extension of these ten:**
1. **Re-measure first.** Phase 1's grep-sweeps (the "fix the *pattern*, not the instance" rule) will already have absorbed every *same-shape* deferred sibling, so the surviving pool is materially smaller than 280. Count what's actually left before deciding anything.
2. **Verify before fixing — no patch-on-suspicion.** Run the surviving leads back through the audit harness (`.claude/workflows/codebase-audit.js`, per `RESUME.md`) so the verify gate sorts confirmed / inconclusive / refuted. Fixing a deferred candidate without re-verification is the line we don't cross — it's what makes the 104 trustworthy and the 280 not.
3. **Pick by yield, stop when it drops.** Highest-expected-yield pools (capstone §7): A4 GenericClosureBridge, A7 `WrapperEmitter.Async` live/dead boundary, C1 parser brace/scope, M3 classification family.
4. **The survivors become their own capped fix-plan**, gated exactly like this one (§1).

**This is a decision made ONCE, after Phase 1 lands and the tail is re-measured — not a mid-stream cascade.** Until then, the ~280 are neither ignored nor open season: they are logged, unverified, and out of scope.

---

## 1. Scope discipline (read this before every session)

**This is a hard contract, not aspirational. It exists because plans like this reliably balloon to 20+ sessions through deferral and cascade.**

1. **Ten sessions is the ceiling.** Not a starting estimate. The number does not grow. It compresses to ~8 or splits to ~12 *only* along the levers in §2 — and a split (10a/10b) still counts as **one** of the ten.

2. **Same-shape discovery is absorbed, never deferred.** The sessions are grouped by fix-shape precisely so that finding more sites of the same bug is *expected*. If Session 2 finds 12 unguarded UCO callbacks instead of 8, you fix all 12 in Session 2. Finding more is the grouping succeeding — it is never a reason to spawn a follow-on session, file a roadmap item, or close "partial with deferral." (See memory `feedback_no_session_cascade.md`, `feedback_no_autonomous_defer.md`.)

3. **The ~280 deferred candidates are OUT of scope.** This plan fixes the **confirmed ~104 only**. We do not chase the deferred pool here. Pulling deferred candidates into a session is the #1 way this plan hits 20 — don't.

4. **New confirmed bugs found mid-session route by file/shape, not by spawning:**
   - Same file *and* same fix-shape as the session's scope → **absorb it** (fix it now).
   - Anything else → log one line in §6 (Discovered / Out-of-Scope). **Do not** spawn a session, do not fix it now, do not let it expand the current session's review surface. §6 gets triaged *after* the ten land, as a deliberate decision.

5. **Splitting is not a lever; compression is.** When a session feels too big, the default response is to *compress* — drop its P2s to §6, sequence its sub-steps inside the one session — **not** to split. A 10a/10b split is permitted only under all four of these conditions, together: (a) it divides the session's **existing** scope along a review-surface seam and adds **zero net new scope**; (b) **both halves land before the next session starts** — 10b is never deferred, never queued, never carried; (c) it has explicit **owner (user) sign-off** — never an engineer's or reviewer's self-judgment at plan-check; (d) a written compression attempt was made first and is recorded as having failed. "The diff is large" is not, by itself, sufficient — large is the *expected* shape of a meaty session. A split still counts as **one** of the ten. The plan pre-authorizes **no** splits; S3 and S4 in particular are explicitly *not* pre-cleared to divide.

6. **"Done" is not negotiable down.** A session closes only when: every backlog ID in its scope is fixed (not weakened, not skipped), fixtures are green, `nuke test` + `nuke binding-tests` pass count ≥ baseline, the codebase is grep-swept for *all* instances of the pattern fixed, and the Codex re-review loop is clean. No "will fix later" for items in scope. (Root-cause, not symptom — `CLAUDE.md` Working Guidelines.)

---

## 2. The ten sessions

Hard ordering is only **1 → {2,3,4} → 10**: prep first, capstone last (it verifies 2/3/4). Sessions 5–9 slot anywhere in between.

| # | Session | Size | P0s | Other backlog | Gate |
|---|---|---|---|---|---|
| 1 | Prep & de-risk | S | — | Cluster 7 live/dead; identifier-guard helper | `nuke test` + compile |
| 2 | UCO-escape hardening | L | P0-01, P0-02 | P1-12, P1-16, P1-17, P1-18, cdecl-invoke-thunk | `--device` |
| 3 | ABI core | L | P0-05/06/07/08 | P1-09/10/11/13, P2 enum+struct | `--device` |
| 4 | Existential + runtime struct safety | L | P0-09, P0-10 | P1-01..08, P1-14, P1-15, UCO-escape (proxy + x-module) | `--device` |
| 5 | SwiftUI bridge | M–L | P0-03, P0-04, P0-13 | P1-19, P1-20, trampoline, dup-param guard | `--sim` |
| 6 | Generics / specialization | M | P0-11, P0-12 | P1-28, BoundGenerics P2 | `--device` |
| 7 | Name/key consistency | M | — | P1-21, identifier-guard rollout | `nuke validate` |
| 8 | Input fidelity | M | — | P1-26, P1-27, classifier theories | `nuke validate` |
| 9 | Packaging / SDK + co-gater | M | P0-14 | P1-23/24/25 | pack-gate + consumer round-trip |
| 10 | Gate-hygiene capstone | M | P0-15 | false-"Issue 1" purge, meta-test, P2 coverage | `--sim` + `--device` |

**Coverage check:** every P0-01..15 and P1-01..28 is placed; the §4-"most important" P2s ride their owning session. Nothing in the confirmed backlog is unassigned.

**Levers (compression only):** compress to ~9 by merging 8+9; to ~8 by also folding 1 into the front of 2. **There is no expansion lever.** A session that feels too big gets a compression plan first (drop its P2s to §6, sequence sub-steps within the one session); it splits only with explicit owner sign-off under §1.5's four conditions. The plan does **not** pre-authorize splitting S3 or S4 — the earlier framing of "split runtime-ARC vs emitter-proxy" was a loophole and is withdrawn (Grok H3). S4's runtime+emitter coupling is precisely why it stays whole.

---

### Session 1 — Prep & de-risk ✅ COMPLETE
- **Status:** Done — gates green (`nuke test` unit 12223/0, runtime 575/0; `nuke binding-tests --compile-only` regression gate passed — emitter changes, no compile regression), Codex+Grok paired review signed off. See §5 tracker + the §Session-1 live/dead map below.
- **Scope:** Resolve every live/dead emitter boundary the audit flagged (Cluster 7): async dead duplicate (the `EmitAsyncWrapper*` copy in `WrapperEmitter.Async.cs`) vs live `AsyncHarnessEmitter`; shadowed-but-wired `MethodGenericBridgeEmitter`; EveryProtocol skip-ladder where the `HasNoncopyableMember` gate sat at the emission ladder (`EmitProtocolConformance`) but was missing from the pre-scan ladder (`WillSkipConformance`). Delete the dead paths; add a structural test asserting exactly one live emitter per family. Build the shared **reserved-synthetic-name guard helper** (infra for the P1-22 identifier family rolled out across later sessions).
- **Why first:** §7 of the synthesis is explicit — resolve the live/dead boundary *before* patching, or Cluster 1/Generics fixes land in dead code. This is the one risk code-level confidence does not cover.
- **Done-when:** live/dead map written into this doc; dead emitters deleted; one-live-emitter tests green; guard helper merged with unit coverage.

#### Session 1 live/dead map (Cluster 7 — RESOLVED)

Three emitter-family boundaries resolved. Structural guard: `EmitterFamilyLivenessTests` (reflection over the generator assembly + `MethodHandler.BridgeEmitters`) pins each partition so a refactor can't silently re-split a family into a live half + a dead-shadow half.

**1. Async wrapper emission — two complementary halves, both LIVE, one per file (the duplication was a *cross-copy*, not a single dead emitter).**

| Half | Live owner | Entry / call site |
|---|---|---|
| C# async callback plumbing (TCS, GCHandle, `UnmanagedCallersOnly` callbacks) | `AsyncHarnessEmitter` | `EmitAsyncWrapper` (+ `EmitAsyncWrapperForTuple/String/ArrayString/ComplexType/Collection`); invoked at `WrapperEmitter.cs:527` |
| Swift `@_cdecl`/`@_silgen_name` async wrapper body | `WrapperEmitter` (`.Async` partial) | `EmitAsync` (`WrapperEmitter.Async.cs:39`) → `BuildSwiftAsyncWrapperCode` (`:1437`) / `BuildSwiftCatchBody` (`:1359`); invoked at `WrapperEmitter.cs:553` |

- **DEAD (deleted):** the cross-duplicates — the C# `EmitAsyncWrapper*` copy that lived in `WrapperEmitter.Async.cs` (−1187 lines) and the Swift `BuildSwiftAsyncWrapperCode`/`BuildSwiftCatchBody`/`BuildAvailabilityLines` copy that lived in `AsyncHarnessEmitter.cs` (−~230 lines). Each file now keeps exactly one half. `AsyncHarnessEmitter`'s class docstring was corrected — it no longer claims to emit Swift.
- **Note for S2:** P0-02's async-callback line refs (`AsyncHarnessEmitter.cs:410/497/539/912/1187/1289/1291`) are the **C# plumbing** half — that is the live UCO-escape surface. The Swift body half is `WrapperEmitter.Async`.

**2. `MethodGenericBridgeEmitter` (sync method-generic bridge) — CONDITIONALLY LIVE, KEPT (not dead, not a duplicate of the async sibling).**
Reachable for a sync, non-throwing method with exactly one method-own generic param constrained to a **class-bound** protocol (explicit `AnyObject` or transitive `TypeRecordFlags.ClassBound`), the generic param in direct parameter positions only, on a **non-generic** parent, in **XCFramework** mode. Wired once as `MethodGenericBridgeAdapter` (`MethodHandler.cs:761`), dispatched in the bridge loop (`:907`, first non-null short-circuits). CSM (`ConcreteProtocolSpecializationEmitter`) does **not** pre-consume this shape: its suppression (`MemberValidationPipeline.IsCsmSyncEligibleForGenericParent`) requires `parent.IsGeneric`, and CSM runs *after* the method pass and is additive for the non-generic-parent sync route. No end-to-end fixture exercises sync emission today — only direct `TryEmit`/`IsEligible` unit tests and the async sibling (`AsyncMethodGenericBridgeAdapter`, `_XMA`).
- **Consequence: P0-12 (fixed-256B buffer overflow + double-free) and P1-28 (frozen-ref variant) are real latent bugs on the live path, NOT moot.** This resolves the §Session-6 prerequisite (this doc, Session 6 "Prereq"). Session 6 owns the buffer/ownership fix **and** the first end-to-end BindingTests fixture for sync MGB.
- Doc-drift fixed: `IsEligible`'s docstring claimed a `MemberEmissionValidator` placeholder-gate consumer that does not exist (zero production call sites — only unit tests; sibling bridges *do* have that wiring). Corrected, with the missing placeholder-gate wiring logged as P1-28/S6, not S1.
- Concurrence: Codex + Grok independent reviews and direct code-reading all classified CONDITIONALLY LIVE.

**3. EveryProtocol skip-ladder triplication — SYNCED (single live emitter; the three gate sites now move in lockstep).**
`EveryProtocolEmitter` is the sole emitter. The `HasNoncopyableMember` skip gate — present at the emission ladder (`EmitProtocolConformance`) but **missing** from the pre-scan ladder (`WillSkipConformance`) — was added at the pre-scan site so Pass-1 `PreScanProtocols` seeds the noncopyable-member skip that Pass-2 propagates through `genericSig` constraints (`HasUnsatisfiedProtocolConstraintInGenericSig`). Both `HasNoncopyableMember(protocolDecl)` call sites now move in lockstep (referenced by method name, not a drift-prone line number). Covered by `PreScan_ChildBeforeNoncopyableParent_StillSkipsChild`. (Per §3, behavioral `EveryProtocolEmitter` *leak* fixes remain S4-owned; S1's edit here is the mechanical gate-sync only.)

**Infra also delivered:** the reserved-synthetic-name guard — `NameProvider.MakeNonCollidingSyntheticName` + `SyntheticNameScope` (escapes a synthetic emitter local with a `__` prefix when a projected user identifier collides — CS0136/CS0100). Built + 25 unit tests here (incl. the verbatim-`@` no-collision / empty-reserved edges — the result is never `@`-prefixed on any path — and rejection of a prefix-only `@` that strips to an empty identifier); **applied** at the per-site owners in later sessions (§3 P1-22 table). Not yet wired into any emitter — Session 1 builds the tool only.

### Session 2 — UCO-escape hardening (Cluster 1)
- **Scope:** P0-01 (closure callbacks `ClosureEmitter.cs:132`, `.Throwing.cs:86`, `.IndirectReturn.cs:83`, `.SwiftWrapper.cs:492`), P0-02 (async callbacks `AsyncHarnessEmitter.cs:410/497/539/912/1187/1289/1291`, `AsyncMethodGenericBridgeEmitter.cs:824/866`), P1-16 (nested-closure box leak), P1-17 (cancellation key recycle), P1-18 (async GCHandle leak), P1-12 (`@convention(c)` Bool param). Wire the dead cdecl invoke thunk (unblocks Session 10). **Apply the S1 identifier guard at `MethodClosureBridge.cs:385` + `NestedClosureBridge.cs:326`** (P1-22 sites in files this session already edits — in-scope by §3, not an expansion).
- **Fix-shapes (two, both closure-family):** (a) *UCO-escape hardening* — every UCO callback body wrapped try/catch → existing `SwiftError*`/`TrySetException` channel; check `errorPtr` *before* consuming any result pointer; zero/skip indirect buffers on throw; free buffers in catch. (b) *Closure param-direction bridging* — P1-12 + the cdecl-invoke-thunk wiring are a distinct return/param-path fix-shape, not UCO-escape (Grok M1); they ride this session by file-ownership (`WrapperEmitter.Marshalling` is closure code), and are called out separately so the grouping stays honest.
- **Prereq:** Session 1 live/dead async map.
- **Done-when:** fixture where the C# delegate throws (non-primitive / indirect / async success+error) asserts graceful fault, no SIGABRT, on `--device`.

### Session 3 — ABI core (A1/A2 codegen)
- **Scope:** P0-05 (throwing-ctor register swap), P0-06 (`consuming` double-free), P0-07 (eightbyte mis-count → silent garbage), P0-08 (x8 sret loss), P1-09 (null-layout small return), P1-10 (`Foundation.Data` decompose), P1-11 (PWT culture-order), **P1-13 first** (parser ownership — upstream enabler of P0-06). P2: enum-tag truncation, nested/packed struct alignment, `AlignmentMask`.
- **Fix-shape:** drive register placement from `CdeclSignatureContract` phase order; bucket fields into eightbytes; `IsIndirect`-aware x8 bridge; `StringComparer.Ordinal` for PWT.
- **Prereq:** P1-13 before P0-06 within the session.
- **Done-when:** `consuming` deinit-runs-exactly-once fixture; `{Int8×5,Int64,Int64}` by-value return fixture; both green on `--device`.

### Session 4 — Existential ownership + runtime struct safety
- **Scope (13 owned — was 14; P1-01 closed by the merged #40 fix):** P0-09 (opaque `any P` double-release), P0-10 (finalizer-thread direct VWT), **P1-01 ✅ CLOSED via merge — verify, don't redo** (`Arc.Retain` → `Arc.UnknownObjectRetain`, both twins; `SwiftMarshal.cs:469`/`:1523`), P1-02 (borrowed SafeHandle), P1-03 (existential box leak), P1-04 (optional-existential nil stub), P1-05 (value-return leak ×3), P1-06 (int/long box drift), P1-07 (collection-element leak), P1-08 (class-bound array stride), P1-14 (`SwiftHandle` sub-8B over-read), P1-15 (`Optional<T>` inline size), **+ the 2 UCO-escape FailFast guards promoted from §6** (still unguarded post-merge — see Second fix-shape + the coordination note).
- **Fix-shape:** the existential **+1/Destroy contract is one fix** spanning runtime (`ExistentialContainer`/`SwiftMarshal`) and emitters (`ProtocolProxyEmitter.*`) — balanced *conditionally* (the proxy path is already balanced; a blanket Destroy over-releases). That coupling is why this is one session, not two. *Note: the ObjC-rooted retain twin of this contract (P1-01, the isa-dispatching `Arc.UnknownObjectRetain` on the class extraction path) already landed with the #40 merge — S4's remaining work is the existential/opaque-`any P` half + the runtime struct items, not the class-extraction retain.*
- **Second fix-shape (rides this session by file-family — UCO-escape hardening, promoted from §6; STILL OPEN post-merge).** The same P0-01/P0-02 escape shape — a generated `[UnmanagedCallersOnly]` callback invoking managed user code with no fault guard → throwing continuation unwinds into native Swift → SIGABRT — survives in two emitters S2's enumerated scope never touched. **The #40 merge did NOT touch these** (it added only the class-param *marshalling* branch, not the fault guard), so both are confirmed still-bare and remain S4's: **(a)** every `ProtocolProxyEmitter.Receivers.cs` receiver that invokes the user `IProtocol` impl — method `:1168`/`:1185` (`impl.{Method}(…)`), property getter `:198` / setter `:277`, `_del()` thunks `:447`/`:517`, async closure receiver `:593` (`impl.{Method}(handler)`); the bodies guard only the `TryGetProxy`/null-impl path, the managed call is bare, and the in-source comments at `:164`/`:263`/`:642`/`:970` *explicitly acknowledge* "`[UnmanagedCallersOnly]` cannot throw / terminates the process" yet leave the call unguarded — **this is the file S4 already owns**, so harden it in the same pass; and **(b)** `CrossModuleExtensionEmitter.Class.cs` `EmitClosureCallback` (`:367–392`, `var __del = (…)__handle.Target!; __del(args);` bare — the completion-handler closure path; the sibling Task-bridge completion UCO at `:1105–1143` is already guarded). Apply the existing `ClosureEmitter.EmitNonThrowingFailFastCatch` (no error channel) or TCS `TrySetException` guard, `.Target!`-inside-try, matching the proven P0-01/02 pattern. Full technical detail (file:line per call site, the GCHandle-ownership note for the x-module box) is in the §6 "Unguarded `[UnmanagedCallersOnly]` callbacks" entry. Each newly-guarded site ships an emitter-output fixture asserting the FailFast/TCS catch wraps the delegate invoke (the runtime FailFast path aborts the process, so the durable assertion is at the emitter layer, as with P0-01/02).
- **Done-when:** ObjC-backed class via Optional/Result/tuple asserts retain balance (**the merged `ClassParamCallback`/`ClassObjCRooted`/`CompositeProjection`/`EnumHandlerOutput` fixtures already cover this for P1-01 — reuse, don't re-author**); owned opaque `any P` dropped without Dispose + `WaitForPendingFinalizers` asserts single deinit; the two UCO FailFast guards each carry an emitter-output fixture; `--device`.
- **✅ Coordination — #40 fix MERGED into this branch (do NOT duplicate; verify what landed, then build on it).** Branch `fix/protocol-proxy-class-param-receiver` (customer issue [#40](https://github.com/justinwojo/swift-dotnet-bindings/issues/40)) has **landed in `audit-workflows`** — commits `8fe86baf` (reverse-callback receiver fix + ObjC-rooted ARC sweep), `793e77a4` (Fix A — `Optional<@objc>` return adopt), `f73a86f7` (UCO closure-callback exception guard). Its plan is now in-tree at `src/docs/protocol-proxy-class-param-receiver-fix.md`. **S4 starts from this merged state.** What it did, and what it explicitly leaves for S4:
  - **P1-01 — CLOSED.** `Arc.Retain` → `Arc.UnknownObjectRetain` at `SwiftMarshal.cs:469` (`ExtractCopiedValue`) + `:1523` (`ExtractCopiedElement`). Marked ✅ done in §5; **S4 verifies, does not re-implement.** Drops Session 4 to **13** owned items (was 14).
  - **§7 #7 ObjC-NSObject retain-balance fixture exists** — `BindingTests/Sources/SwiftBindingsTestLib/Protocols/ClassParamCallback.swift` + `BindingTests/RuntimeTestsApp/Protocols/ClassParamCallbackTests.cs`, plus the `ProtocolProxyEmitterTests` copy-out assertion and `ClassObjCRootedTests`/`CompositeProjectionTests`/`EnumHandlerOutputTests` unit coverage. **S4's "retain balance" done-when reuses these — don't re-author the reverse-callback variant.**
  - **Reverse-callback marshalling (the former §6 [COVERED] item) — DONE.** `ProtocolProxyEmitter.Receivers.cs` now routes `ClassProjection`/`ObjCRootedClassProjection`/`Optional<class>` receiver params through `SwiftMarshal.MarshalBorrowedClassFromSlot`/`MarshalBorrowedOptionalClassFromSlot` via `GetReceiverClassCopyOutExpr` (`:1377`) at every receiver site. **This is the marshalling branch ONLY — it is NOT the UCO FailFast guard, which is still bare and still S4's (see Second fix-shape).**
  - **Adjacent ObjC-rooted ARC sweep also landed (same root cause as P1-01, mostly outside S4's files — be aware so you don't re-find it):** async `self`-retain isa-dispatch (`WrapperEmitter.Async`, `AsyncMethodGenericBridgeEmitter`, `AsyncHarnessEmitter`), enum class-payload over-retain *removal* (`EnumHandler.Marshalling.cs`, three sites — `InitializeWithCopy` already owns the +1), and **Fix A** — `Optional<@objc>` return now adopts via `GetINativeObject(…, true)` in `OptionalProjection.GetReturnPlan`. Two flagged sites (CE1 closure class-return, SUI1 SwiftUI trampoline) were verified pure-Swift-only and left untouched. None are S4-owned files, but all share P1-01's isa-dispatch root cause — useful precedent for the P1-05/06/07 value/box/collection-leak items.
  - **STILL ours (the merge scoped these OUT):** P0-09, P0-10, P1-02, P1-03, P1-04, P1-05, P1-06, P1-07, P1-08, P1-14, P1-15, **and the two UCO-escape FailFast guards** (group (a) ProtocolProxy receivers + group (b) CrossModuleExtension closure callback — confirmed still-bare post-merge).
  - **Same-file neighbors — coordinate, don't collide (files moved with the merge, findings didn't):** the merged `Optional<class>` receiver branch sits **adjacent to P1-04** (`Receivers.cs:931`-region, optional-existential nil stub — keep distinct); `…SwiftObject.cs` hosts both the merged copy-out helper region and **P0-10** (finalizer-thread VWT — different concern). Re-confirm exact line numbers against the merged tree before editing, not against this doc's pre-merge refs.

### Session 5 — SwiftUI bridge
- **Scope:** P0-03 (`init(rawValue:)!` trap, 6 sites), P0-04 (ObjC-pointer-as-struct-bytes), P0-13 (async dup trailing param), P1-19 (Data→NSData UAF), P1-20 (frozen-with-ref closure leak), the Cluster 1 trampoline try/catch (`SwiftUIBridgeEmitter.cs:3468`), and the SwiftUI identifier-dup (apply Session 1 guard).
- **Fix-shape:** `guard let … else` graceful surface uniformly; `IsObjCBridgeable` branch (closure path already correct at `:3854-3873`); de-dup trailing synthetic params vs user params.
- **Note:** single 3,962-line file → all SwiftUI bugs land here regardless of cluster.

### Session 6 — Generics / specialization (A6)
- **Scope:** P0-11 (class-conformer carrier-wrap UAF), P0-12 (fixed-256B buffer overflow + double-free), P1-28 (frozen-ref variant), P2 (`BoundGenericsHandler` leaf-arg drop). Apply identifier guard at `ConcreteProtocolSpecializationEmitter.cs:1585`.
- **Fix-shape:** read class conformers via `*(IntPtr*)resultPtr`; size buffers via `GetSwiftTypeSize<T>()`; discriminate ownership before freeing. The CSM emitter is the correct reference — port its discrimination to the bridge.
- **Prereq:** Session 1 resolved whether `MethodGenericBridgeEmitter` is the live path.

### Session 7 — Name/key consistency (C2) ✅ DONE (see §5 row 7 for the landed detail)
- **Scope:** P1-21 (emitted-name/dedup-key divergence: `IHandler.GetProjectedCSharpMethodKey`, `IEnvironment.CSharpMethodName`, `WrapperEmitter.Signature.cs`). **P1-22 orphan rollout collapsed to a verify** — the named sole-owner site `ModuleEmissionContext.cs:82` is a **FALSE POSITIVE** (a `Regex` collision field in `SetCollisionContext()`, not a synthetic-name emission site → no `SyntheticNameScope` guard applies); the sweep found no genuine sole-owner synthetic-name orphan (the only residual was the §6 *user-param escape* gap, not a synthetic-local one). All P1-22 sites verified covered across S2/S4/S5/S6 (§3 table). Refreshed the stale `constraints.md` WasEmitted inventory (doc claimed 13/6 → live **23 assignments across 12 files**).
- **Fix-shape:** thread `propertyNames` into both static key builders (`IHandler`/`DefaultParameterOverloadEmitter`) in lockstep; make the same-module override verifier and the existential-bypass emitter prefer the authoritative `EmittedCSharpName`/`env.CSharpMethodName`; reserve adopted collision-suffix override names up front (`PreReserveAdoptedOverrideNames`) so dedup is declaration-order independent.
- **Gate:** `nuke validate` — changes dedup keys → broad output blast. Run after emitter sessions settle so it rebases on their output.

### Session 8 — Input fidelity (M3 + A8)
- **Scope:** P1-26 (classification drift: objcPrefix, rawValueType, NSString-typedef-as-blittable, Date tuple, forced ObjC-class — `apple-frameworks.json`, `*Database.xml`, `AppleFrameworkRegistry.cs`), P1-27 (parser/demangler: typed-throws last-match, `@Sendable`/`YK`, `where …: AnyObject` decl-drop, paren-in-string EOF, protocol-requirement misclassification). Add the missing classifier `[InlineData]` theories (currently zero coverage).
- **Gate:** `nuke validate` — data-table changes touch many libs.

### Session 9 — Packaging / SDK + co-gater (M2)
- **Scope:** P0-14 (co-gater blind to `[DllImport]`+`static extern`), P1-23 (bridge xcframework arm64-only → DllNotFound on x64-sim/Rosetta), P1-24 (Guard 2a static slice-id), P1-25 (non-transactional fat-fold).
- **Fix-shape:** broaden co-gater regex + partial-decl finder; thread `targetArchitectures`/`--target-architectures` through the bridge compile exactly like the wrapper; atomic lipo-overwrite.
- **Caution:** `constraints.md` carries dense hard-won wrapper-arch invariants — high-trap session. Honor every one.
- **Gate:** pack-gate + `dotnet build -r` consumer round-trip on `iossimulator-x64`.

### Session 10 — Gate-hygiene capstone (M4) ✅ DONE (see §5 row 10 for the landed detail)
- **Scope:** P0-15 (live wrong-ABI `AsyncGenericContainer` method behind a false-reason `[Skip]`), purge every "Issue 1"/"!ji->async" skip on a pure-Cdecl path (`ClosureEdgeCaseTests.cs:224/234`, `SwiftBindingsTestLib.cs:25006` + `:169929`, `Build.RuntimeTests.cs:1849`), un-skip the tests Sessions 2/3/4 un-gated, add the **meta-test invariant** (any such skip must have ≥1 CallConvSwift P/Invoke on its path), fix P2s (`coverage-report.py:1037` runtime-coverage gap, `TestDiscoveryGenerator.cs:149` async-void detached).
- **Why last:** it verifies the earlier fixes and restores the `feedback_mono_jit_blame.md` trust contract. Depends on 2/3/4.
- **Landed:** P0-15 suppressed at source (`MemberValidationPipeline:274`); false Issue-1 skips purged + each remaining path documented pure-`CallConvCdecl`; Sessions 2/3/4 un-skips restored; meta-test invariant `Issue1SkipAttributionTests` (skip's `$s…` symbol must be a real `CallConvSwift` entry point AND referenced in the test body); the **macOS/Catalyst third-platform gate fix** via the runtime-detected `[SkipOnMonoJit]` (`IsMonoJitRuntime = IsMonoRuntime || IsMacCatalyst`); both P2s (`coverage-report.py` runtime-coverage WARNING; `SBTD001` async-void Roslyn diagnostic). Gates green on `nuke test` + sim + macOS + device. Codex+Grok r1/r2 paired review closed (r2: Codex 0 findings; Grok no new High, one doc-drift Medium fixed in place).

---

## 3. Single-owner file map

A file has one owning session (sequential execution relaxes this, but keep it for review coherence — don't re-open a file gratuitously). Hot multi-cluster files resolved to one owner: `SwiftUIBridgeEmitter.*` → S5; `EveryProtocolEmitter.cs` → S1 (skip-ladder) + S4 (leaks); `ConcreteProtocolSpecializationEmitter.cs` → S6; `WrapperEmitter.Async.cs` → S2; the runtime struct files (`SwiftHandle`/`SwiftOptional`/`FrozenStructHandler`) → S4.

**S1's edits to later-owned files are bounded to mechanical de-risk only** (delete dead `WrapperEmitter.Async` duplicates; sync the `EveryProtocolEmitter` skip-ladder `HasNoncopyableMember` gate; build the guard helper). The *behavioral* fixes to those files stay with the owning session (S2 async UCO, S4 Every leaks). So S1↔S2/S4 touch the same files at non-overlapping intent — that bounds the review surface Grok flagged in H2.

### Identifier-guard (P1-22) — explicit per-site ownership (resolves Grok H1)

The P1-22 family does **not** localize to one file, so the helper is built once in S1 and **each site is owned by the session that already edits its file**. Applying the guard at a P1-22 site in a file a session is *already touching* is **in-scope by definition — not a scope expansion** (this is the one cross-cutting exception §1.2's "absorb same-shape" explicitly covers). S7 owns only the C2 key-divergence (P1-21) plus the orphan sites in files **no other session edits**, and then **verifies** total coverage — it never re-opens a file another session already closed.

| P1-22 site | File | Owning session |
|---|---|---|
| `MethodClosureBridge.cs:385` + `NestedClosureBridge.cs:326` synthetic names | closure-bridge | **S2** (already edits both for P0-01/P1-16) |
| `SwiftUIBridgeEmitter.cs:2776` | SwiftUI | **S5** |
| `ConcreteProtocolSpecializationEmitter.cs:1585` / `AsyncGenericParent.cs:895` | generics | **S6** |
| `EveryProtocolEmitter.cs` synthetic locals | existential | **S4** |
| `ModuleEmissionContext.cs:82` ~~(+ any orphan sites)~~ | — | **S7** — ✅ resolved: **FALSE POSITIVE**, not a synthetic-name site (a `Regex` collision field in `SetCollisionContext()` that rewrites type-reference strings; no guard applies). No genuine sole-owner orphan exists. |
| coverage verification across all sites | — | **S7** ✅ verified — all sites covered across S2/S4/S5/S6 |

Note: `constraints.md:18`'s WasEmitted inventory was stale (claimed 13 points/6 files). **S7 refreshed it to the live count: 23 `WasEmitted = true;` assignments across 12 files**, and added a durable guard test pinning that count so the next forgotten flag is a red test rather than a silent CS0115.

---

## 4. Per-session workflow (the cadence)

1. **Plan check (Grok)** — sanity-check the session's scope and fix-shape before writing code. Confirm up front whether the session decomposes into >1 emission mechanism (the §1.5 split test).
2. **Fixture-first TDD** — write the BindingTests fixture, verify it goes **red** (this is the live reproduction the audit never did), fix, verify **green**. Maximum-case fixtures, not minimum-repro (`feedback_tdd_for_regression_fixes.md`).
3. **Grep-sweep** — before closing, grep the whole codebase for every other instance of the pattern fixed (`CLAUDE.md`: fix the bug *pattern*, not the instance).
4. **Code review (Codex)** — run the review → fix → re-review loop until clean.
5. **Gates** — `nuke test` + `nuke binding-tests` every session; `--device` where the session row says so (calling-convention/marshalling/ARC changes — Mono and NativeAOT have different bugs); `nuke validate` only for S7/S8/S9.
6. **Zero-regression** — pass counts ≥ baseline before the session closes. No exceptions for in-scope items.

---

## 5. Status tracker

| # | Session | Status | Fixed / Scope | Gates | Notes |
|---|---|---|---|---|---|
| 1 | Prep & de-risk | ✅ done | Cluster 7 live/dead map + guard helper | `nuke test` (12223 unit / 575 runtime, 0 fail) + `binding-tests --compile-only` (no regression) | Async cross-duplicates deleted; MGB CONDITIONALLY LIVE (kept) → P0-12/P1-28 real; EveryProtocol skip-ladder synced; `MakeNonCollidingSyntheticName`/`SyntheticNameScope` (verbatim-`@` contract hardened, empty-strip rejected) + `EmitterFamilyLivenessTests` added. Codex+Grok r1/r2 paired review clean (1 convergent code finding fixed: `@`-prefix leak). See §Session-1 live/dead map. |
| 2 | UCO-escape hardening | ✅ done | 6 / 6 + 4 reviewer absorbs | `nuke test` (Unit: 0 fail; Runtime: 579/0) + `binding-tests --compile-only` (clean) + sim (2553 pass, +1) + device/NativeAOT (2568 pass, +1) | P0-01/02 UCO `handle.Target!`-inside-try across the 4 closure bridges + AsyncHarness/AMGBE; P1-16 NCB box leak; P1-17 `NextCancelKey` monotonic counter (+0-sentinel overflow guard); P1-18 async GCHandle free; P1-12 + cdecl-invoke-thunk; P1-22 `SyntheticNameScope` in all 4 closure bridges. Codex+Grok r2 absorbs: async holder-cleanup **resource leak** in both UCO fault catches (cleanup before faulting TCS); cleanup-**loop-index** CS0136 collision in `AsyncHarness`/`WrapperEmitter.Async`/`AMGBE` (guarded via `SyntheticNameScope`, alias `__i` only on real `i:`-param collision); `NextCancelKey` 0-skip on wraparound. GenericClosureBridge self-register ABI + class-return defects → §6 (pre-existing, `[Skip]`'d). Broader synthetic-name guard gap → §6 (one cross-cutting entry, defense-in-depth). **r3 convergent-High fix:** extracted the inline async-holder cleanup into runtime helper `SwiftAsyncCallHolder.Cleanup`/`CaptureCancellationToken` (per-slot `try/catch`+`finally`-clears → idempotent, non-throwing); this *structurally eliminates* the holder-cleanup loop-index + pattern-var surface (CLOSED, stronger than guarded). **r4 verifying re-review:** Codex clean; Grok r4 Highs empirically refuted (no double-free window — launch P/Invoke is terminal-in-try / AMGBE `__ctxTransferred` flag; `GCHandle.FromIntPtr` is non-throwing so outside-try is safe and `.Target!` is inside-try; utf8-closer bare-`i` is a false positive — the emitter loop emits only `}`). One NEW cross-emitter category surfaced + dispositioned → §6: unguarded-UCO-delegate escape in emitters S2 never enumerated (CrossModuleExtension / ProtocolProxy.Receivers / SwiftUIBridge), out-of-scope per §1.4/§3, routed S7/S4/S5. |
| 3 | ABI core | ✅ done | 8 / 8 | `nuke test` (Unit: 12269 pass / 0 fail) + `binding-tests --compile-only` (clean) + sim (2566 pass, +2) + device/NativeAOT (2581 pass, +3) | P1-13 parser models `consuming`/`borrowing` ownership (enabler) → P0-06 consuming non-copyable double-free (`.move()`+`MarkConsumed()`, mark-before-rethrow on throw path); P0-05 throwing class-ctor error-register swap; P0-07 eightbyte mis-count → garbage return; P0-08 ARM64 `x8` sret loss across metadata accessor; P1-09 null-layout small-return float reads 0; P1-10 `Foundation.Data` `@_cdecl` decompose (`_w0`/`_w1`); P1-11 generic PWT culture-order swap (Ordinal sort); P2s enum-tag truncation + nested/packed alignment + AlignmentMask. New BindingTests: throwing + success consuming-noncopyable deinit-exactly-once (sim+device). **Codex+Grok r1/r2 paired review.** r2 Highs dispositioned: (1) baseline `Alamofire`/`YouTubePlayerKit` `compile: ok → fail` — proven **PRE-EXISTING in `f73a86f7`** (clean-HEAD worktree regen byte-identical, 69 = 69; same 5 CS0103), root-caused to a co-gater × unregistered-`SBW_CreateError` stripping asymmetry surfaced via optional throwing-Void closures → **routed to §6**; `.validation-baseline.json` **reverted to HEAD** so the commit doesn't bake in the fail and the next validate re-flags it loud. (2) `ProtocolExtensionEmitter.cs:2106/:2230` synthetic `ArgumentDecl` drops `Ownership` — **LATENT** (a `consuming` protocol-extension param is suppressed upstream by the `IsCdeclCompatibleType` two-layer gate, so no live double-free); defensive explicit `Ownership = ParameterOwnership.Default` + comment added so it stays correct if that gate is ever lifted. (3) Codex Medium reuse-after-consume (SafeHandle stays open post-`MarkConsumed`) — known semantic gap (C# can't reproduce Swift's static use-after-consume ban), not a memory defect, out of scope. No new live-High correctness fix → no r3 per the gated cap. |
| 4 | Existential + runtime struct | ✅ done | 13 / 13 + 2 UCO guards (P1-01 pre-closed via merge) | `nuke test` (Unit: 12395 pass / 0 fail) + `binding-tests --compile-only` (clean) + sim (2619 pass, +29) + device/NativeAOT (2639 pass, +29) | P0-09 opaque `any P` member-return double-release; P0-10 finalizer-thread direct VWT (`DestroyAndFreeExistential` off-thread guard); P1-02 borrowed SafeHandle owns +0; P1-03 existential `swift_allocBox` leak; P1-04 optional-existential receiver nil stub; P1-05 EveryProtocol value-return buffer leak ×3; P1-06 int/long box drift; P1-07 collection-element return leak; **P1-08 class-bound `[any P]` array stride + ✅ sibling `[K: any P]` dict-value stride + ✅ opaque sibling `[any P]`/`[K:any P]` param/write element over-release** (the donate-vs-alias `__owned` carrier fix — `CreateOwnedExistential1`/`CreateOwnedClassCarrier` mint/donate a fresh +1 instead of aliasing the proxy's only +1; one-arg co-gater-stripped overload reached at generated `:41144`, exercised by `TestLabelledContainerBuilder_VariadicExistentialSplat`); P1-14 `SwiftHandle` sub-8B over-read; P1-15 `Optional<T>` inline size mis-clamp; **2 UCO-escape FailFast guards** (group a ProtocolProxy `Receivers.cs` `impl.*` calls; group b `CrossModuleExtension.Class.cs:367–392` closure callback). **P1-01 ✅ CLOSED by the merged #40 fix** (`Arc.UnknownObjectRetain` at `SwiftMarshal.cs:469`/`:1523`) — verified, not redone. New BindingTests: opaque/class-bound existential-collection param + array-write + dict-value LifetimeTracker leak probes (single-deinit / no-leak), `ExistentialParamLeak.swift`. **Codex+Grok r1–r4 paired review.** r2/r3 convergent Highs fixed: (1) one-arg owned-carrier overloads added for the suppressed-proxy co-gater-downgrade path (`CreateOwnedExistential1<T>(value)`/`CreateOwnedClassCarrier<T>(value)` delegating to shared `MintOrDonate*` helpers via no-fallback `GetOrCreate`); (2) Grok r3 H2 broad-catch masking — both carrier-path catches narrowed to `catch (SwiftRuntimeException)` (proven sole no-runtime exception type) + `DestroyAndFreeExistential` frees in `finally`, so a genuine VWT/destroy fault propagates loudly instead of silently returning a +0 alias and re-introducing the P1-08 over-release. **r4 verifying re-review: both reviewers confirm the catch fix introduced no new issue and the one-arg path is genuinely reached at runtime; no new actionable High.** Residuals → §6 (pre-existing, empirically unreachable, logged not queued): Grok r3 H1 carrier-conversion fall-through (EC1 `_proxyClassName==null` + composition arity ≥2; grep = 0 aliasing tails / 0 EC2/3 carriers / 19 owned-carrier conversions), Cluster A FrozenStruct sub-word multi-Optional packing (Codex-High/Grok-Low split, no live consumer), nested `[[any P]]` owned-return. |
| 5 | SwiftUI bridge | ✅ done | 7 / 7 + 2 reviewer absorbs | `nuke test` (Unit: 12398 pass / 0 fail; Runtime: 592/0; Analyzers: 21/0) + sim (2632 pass, +13) + device/NativeAOT (2652 pass, +13; 0 fail / 0 crash both) | P0-03 BoundEnum `init(rawValue:)!` force-unwrap → graceful failable-init+guard-nil (create), if-let-drop (update/set), async-`onError` across 7 Swift sites + C# `Enum.IsDefined` for SimpleEnum; P0-04 ObjC-bridgeable struct init param crosses ABI as an ObjC object pointer (factory `.Handle`; Swift async `_Create` reconstructs via `Unmanaged<AnyObject>.fromOpaque(ptr) as! URL`); P0-13 async `_Create` dedups duplicate trailing synthetic params; **P1-19 ResultClosure ObjC-bridgeable struct UAF** (Swift `let xObj = v as AnyObject` + `withExtendedLifetime` + `passUnretained`; C# `GetNSObject<T>` instead of `MarshalFromSwift` reinterpreting an obj ptr as struct bytes); P1-20 frozen-with-ref closure-arg buffer/ARC leak; **Cluster 1 trampoline UCO FailFast guards** (lifecycle OnAppear/OnDisappear, VoidClosure, TypedClosure `:3468`, ResultBranch success/error, AsyncPattern OnResult); P1-22 identifier-collision guard at `SwiftUIBridgeEmitter.cs:2776`. **Reviewer absorbs (same-shape, in S5-owned files):** (a) Codex High — typed-closure ObjC-bridgeable `BoundStruct` arg (the P1-19 fix-shape applied to the typed `(URL)->Void` closure path: Swift `objcArgIndices` + `as AnyObject` + `withExtendedLifetime` + `passUnretained`; C# `GetNSObject<T>` decode), exercised by `UrlClosureView`; (b) Grok High — 3 missed UCO FailFast guards (`FreeGCHandles`, async `OnReadyTrampoline`, `OnErrorTrampoline`). New BindingTests: `AuditSession5Views.swift` + `AuditSession5BridgeTests.cs` (12 methods incl. `UrlClosureView`/`UrlResultView`/`FrozenRefClosureView`); RED→GREEN proven (revert C# decode to `MarshalFromSwift` → SIGSEGV 9 pass/4 crash; restore → 13/0/0). **Codex+Grok r1/r2 paired review** (Codex `Session` r2 / Grok `sessionId 20260604-030529`). r2 = no actionable High (both reviewers confirmed the two re-examined changes correct + parity to Result/ObjC paths + all 3 guards present); dispositioned: Codex Medium (async `CreateAsync` IntPtr surface) → §6, Grok M1 (emitter unit assert for typed-objc literals) → added `ClosureWithObjCBridgeableStructArg_UsesGetNSObjectAndPassUnretained`, M2 (doc summary missing `UrlClosureView`) → fixed, M3 (typed `BoundType`+IsObjC parity) → §6, M4 (guard formatting) → no action (by design). Residuals → §6 (async public-IntPtr surface + typed `BoundType`+IsObjC parity, both pre-existing/unexercised, fix-shape logged). |
| 6 | Generics / specialization | ✅ done | 4 / 4 + P1-22 guard (CSM/AsyncGenericParent) + synthetic-name collision class (grep-sweep discovery, whole-category fix) | `nuke test` (Unit: 12402 pass / 0 fail; Analyzers: 21/0; Runtime-lib: 592/0) + `binding-tests --compile-only` (strict/fail-closed; 0 CS0100/CS0136) + sim (2655 pass / 0 fail / 0 crash) + device/NativeAOT (2675 pass / 0 fail / 0 crash) + `nuke validate` (130/130 cs_compile; GRDB `swift_compile` restored to **ok** — confirms the setter-escape regression fix; no library regressed) | P0-11 class-conformer carrier-wrap UAF (read the class conformer via `*(IntPtr*)resultPtr`, porting the CSM emitter's ownership discrimination to the bridge); P0-12 `MethodGenericBridge` fixed-256B buffer overflow + double-free (size via `GetSwiftTypeSize<T>()`; discriminate ownership before free); P1-28 frozen-with-ref variant ARC leak; P2 `BoundGenericsHandler` nested bound-generic leaf-arg drop (CS0305). P1-22 identifier guard applied at the S6-owned sites (`ConcreteProtocolSpecializationEmitter.cs:1585` / `AsyncGenericParent.cs:895`). New BindingTests: `CsmClassConformerCarrier.swift`/`CsmClassConformerReturnTests.cs`, `MethodGenericBridgeReturn.swift`. **Grep-sweep discovery (whole-category fix, no cascade):** the P1-22 sweep surfaced the *user-param == injected-synthetic-PARAM* collision class — THREE emission mechanisms, fixed in one pass: (#2, highest severity, SILENT) Swift `@_cdecl`/`@_silgen_name` label collision → `NameProvider.EscapeReservedSwiftWrapperLabel` (set `ReservedSwiftWrapperParamNames`) wired through the shared `CdeclParamMapper.Map` chokepoint via a `escapeReservedCollision` opt-out flag (false ONLY where the synthetic IS the param the body references by bare name — the setter `newValue` value-param sites in `PropertyWrapperEmitter`/`SubscriptWrapperEmitter`/`OptionalPointerWrapperEmitter`); (#1, CS0100) C# P/Invoke decl duplicate → idempotent `PInvokeEmitHelper.DeduplicateCSharpParamNames` at the universal `FormatDeclarationLines` chokepoint (positional-safe: C# P/Invoke names are positional-only), plus the four raw-`WriteLine` enum-decl sites in `EnumHandler.SimpleEnum.cs` + `ConformerKeyPathInitFactoryEmitter.cs:398` that bypass the chokepoint; (#3, CS0136) C# body-local shadow → `NameProvider.SyntheticLocalNames.Resolve(MethodDecl)`. New BindingTests: `SyntheticNameCollisions.swift` + 10 `CollisionTests` (self_/resultPtr/errorOut/newValue/tag/_resultBuf across instance/init/async/throwing/enum/default paths) + 3 cross-module `CrossModuleTests` (struct-receiver `offset(self_:)`, class-receiver `tagWithSelf(self_:)`, async trampoline `computeAsyncWithSelf(self_:)`); all +13 pass on BOTH sim and device. **GRDB `nuke validate` regression (caught + fixed this session):** the value-param `newValue` carve-out originally covered only `PropertyWrapperEmitter`/`SubscriptWrapperEmitter`; a non-frozen value-type struct subscript with an optional-existential element (`subscript -> (any P)?`) routes through `OptionalPointerWrapperEmitter`, whose setter value param is ALSO the synthetic `newValue` referenced by bare name in the body — the reserved-name escape renamed the DECLARATION to `__newValue` while the body kept bare `newValue`, swiftc rejected the wrapper, the build SILENTLY stripped it, and GRDB's `PersistenceContainer.subscript -> (any DatabaseValueConvertible)?` setter went missing (validate `swift_compile`=err). Fixed by extending the `escapeReservedCollision:false` exemption to `OptionalPointerWrapperEmitter`'s `isSetter && argIndex==0` value param (lines 146–150, 180–185). **Durable fixture** (CLAUDE.md "reproduce the validate Swift pattern in BindingTests" + TDD-for-regression-fixes): `Properties/Subscripts.swift` `ItemBag` (value struct, `subscript(String) -> (any StoredItem)?`) + 3 `SubscriptTests` — setter round-trip (the direct GRDB regression catch), getter round-trip (also catches the §6 optional-existential return-marshalling bug), set-nil delete; all 3 pass sim + device. **Codex+Grok r1 end-of-task paired review:** Grok (sessionId `019e950b-92a2-7ee0-9ba0-19982ed9b891`) found no Highs — every S6-scope path (CSM carrier, MGB return, frozen-ref ARC, P2 nested generics, the reserved-name escape + carve-outs, the `:519` getter block) independently confirmed sound. Codex (`Session: 019e950b-71ad-7ea0-b083-a7fa06db9925`) surfaced one P1 — a PRE-EXISTING, different-shape blittable-optional/`omitLabels:true` silent-strip in the `useCdecl` branch of the closure-cdecl + optional-pointer wrappers — CONFIRMED reachable by code-path trace and §6-logged per §1.4 (different shape ⇒ not absorbed mid-session; fix touches the shared `CdeclParamMapper` contract). No verifying re-review round needed (no S6-scope fix to verify). See §6 for the category disposition (what S6 resolved vs. what the S2 synthetic-LOCAL bullet leaves for S7), plus the three §6 entries surfaced this session (RESOLVED optional-existential getter return-marshalling; OPEN EveryProtocol method-name collision; RESOLVED blittable-optional `omitLabels` — hardened defensively + reclassified LATENT, the buggy fallback branch is preempted by MethodWrapperEmitter and unreachable by any compilable shape). |
| 7 | Name/key consistency | ✅ done | 2 / 2 + reviewer/regression absorbs | `nuke test` (Unit: 12416 pass / 0 fail / 1 skip; Analyzers: 21/0; Runtime-lib: 592/0) + `binding-tests --compile-only` (clean) + sim (2703 pass / 0 fail / 53 skip / 0 crash) + device/NativeAOT (2723 pass / 0 fail / 33 skip / 0 crash) | **P1-21 (emitted-C#-name vs dedup/override-key divergence):** the authoritative emitted name (`MethodEnvironment.CSharpMethodName`, via `GetPublicMethodName`) folds in a property-collision rename (`Foo`→`FooMethod`), a numeric `CollisionIndex` suffix, AND a derived-class adopted ancestor-slot name — none of which the dedup keys or the same-module override verifier independently recomputed. Threaded the sibling-`propertyNames` set through all THREE projected-key builders so they stay structurally identical (`IHandler.GetProjectedCSharpMethodKey`, `ProtocolSignatureHelper.GetProjectedCSharpMethodKey`, `DefaultParameterOverloadEmitter.GetProjectedOverloadKey`), and fixed the same-module override path for B15 collision-suffixed base overloads (`TryResolveAdoptedOverrideName` resolves the precise overridden slot by full Swift selector + adopts its `EmittedCSharpName`; `PreReserveAdoptedOverrideNames` reserves the adopted key BEFORE the main loop so an unrelated natural sibling projecting to the same name disambiguates regardless of declaration order). **Pre-reserve hardening (this session, two defects):** (a) the pre-pass tombstone-view key must match the main loop's `object?`-collapsed view (`treatAsClosureTombstone` predicted via the new `ClassifyOverridePrePassEmission`) or a closure-tombstone override's reservation misses the collision; (b) the local key-count multiset must be built over the EMITTING partition only — a validation-skipped non-tombstone sibling sharing the key (e.g. one skipped solely for an unsupported RETURN, which the return-insensitive key still matches) was inflating the count past 1 and suppressing a valid `count==1` pre-reservation → CS0111. **Sibling-method dispatch (54eb3fac / Kingfisher CS1061 regression):** decoupled the Swift owner/peer witness grouping (`GetSwiftMethodFullSignature` default — OMITS `async`, so a sync witness satisfies the async requirement and the two share one owner + an empty-extension peer, avoiding "invalid redeclaration") from the C# sibling-fallback grouping (`includeAsyncEffect:true` — CARRIES `async`, so `Foo`/`FooAsync` stay distinct members and the receiver never emits a sync call against an async-only interface → no CS1061); mixed async/sync groups fan out sync-first so dispatch hits the ABI-compatible sync vtable, async branch trailing. **P1-22 (reserved-identifier guard) rollout:** the §6 synthetic-LOCAL category — `EscapeReservedSwiftWrapperLabel` (proven S6 pattern) applied to the two extension emitters S6's user-param-escape rollout missed (`ProtocolExtensionEmitter` + the sibling the audit overlooked, caught by the Grok plan-check, `ForeignTypeExtensionEmitter`); every other emitter in the §6 list confirmed already-protected (no cascade, per the no-cascade rule). **New BindingTests:** `SiblingMethodDispatch.swift` (owner/peer same-signature, name-divergence with method/property collision, async/sync refinement, unrelated async/sync MixedFan) + `SiblingMethodDispatchTests.cs`; `BlittableOptionalCdeclWrappers.swift`; reserved-label protocol/foreign-type-extension fixtures — all pass sim + device. **New unit tests:** `ClassifyOverridePrePassEmission_ValidationSkippedSibling_ExcludedFromEmittingPartition` (emitting-partition exclusion), `GetProjectedCSharpMethodKey_PrePassTombstoneView_MatchesMainLoopFlagView` (pre-pass/main-loop key agreement), WasEmitted-count guard. **Codex+Grok r1–r4 paired review** (Codex `Session 019e986e-4cd4-79f2-9bbc-ddb1a4e12d5a`; Grok `sessionId 019e986e-6fc5-7f72-a944-4d32947e734e`). The r2 boxing-**High** (claim: `EmitMethodFanOutBody` boxing `self` as `branches[0]` reintroduces a sibling-conformance cycle → wrapper COMPILE failure) was **EMPIRICALLY REFUTED** across r3/r4 and **WITHDRAWN**: a temp-red build with the sync-peer box compiled for ALL groups, and the C# receiver reads only word 0 (the class reference) of the existential container — never the witness table — while EveryProtocol unconditionally conforms to every same-module sibling (empty peer extensions borrow the owner's witness), so the box protocol type is behaviorally IMMATERIAL on both the compile and runtime axes. Owner-box retained as a clarity/robustness INVARIANT (not a correctness gate); all stale "mid-typecheck conformance cycle" comments (method fan-out + property getter/setter + subscript getter/setter) corrected, and the `MethodEmissionPlan.Siblings` "owner-first" XML doc fixed to sync-first. The r3 **NEW High** — `SwiftSourceStripper` `PreservedProtocols` omitted the new `MixedFanAsyncOwner`/`MixedFanSyncPeer`, so `StripFile` would remove their `extension EveryProtocol:` blocks + `Get_EveryProtocol_MixedFanSyncPeer_WitnessTable` getter in lockstep → runtime `EntryPointNotFoundException` on the stripped path — was **FIXED** (both preserved, matching the refine-pair precedent) and **verified** by `TestMixedFanUnrelated_SyncDispatch` passing on BOTH sim and device. **r4 verifying re-review: Codex 0 findings; Grok no High / no Medium** (only by-design Lows — the dual Swift-vs-C# grouping key, the classifier-only unit-test scope covered end-to-end by Scenario E, and a `constraints.md` `WillBeClosureTombstone` doc-drift which was fixed). §6 residuals unchanged (intentional dual-key grouping; full `PreReserveAdoptedOverrideNames` integration covered E2E by Scenario E `CollisionOverrideDerivedSiblingFirst`). |
| 8 | Input fidelity | ✅ done | 3 / 3 | `nuke validate` (baseline updated in-commit `8f92f6c5`) | **Landed in `8f92f6c5`** (the commit immediately preceding Session 9). **P1-26 (classification drift):** brought the no-XML `AppleFrameworkRegistry` path into parity with the XML databases — NaturalLanguage `NLLanguage`/`NLTagScheme` resolve to `Foundation.NSString` on both paths — plus the objcPrefix / rawValueType / NSString-typedef-as-blittable / Date-tuple / forced-ObjC-class fixes across `apple-frameworks.json` + `AVFoundation`/`NaturalLanguage`/`QuartzCore`/`UIKit` `*Database.xml` + `AppleFrameworkRegistry.cs`. **P1-27 (parser/demangler):** tightened `SwiftInterfaceAccessParser`, `GenericSignatureParser`, and the Swift5 demangler/reducer (`Swift5Demangler`/`Swift5Reducer`/`SwiftABIParser`) against the typed-throws last-match, `@Sendable`/`YK`, `where …: AnyObject` decl-drop, paren-in-string EOF, and protocol-requirement misclassification inputs the audit surfaced; plus a shared constructor-admissibility helper (`StringEmitter/ConstructorAdmissibility.cs`) refusing a dropped concrete same-type pin (`where RowDecoder == ()`) identically from the open-erasure and CSM constructor paths. **Classifier theories (was zero coverage):** `AppleFrameworkTypeTests`, `AppleFrameworkRegistryTests`, `TypeDatabaseExtensionsTests`, `SwiftInterfaceAccessParserTests`, `GenericSignatureParserTests`, `SwiftInterfaceTypedThrowsTests`, `FunctionAnnotationDemanglingTests`, `TupleDateElementEmitterTests`, `MarshallingHelpersTests`, `ConstructorAdmissibility` unit + BindingTests fixtures. _(Tracker row reconciled to the committed reality after the fact — it was never flipped when the work landed; the Session-9 pass only corrected the doc, it did not re-run Session 8's gates.)_ |
| 9 | Packaging / SDK + co-gater | ✅ done | 4 / 4 | `nuke test` (Unit: 12503 pass / 0 fail) + `binding-tests --compile-only` (clean) + sim (`--skip-regen` 2707 pass / 0 fail / 0 crash) + `nuke X64SimGate` (PASS — iossimulator-x64 + tvossimulator-x64 packaging round-trip green for third-party + Apple-framework bindings, sim-first and device-first; fat fold lands `ios-arm64_x86_64-simulator`/`tvos-arm64_x86_64-simulator`) + `nuke validate` (130/130, no regressions; baseline → `8f92f6c5`) | **P0-14 (co-gater blind to `[DllImport]`+`static extern`):** broadened `CSharpWrapperCoGater.IsPInvokeSignatureLine` to recognize the `static extern` P/Invoke shape alongside `static partial`, threaded through all three scanners (`FindPartialDeclaration`/`FindAmbiguousMethodNames`/`FindCollidingPreStrippedNames`); **review-driven tightening** — also require ` static ` + `(` so the *contextual* keyword `partial` used as an identifier in a surviving caller body (`bool partial = PInvoke_X();`) can no longer inflate decl counts and suppress a real strip (`extern` is reserved, can't be an identifier — the asymmetry the original predicate missed). **P1-23 (bridge xcframework arm64-only → DllNotFound on x64-sim/Rosetta):** three coupled parts — (a) thread `targetArchitectures`/`--target-architectures` through the bridge compile *exactly like the wrapper* (`Program.cs RunCompileBridgeOnly` arch fan-out via `CompileWrapperForArchitectures`; `SwiftWrapperCompiler.CompileBridge`/`CompileBridgeAll` stamp each slice via `SliceVariant.WithArchitecture` — rewrites target triple AND slice-id); (b) `Sdk.targets _CompileSwiftUIBridge` injects `--target-architectures $(SwiftTargetArchitectures)`; (c) `BindingsGeneratorCommand` direct-mode stops hardcoding `HasBridgeXCFramework=false` (detects `directHasBridgeSwift` post-inline-compile, passes to both `BindingProjectEmitter` + `ConsumerTargetsEmitter`). **P1-24 (Sdk.targets static slice-id default → false SWIFTBIND031 on an x86_64-primary sim slice):** generalized the disk-glob resync — renamed `_ResyncAppleFrameworkWrapperSliceIds` → `_ResyncWrapperSliceIds`, un-gated from AppleFramework-only to BOTH binding modes (wrapper-module-only condition + `Exists(xcframework)`), globs the real on-disk slice dir by `*-simulator`/`!*-simulator`, `AfterTargets _CompileSwiftWrapper` / `BeforeTargets _ValidateSwiftBindingPackSlices`. **P1-25 (non-transactional fat-fold → denied slice/DllNotFound):** `WrapperXCFrameworkMerger.MergeFatSlices` builds into a `.merge-staging` dir and commits via a two-rename atomic swap (`primary→.superseded`, `staging→primary`, restore-on-failure `catch`), PLUS cross-run recovery at method entry (heal-or-clear a `.superseded` left by a kill between the two renames — a single same-volume `rename(2)` recovers the original whole). **New tests:** co-gater `Process_PartialKeywordIdentifierInCaller_DoesNotSuppressStrip` + `Process_InternalStaticUnsafePartial_StrippedSymbol_RemovesDeclaration` (atop the 6 prior DllImport-shape tests); SdkTargets content-assertion `_ResyncWrapperSliceIds_TargetExists`/`_NotAppleFrameworkGated`/`_RunsAfterWrapperCompileBeforePackValidation`/`_GlobsBySimulatorSuffix` + `Targets_CompileBridge_ThreadsTargetArchitectures`; merger `MergeFatSlices_RecoversFromInterruptedPriorCommit` (macOS-only). X64SimGate is the durable end-to-end pack gate. **Codex+Grok r1→r2 paired review** (Codex `Session 019e9a87-1f86-79c1-bee9-91365bc9425c`; Grok `sessionId 019e9a87-42e9-7882-95a1-025169649334`). r1 acted on: merger cross-run recovery + co-gater predicate tightening (above). r1 declined w/ reasoning: resync scalar-multiplicity (single-slice-per-suffix layout → unrealizable), direct-mode bridge degrade-not-drop (by-design per `constraints.md` wrapper-degrade contract — `HasBridgeXCFramework=true` truthful because an arm64 bridge xcframework exists, consumer `NativeReference` carries `Exists()` guard), CopyDirectory exec-bit fidelity (flat locally-built bundles, dylibs load without +x). **r2 = no new actionable High:** Codex 0 High/0 Medium (1 Low = the same direct-mode fat-bridge degrade-not-drop, by-design); Grok's 2 residual "Highs" both refuted — recovery `Directory.Move` is a single same-volume atomic `rename(2)` (not a torn tree), and the line-based predicate's false-negative surface is empirically closed (all 80 emitted P/Invoke shapes carry `static partial` (72) / `static extern` (8) single-line signatures → behavior-preserving, no under-strip). Loop terminated at r2 (no new High to verify). |
| 10 | Gate-hygiene capstone | ✅ done | 3 / 3 + macOS/Catalyst gate fix + 2 P2s + meta-test invariant | `nuke test` (Unit: 12509 pass / 0 fail / 1 skip; Analyzers: 21/0; Runtime-lib: 592/0) + sim (2723 pass / 0 fail / 36 skip / 0 crash) + macOS/CoreCLR (2161 pass / 0 fail / 31 skip / 0 crash) + device/NativeAOT (2735 pass / 0 fail / 24 skip / 0 crash) | **P0-15 (live wrong-ABI `AsyncGenericContainer<T>` async method behind a false-reason `[Skip]`):** suppressed at its source — `MemberValidationPipeline.ValidateMethodEmission` gate (`:274`) skips `IsAsync && ParentDecl is TypeDecl { IsGeneric: true } && !HasMethodOwnGenericParameters` as `GenericTypeCallback` (the wrapper would need the parent's type metadata + `self` in Swift's implicit registers, which a C# `CallConvSwift` P/Invoke passes as trailing `IntPtr` → SIGSEGV); companion "suppressed at source (P0-15)" comments threaded into `MethodHandler.cs` + `WrapperValidation.cs`. The prior `[Skip]`'d async-container tests are replaced by the **sync generic-static-dispatch surface** that is genuinely emit-able and ABI-safe — `AsyncGenericTupleTests` `StoredValue` round-trips (ctor + getter/setter), which run + pass on all platforms. Unit coverage: `MemberValidationPipelineTests` (plain-`Int32`-return + throwing-async skip on an unconstrained generic parent) + `AbiSafetyTests` pinning the `IsSkippedWrapperDirectPInvoke` scoping across all three shapes (no-method-own parent-generic → false; method-level generic → true; **combined parent-generic + method-own generic → true**, the reviewer-driven addition below). **Purge of false "Issue 1"/"!ji->async" skips on pure-Cdecl paths:** un-skipped throwing-closure success / delegate graceful-fault tests (`ClosureEdgeCaseTests`, `OptionalThrowingVoidClosureTests`, `ThrowingMethodTests`) — each now carries a comment proving the path is **pure `CallConvCdecl` end to end** (entry P/Invoke is an `SBW_` `@_cdecl` wrapper, managed callback is `[UnmanagedCallersOnly(CallConvCdecl)]`), so the Mono `!ji->async` signal-unwind assertion (which requires a `CallConvSwift` frame on the path) cannot apply; the prior blanket `[SkipOnSimulator]` cited Issue 1 on a path with no `CallConvSwift` P/Invoke. **Un-skip of Sessions 2/3/4 un-gated tests:** failable `TryCreate` inits (`InitializerTests` SafeDiv / NonEmptyString / RangedInt, cross-referenced in `ConstructorParamTests` / `OptionalMarshallingTests`). **Meta-test invariant (`Issue1SkipAttributionTests`):** walks every `[Skip]` whose reason cites "Issue 1" / "ji->async", extracts the `$s…` symbol from the reason + adjacent comment, and **hard-requires** it to be (a) a real `CallConvSwift` entry point in the on-disk generated `SwiftBindingsTestLib.cs` AND (b) referenced in the annotated test's body — so any future false attribution (a Cdecl-path crash mislabelled "upstream") is a RED unit test, restoring the `feedback_mono_jit_blame.md` trust contract; asserts `issue1SkipCount > 0`. **macOS/Catalyst gate fix (the third-platform defect):** the harness re-uses `--platform simulator` for Simulator, macOS, AND Catalyst, so a CLI-flag-keyed `[SkipOnSimulator]` would have over-suppressed CoreCLR macOS runs — and `IncludeSwiftBindingsRuntimeNative=false` means `libSwiftBindingsRuntime.dylib` is absent on Simulator + macOS + Catalyst but present on device. Introduced the **runtime-detected** `[SkipOnMonoJit]` (`TestResults.cs` / `TestDescriptors.cs` / `TestBase.cs`), keyed on `IsMonoJitRuntime` = `SwiftRuntimeInfo.IsMonoRuntime` OR `OperatingSystem.IsMacCatalyst()` (the disjunction augments the `IsMonoRuntime` "simulator"-RID detection which misses `maccatalyst-*`), honored per-method in `RunAllTestsAsync` — distinct from `[SkipOnSimulator]`/`[SkipOnDevice]` (which `TestDiscoveryGenerator` reads at class scope). Proven correct: `[SkipOnMonoJit]` tests RUN+PASS on macOS (CoreCLR) and device (NativeAOT), SKIP on Mono (Simulator + Catalyst); the dylib-absent group still skips on macOS. **P2 `coverage-report.py` runtime-coverage gap:** `get_runtime_tested_names()` whole-tree identifier scan + `runtime_tested` heuristic + `passing_untested` summary field + a `*** WARNING ***` block surfacing must-pass features with a passing binding but no runtime test. **P2 `TestDiscoveryGenerator` async-void detached:** new `SBTD001` Roslyn incremental-generator diagnostic (Error severity) that fails the build on a discovered `async void Test*` method (the discovery invoker can't await it, so post-await assertions detach and the test falsely passes) — registered via `AnalyzerReleases.{Shipped,Unshipped}.md` + csproj `AdditionalFiles` (RS2008); plus a `nuke test`-layer source-grep guard (`AsyncVoidTestMethodTests`) and the `AsyncComplexTypeTests` `async void`→sync `void` conversion. **Codex+Grok r1/r2 paired review** (Codex `Session 019e9b00-33c7-7301-883b-77ec536f2514`; Grok `sessionId 019e9b00-505d-7850-9c0f-db9dd381608c`). **r1** — Codex **High**: the combined async + generic-parent + method-own-generic shape (`Box<T>.load<U>(…) async`) falls THROUGH P0-15 (whose predicate excludes method-own generics) → investigated (Explore) and **proven not a live defect**: it is suppressed at `IsSkippedWrapperDirectPInvoke` (`:1704`, the method-own-generics check) because the async-generic bridge bails on a generic parent (`AsyncMethodGenericBridgeEmitter:119`) so no `@_silgen_name` wrapper is emitted (`UsesWrapperLibrary` stays false, the `:1689` early-return doesn't fire) — the exact combination was UNTESTED, so added the durable `AbiSafetyTests.IsSkippedWrapperDirectPInvoke_AsyncOnGenericParentWithMethodOwnGeneric_ReturnsTrue` guard pinning the suppression. Codex **Medium**: `[SkipOnMonoJit]` advertised `Method`-or-`Class` `AttributeUsage` but class scope is silently ignored (only `Skip`/`SkipOnSimulator`/`SkipOnDevice` are read at class scope) → restricted **both** runtime-detected attributes (`[SkipOnMonoJit]` + `[SkipOnCatalystX64]`) to `AttributeTargets.Method` so a class-level annotation is now a compile error not a silent no-op (grep-verified zero existing class-level usage). Codex **Note** + Grok **Low**: untracked `build/scripts/__pycache__/*.pyc` → `.gitignore` `__pycache__/` + `*.pyc`. Grok r1: no new actionable High (observational confirmations of the runtime-detection composite, SBTD001, P0-15 scoping, the un-skips, the coverage heuristic). **r2 verifying re-review: Codex 0 High / 0 Medium / 0 Low** (both prior findings confirmed closed); **Grok no new High**, one doc-drift **Medium** — the pre-existing `…NoMethodOwnGeneric_ReturnsFalse` test comment + the `WrapperValidation.cs:1701` predicate comment claimed the no-method-own parent-generic async shape "continues to flow through the legacy direct path", which is stale post-P0-15 (that shape is suppressed UPSTREAM, never reaching the direct path) → **FIXED in place** (both comments corrected to describe the post-P0-15 layering: the predicate scopes to method-own generics; the bare parent-generic case is owned upstream by P0-15 or CSM routing — no behavior change). Loop terminated at r2 (no new High to verify). |

---

## 6. Discovered / out-of-scope (logged, NOT queued)

> New confirmed bugs found mid-session that are **not** same-file-same-shape go here as one line each. This list is triaged **after** the ten sessions land — as a deliberate decision, never by spawning a session mid-stream. Keeping items here instead of in the queue is what holds the plan at ten.

- **[RESOLVED — MERGED into this branch (#40); do not re-find/re-fix]** Protocol-proxy *reverse-callback* marshalled a concrete **Swift-class** / `Optional<class>` receiver param with a naive `Unsafe.Read<T>` (per-proxy helper `ProtocolProxyEmitter.SwiftObject.cs`; 4 receiver sites in `…Receivers.cs` — method, property-setter `marshalExpr`, subscript getter, subscript setter) → reinterpreted a Swift heap-object pointer as a managed ref → **SIGSEGV** on first use. **Not in the audit backlog** (our `Receivers.cs` findings P1-04/P1-08 are existential, not concrete-class — a recall miss). **Fixed and merged** on `fix/protocol-proxy-class-param-receiver` (issue #40, commit `8fe86baf`): `GetReceiverClassCopyOutExpr` (`Receivers.cs:1377`) now routes `ClassProjection`/`ObjCRootedClassProjection`/`Optional<class>` through `SwiftMarshal.MarshalBorrowedClassFromSlot`/`MarshalBorrowedOptionalClassFromSlot` at all receiver sites, with the runtime ARC half (P1-01) closed in the same PR. Durable gate: `ClassParamCallback.swift`/`ClassParamCallbackTests.cs` + `ProtocolProxyEmitterTests`. **This is the marshalling fix only — the orthogonal UCO FailFast guard on those same receivers remains S4's** (next bullet, groups (a)/(b)).
- **[OPEN — surfaced by Session 2's GenericClosureBridge collision fixtures; pre-existing, different shape from S2's UCO/synthetic-name cluster]** GenericClosureBridge **self-register ABI mismatch** (affects *every* method-generic closure method; `DatabaseReader.read*` in BindingTests). The generated C# P/Invoke is `[UnmanagedCallConv(CallConvSwift)]` with a `SwiftSelf self_` parameter, and the call site passes `new SwiftSelf((void*)_handle.DangerousGetHandle())` — placing the receiver in the Swift self/context register. But the Swift `@_silgen_name` wrapper (`PInvoke_read_…_XC` / `…_XC_void`) is a **free function** that declares the receiver as a *regular trailing parameter* (`_ _self: UnsafeMutableRawPointer`, renamed `___self` under a P1-22 collision) and reads it from the next ordinary GPR — which the C# side never sets, because `SwiftSelf` consumed the dedicated self register instead. → garbage `self` → SIGABRT in the mono GC-safe-region transition (`mono_threads_exit_gc_safe_region_unbalanced` / `…transition_done_blocking`) during the call (void path), or SIGSEGV (return path), on **every** GenericClosureBridge runtime invocation. Reproduces on plain `read` (which has *no* synthetic-name collision), so it predates and is independent of S2's synthetic-name guard — the guard only renames identifiers, it does not choose the self-passing convention. Fix = make C# and Swift agree on `self`: either pass `_self` as a regular `IntPtr` param in the GPR sequence (drop `SwiftSelf`; keep `out SwiftError` for the `throws` error register) or make the wrapper `@convention(method)`; verify the register layout via SIL (`feedback_verify_swift_abi_sil`) and on `--device` → not same-shape as S2, hence §6. Only `DatabaseReader.read*` exercise this path and had **no** prior runtime coverage (no regression). This is the **first** blocker on the path; the class-typed-return defect below is a second, independent one. In-repo markers: `GenericClosureBridgeTests.TestGenericRead_RoundTrips` / `…CdeclParamCollision_RoundTrips` / `…SelfParamCollision_RoundTrips` (`[Skip]`, unskip when this lands). Emitter sites: `GenericClosureBridgeEmitter.cs` C# P/Invoke param list (`SwiftSelf self_`) + call-site (`new SwiftSelf(...)`). The P1-22 synthetic-name guard on this same path *is* proven, independent of this defect, by `GenericClosureBridgeEmitterTests.TryEmit_UserParamNamedCdecl_*` / `…UnderscoreSelf_*` + the compile gate.
- **[OPEN — surfaced by Session 2's GenericClosureBridge collision fixtures, different shape from S2's UCO/synthetic-name cluster]** GenericClosureBridge **generic class-typed return** (`Read<T>(Func<…,T>, …) where T : ISwiftObject`, `DatabaseReader.read*` in BindingTests) — the *second* of two independent runtime defects on this path (the self-register ABI defect above blocks the call before this return-marshal even runs). The C# emission writes the closure result into a caller-owned `resultBuf` via `MarshalToSwift` (an `InitializeWithCopy` storing the object pointer *inside* the buffer), then reads it back with `MarshalFromSwift<T>(new IntPtr(resultBuf))`. For a class `T`, `MarshalFromSwiftCore` hands its argument straight to `NewFromPayload` **as** the object pointer (`SwiftMarshal.cs:854`), so the wrapper's handle becomes the `resultBuf` *address*, which the method's `finally` (`NativeMemory.AlignedFree`) then frees → **SIGSEGV on first member access** (`result.Name`). Correct read must dereference the buffer for a class and copy for value-type/existential/SwiftString — the internal `MarshalMovedValueFromSlot<T>` (`SwiftMarshal.cs:259`) already does exactly this, but it is `internal`; the public helpers (`MarshalFromSwift`/`MarshalFromSwiftObject`) do **not** dereference. Fix = expose a buffer-aware read to generated code (new public runtime surface) + cross-shape test matrix → not same-shape as S2, hence §6. Only `DatabaseReader.read*` exercise this path today and had **no** prior runtime coverage (no regression). In-repo marker: `GenericClosureBridgeTests.TestGenericRead_ClassReturn_SkipPendingBufferDerefFix` (`[Skip]`, unskip when **both** §6 defects land). Generated emitter site: `GenericClosureBridgeEmitter.cs` return-marshal expression.
- **[RESOLVED (S7) — broader theme of S2's synthetic-identifier fix; same shape, untouched emission mechanisms]** **Hardcoded synthetic identifiers emitted without the P1-22 collision guard, across emitters Session 2 did not touch.** Session 2 went past *guarding* the one async-cleanup mechanism it owned and **structurally eliminated** its entire synthetic-identifier surface: the holder-cleanup **loop index** (`for (int i = 1; …)`) and every `is`-pattern variable (`retained` / `deferred` / `copyBuffer` / `existentialHeap` / `__deferredList` / `__d` / `cancelReg`) were moved out of the generated user-scoped bodies in `AsyncHarnessEmitter` / `WrapperEmitter.Async` / `WrapperEmitter.Return` / `AsyncMethodGenericBridgeEmitter` into the hand-written runtime helpers `SwiftAsyncCallHolder.Cleanup` / `CaptureCancellationToken` (`AsyncHelpers.cs`). Those identifiers no longer appear in generated code at all, so they **cannot** collide with a user parameter — a stronger guarantee than the `SyntheticNameScope` aliasing they briefly carried (the now-dead `__i` / `cleanupLoopVar` / `cleanupParamNames` reservations were removed with them; verified: zero `__i` / inline `is RetainedSelfPtr` left in those emitters). The async holder-cleanup loop-index (the original Codex r2 High/Medium#1) and pattern-var collision items are therefore **CLOSED**, not merely guarded. What **remains open** is the *same bare-literal shape in emitters S2 never touched*: the receiver pointer (`_self` / `__self` / `self_` in `PInvokeEmitter` and the `MethodGenericBridge*` family), the `cdecl` buffer local (`let cdecl …` / `_cdeclBuf` in `ProtocolExtensionEmitter` and peers), and — per Grok r2 — `ConcreteProtocolSpecialization*`, `OptionalPointerWrapper`, `CrossModuleExtension*`, `AsyncStream`, `Marker`, and `KeyPathBag`, each writing synthetic locals as bare string literals instead of reserving them through `SyntheticNameScope` / `NameProvider.MakeNonCollidingSyntheticName` against the per-emitter user-name set. These are **lower-reachability than the (now-eliminated) loop index** (the colliding user name must match a less-common identifier, and many sites are in nested scopes), which is exactly why they are §6 (logged, defense-in-depth) and not absorbed: per §1.4 + the no-cascade rule this whole category is the *same bug shape in 2+ untouched mechanisms* — it gets enumerated and fixed in **one** pass (spawn Explore to sweep every `csWriter`/`swiftWriter` literal that names a local, cross-check against the per-emitter user-name set, route each through the existing guard primitive), never as N follow-on sessions. The guard primitive already exists and is proven (`SyntheticNameScope.Reserve` + `MakeNonCollidingSyntheticName`, with the GenericClosureBridge `cdecl`/`__self` emitter tests as working precedent); the remaining work is mechanical application + a collision-fixture per newly-guarded site. Triage after the ten sessions land — most naturally folded into **Session 7 (Name/key consistency, C2)**, which already owns identifier hygiene, rather than as a standalone session. **S7 disposition (RESOLVED):** the §5/§7 prep sweep + a fresh execution-time regen-and-grep confirmed S6's comprehensive *user-param escape* rollout (`EscapeReservedSwiftWrapperLabel` across 36 wrapper-emitter sites) already neutralizes the *injected-synthetic-vs-user-param* collision for every emitter in this list **except two extension emitters** that build their user param list from a limited sanitize with no reserved-name escape — `ProtocolExtensionEmitter` and (the sibling the audit missed, caught by the Grok plan-check) `ForeignTypeExtensionEmitter`. S7 fixed **both** in one pass with the proven S6 escape pattern (route the user param names through `EscapeReservedSwiftWrapperLabel` against `ReservedSwiftWrapperParamNames`, leaving the `self_` receiver slot and the suppressed protocol-extension metadata untouched), with BindingTests fixtures for each (a protocol-extension param named/labeled to a reserved synthetic + a foreign-type-extension param doing the same — RED = silently-stripped wrapper / missing entry point, GREEN after). The other emitters in the list were confirmed already-protected (their user labels are escaped before the injected synthetic is added, or they use fixed synthetic-only signatures), so guarding them would be churn with no reachable defect — deliberately not done, per the no-cascade rule.
- **[SLOTTED — groups 1 & 2 PROMOTED to S4 committed scope (S4 13 remaining; P1-01 pre-closed via #40 merge); group 3 tracked under S5's `trampoline` scope. This entry is now their technical reference, NOT an open triage item. Surfaced by Session 2's r4 grep-sweep + Explore enumeration; SAME fix-shape as P0-01/P0-02 (UCO-escape), in emitters S2's enumerated scope never touched]** **Unguarded `[UnmanagedCallersOnly]` callbacks invoke a managed user delegate with no FailFast/TCS fault guard, across three emitters outside the P0-01/P0-02 enumerated sites.** P0-01/02 hardened every UCO callback in S2's *owned* closure/async bridges (`ClosureEmitter*`, `AsyncHarnessEmitter`, `AsyncMethodGenericBridgeEmitter`, `MethodClosureBridge`, `NestedClosureBridge`, `ProtocolExtensionClosureBridge`) so a throwing managed delegate routes through `ClosureEmitter.EmitNonThrowingFailFastCatch` (→ `SwiftClosureMarshaller.FailFastUnhandledClosureException`, for non-throwing closures with no error channel) or `TaskCompletionSource.TrySetException` (async/Task bridges). The **same defect shape** survives in three emitters S2's scope did not enumerate: the generated UCO body invokes user/managed code directly with **no surrounding try** — a throwing continuation unwinds out of the `[UnmanagedCallersOnly]` frame into native Swift → **SIGABRT** (process abort, no diagnostic). This is a *distinct* defect from the synthetic-identifier bullet above (that one is CS0136 name-collision; this one is a runtime escape), even where both touch the same emitter. All three confirmed this session by direct emitter-body reads (not just the sweep):
  1. **`CrossModuleExtensionEmitter.Class.cs` `EmitClosureCallback` (`:367–392`; verified still-bare post-merge)** — the completion-handler closure path emits `var __del = (…)__handle.Target!;` (`:389`) `__del(args);` (`:392`) bare. The sibling Task-bridge completion UCO in the *same file* (`:1105–1143`) IS guarded (`try { … __tcs.TrySetException/TrySetResult … } finally { free }`), so only the closure path is defective. The GCHandle here is owned by a Swift-ARC box (`_SBClosureCtx`) freed by a destroy callback, so the callback correctly does **not** free the handle — the only fix is wrapping `.Target!` + `__del(...)` in the FailFast catch (matching `MethodClosureBridge`'s `.Target!`-inside-try shape). Was unowned; → **PROMOTED to S4 committed scope**, consolidated with group 2 as S4's second fix-shape (same guard, one pass — S7's name/key theme is a poor fit for a runtime-escape fix). See the §Session-4 detail + §5 tracker (13 remaining).
  2. **`ProtocolProxyEmitter.Receivers.cs` — every receiver that invokes the user's `IProtocol` impl** (line refs against the *post-#40-merge* tree — re-confirm before editing): method `:1168`/`:1185` (`impl.{Method}(…)`), property getter `:198` (`impl.{Property}`), property setter `:277` (`impl.{Property} = value`), subscript get/set, closure-property thunk `:447` (`_del()`), closure-returning-method thunk `:492`/`:517` (`impl.{Method}()` + `_del()`), async closure receiver `:593` (`impl.{Method}(handler)`). The bodies guard only the `TryGetProxy`/`if (impl is null)` paths (returning defaults); the actual `impl.*`/`_del()` managed calls are bare — and the source comments (`:164`/`:263`/`:642`/`:970`) *explicitly acknowledge* "`[UnmanagedCallersOnly]` cannot throw / terminates the process" yet leave the user invocation unguarded. **Confirmed still-bare after the #40 merge** (that PR added only the class-param marshalling branch, not the fault guard). **Owned by S4** (§3: the existential +1/Destroy contract spans `ProtocolProxyEmitter.*`); the `fix/protocol-proxy-class-param-receiver` branch (#40) that edited this exact file has now **merged** (line numbers shifted — that is *precisely why S2 was kept off it*). → **PROMOTED to S4 committed scope** (S4 owns this file; harden in the same pass, building on the merged marshalling branch).
  3. **`SwiftUIBridgeEmitter*.cs` trampolines** invoking user `Action`/typed delegates bare: `Lifecycle.cs` OnAppear/OnDisappear (`~270/282`, `action();`), VoidClosure (`~2477`), TypedClosure (`~3468` — the exact site S5's scope already names — `action(args)` / `var result = func(args)`), ResultBranch success/error (`~3755+`, `action(str/obj/value)`), `AsyncPattern.cs` OnResult (`~1042`, `state.OnResult?.Invoke(...)`). **Owned by S5** (§3: `SwiftUIBridgeEmitter.* → S5`; "single 3,962-line file → all SwiftUI bugs land here regardless of cluster"). S5's scope lists only the `:3468` trampoline try/catch — **its grep-sweep must catch the lifecycle/Void/Result/AsyncPattern siblings too**, not just `:3468`. → **tracked under S5's existing committed `trampoline` scope** (already slotted in §2/§Session-5; this entry just widens the site list S5's sweep must cover).
  **Disposition** (per §1.4 + §3 single-owner map + the no-cascade rule; concurred by an independent Grok r4 scope review, sessionId `019e8b18-f914-75e1-85aa-5fd5c318eca4`, which read all three bodies and cited the same rules): this is the *same bug shape in ≥2 untouched emission mechanisms* — enumerated once here (the Explore sweep covered the whole emitter tree and classified GUARDED vs UNGUARDED; these three groups are the complete set it surfaced outside S2's owned bridges), to be fixed in **one pass per owning session**, never as N follow-on sessions and never absorbed mid-S2 (absorbing would expand S2's review surface and collide with the #40 branch). §1.2's "absorb 12 UCO callbacks" governs discovery *within* a session's grouped mechanisms; the file/shape gate (§1.4) + owner map control cross-emitter routing — the same call S2 already made for the synthetic-identifier category. The guard primitive (`EmitNonThrowingFailFastCatch` / TCS `TrySetException`) already exists and is proven by P0-01/02; the remaining work per group is mechanical application + a throwing-delegate emitter-output fixture per newly-guarded site (the runtime FailFast path terminates the process, so the durable assertion is at the emitter-output layer, as with the P0-01/02 coverage).
  **Resolution (owner-approved):** rather than leave these in post-Phase-1 triage, groups 1 & 2 were promoted into **S4's committed scope** (S4 already edits the ProtocolProxy file; CrossModuleExtension folds in as the same fix-shape, fixed in one consistent pass) and group 3 was confirmed already covered by **S5's `trampoline` scope** (widened to its sibling sites). All three are therefore fixed *during* the ten sessions, not deferred past them. The ten-session ceiling holds — scope was added to existing owners, no new session spawned.
- **[RESOLVED — root-cause fix (a) landed via handler-layer error-mint registration; (b) tracked as defense-in-depth below]** **Optional escaping *throwing* closure returning Void as a method/init parameter → generated C# references a `[UnmanagedCallersOnly]` callback method the co-gater has stripped → CS0103 "name does not exist."** Breaks two real-world libs at the compile gate: **Alamofire** (5×, `Session.upload(…requestModifier:)` — `requestModifier: ((URLRequest) throws -> Void)? = nil`) and **YouTubePlayerKit** (2×, `init(htmlProvider:)` + the `HtmlProvider` setter). The C# param projects as `global::System.Func<…, Swift.SwiftResult<Swift.SwiftVoid, SwiftError>>? = null` (Optional + escaping + throwing + Void-return). **Mechanism (evidence-backed, not inferred):** the throwing-closure callback *method* IS emitted — `ClosureEmitter.EmitThrowingClosureCallback` (`ClosureEmitter.Throwing.cs:20`, body at `:105–130`, invoked from `WrapperEmitter.Marshalling.cs:912`) — and its catch block calls the module error-mint P/Invoke `SBW_CreateError_{module}` (`ClosureEmitter.Throwing.cs:126`). That wrapper symbol was **"not registered by wrapper-emit"** (confirmed verbatim by the co-gater's own strip comment in the generated `Alamofire.cs`: *"P/Invoke removed because the Swift wrapper symbol was stripped during wrapper compilation (wrapper symbol 'SBW_CreateError_Alamofire' not registered by wrapper-emit)"*), so the `CSharpWrapperCoGater` removes the `SBW_CreateError_{module}` P/Invoke **and every method whose body references it** (`FindAndMarkCallers` / Step B) — which deletes the entire `[UnmanagedCallersOnly]` callback method. But the *dependent* one-line **static field** `s_<cb> = &<cb>` (`ClosureEmitter.Throwing.cs:154` via `WrapperEmitter.Marshalling.cs:911`) and the `new SwiftClosureData((IntPtr)s_<cb>, …)` **call-site** (`WrapperEmitter.Marshalling.cs:438`, inside the *surviving* `Upload` method body — that method's own P/Invoke is fine) are NOT block-stripped (`FindBlockEnd` only subsumes braced members), so both survive and dangle on the now-missing method → CS0103. Two coupled defects: **(a) upstream** — the `@_cdecl` error-mint helper `SBW_CreateError_{module}` is never registered by wrapper-emit for this module/path (the actual originating fault); **(b) co-gater asymmetry** — stripping a P/Invoke removes dependent *block* methods but leaves dependent *one-line* fields + call-sites in surviving bodies, turning a clean suppression into a dangling reference. **Attribution (proven, not assumed):** a clean-HEAD `f73a86f7` worktree regen produces byte-identical output (the orphaned field appears at the same locus referencing the same missing method; callback method-def set identical, 69 = 69 via `comm`) and the clean-HEAD C# fails to compile with the *same* 5 CS0103 errors — S3's ownership/`@_cdecl`/thunk/return-ABI changes are orthogonal to wrapper-symbol registration and the co-gater. The `.validation-baseline.json` is reverted to HEAD by S3's commit so it does **not** bake in this `compile: ok → fail` (Codex r2 + Grok r2 both flagged the baseline downgrade as High — the comparer treats `fail → fail` as a non-regression, so committing the failed entry would silence it); reverting keeps it **loud** — the next full `nuke validate` re-flags Alamofire + YouTubePlayerKit `ok → fail`. **Minimal repro shape:** a method or initializer taking an *optional* escaping throwing closure returning Void — `func f(cb: ((SomeType) throws -> Void)? = nil)` — in a module where the `SBW_CreateError_{module}` error-mint helper is not registered. **Disposition:** not same-shape as S3's ABI-core scope and spans the wrapper-symbol contract + co-gater stripping, not the closure emitter alone — routed to a dedicated closure/co-gater remediation session, triaged after the ten sessions land (per the no-cascade rule); in-repo gate when it's fixed = a BindingTests fixture with an optional `(T) throws -> Void` parameter that currently would compile-fail.
  **Resolution (root-cause fix a):** the C# side emits the `SBW_CreateError_{module}` P/Invoke for *every* synchronous throwing-closure parameter at a single chokepoint (`WrapperEmitter.Marshalling.cs` → `SwiftErrorMintEmitter.EmitPInvokeIfNeeded`), but the matching Swift `@_cdecl` helper was only *registered* by paths funneling through `ClosureEmitter.GetSwiftClosureAdapterCode` — so the native `_optbuf`/default-parameter/non-optional-property-setter forwarding paths emitted the P/Invoke against an unregistered symbol. Fixed by consolidating helper emission+registration to the **handler dispatch layer**, before any leaf Swift-wrapper emitter and before that decl's C# wrapper-symbol contract check: `SwiftErrorMintEmitter.EmitForMethodIfNeeded` (called from `MethodHandler.Emit` and `ConstructorHandler.Emit`) and `EmitForPropertyIfNeeded` (called from `PropertyHandler.Emit`). Each decl self-satisfies, so the fix is emission-order-independent and covers every present and future wrapper path for that decl kind in one place; idempotent per module via `ModuleEmissionContext.SwiftErrorMintHelperEmitted`. The inline `OptionalPointerWrapperEmitter` guard (a partial earlier attempt) was removed in favor of the handler-layer policy. Coverage: emitter-level unit tests `SwiftErrorMintEmitterTests` (policy + `MethodHandler.Emit` wiring fires before the contract check) and the BindingTests fixture `OptionalThrowingVoidClosures.swift` / `OptionalThrowingVoidClosureTests.cs` (free-function param, initializer param, default-parameter shim, and optional + non-optional settable properties). Verified: `nuke binding-tests --compile-only` green (zero CS0103, `SBW_CreateError` registered in both the C# P/Invoke set and the Swift `@_cdecl` wrapper, callback method present — not stripped), sim run 3 pass / 5 `[SkipOnSimulator]` / 0 fail / 0 crash, and `nuke validate --filter` flips Alamofire + YouTubePlayerKit back to `compile: ok`.
- **[RESOLVED — second, distinct defect surfaced by the fixture above once it compiled; box/unbox context-read ABI skew, NOT the co-gater/wrapper-symbol fault]** **A non-cdecl escaping *throwing* (or indirect-return) closure setter/param boxes the GCHandle context in an `_SBClosureCtx` but the throwing / indirect-return `[UnmanagedCallersOnly]` trampoline reads it RAW → the box pointer is misread as a `GCHandle` → `InvalidCastException` escapes the UCO boundary → SIGABRT on device/NativeAOT (any invocation — success *or* throw — of a C#-set Validator-shape closure).** This is a *separate* fault from the CS0103 compile regression above (which the error-mint fix resolved); it could only surface once that fix let the code compile and run on device. **Mechanism:** on the non-cdecl legacy `SwiftClosureData` path the setter boxes the GCHandle via `SwiftClosureMarshaller.TryAllocateBoxedContext` (the 0.11 leak fix — `legacyEscaping` in `WrapperEmitter.Marshalling.EmitClosureMarshalling`, so Swift's release of the closure deinits the box and frees the handle exactly once) and the `SwiftClosureData.context` word carries the box pointer with **no Swift-side unbox** (the trampoline is `delegate* unmanaged[Swift]<…, SwiftSelf, void>`, context in the Swift self register). The non-throwing escaping callback (`ClosureEmitter.EmitEscapingClosureCallback`) already chose `GetDelegateFromBoxedContext` vs `GetDelegateFromContext` via the gate `useBoxedContext = !useCdecl && WrapperValidation.IsEffectivelyEscaping(…)`, but `EmitThrowingClosureCallback` and `EmitIndirectReturnCallback` **hardcoded** `GetDelegateFromContext` (raw) — so on this path the box pointer was read as a GCHandle and the `(T)handle.Target!` cast threw, *outside* the try in the throwing callback, unwinding into native Swift. **How it surfaced (why sim missed it):** the YouTubePlayerKit `HtmlProvider`-shape non-optional throwing setter `validator: () throws -> Void`, when set from C#, takes the non-cdecl legacy boxing path; the crashing test is `OptionalThrowingVoidClosureTests.TestHolder_SetValidator_DelegateThrows_GracefulFault`. It is `[SkipOnSimulator]` (the throwing-closure callbacks carry `SwiftError*` → Mono JIT `!ji->async`, confirmed upstream Issue 1), so it runs **only on device/NativeAOT** — the sim gate that signed off the CS0103 fix never executed it. **Root cause = the gate was threaded into one of three sibling escaping-callback shapes and not the other two.** **Fix:** added a `bool useBoxedContext` parameter to `EmitThrowingClosureCallback` (`ClosureEmitter.Throwing.cs`) and `EmitIndirectReturnCallback` (`ClosureEmitter.IndirectReturn.cs`), each selecting `GetDelegateFromBoxedContext<T>` vs `GetDelegateFromContext<T>`; and — to kill the drift that *was* the defect — consolidated the gate to a **single** `useBoxedContext` computed once at the top of the `RequiresThunk` block in `WrapperEmitter.Marshalling.EmitClosureCallbacks` and passed unchanged to all three `Emit*Callback` calls (previously the expression was inlined three times; the throwing + indirect copies were simply absent). The cdecl path is unaffected (`useCdecl` ⇒ the Swift wrapper unboxes before calling the trampoline, which stays raw — e.g. the `init(validator:)` cdecl param callback correctly stays `unmanaged[Cdecl]<…, IntPtr, void>` + raw). **Coverage:** emitter unit tests in `ClosureEmitterDirectTests` assert boxed-vs-raw symmetry for the throwing + indirect-return shapes (legacy-escaping ⇒ `GetDelegateFromBoxedContext`; cdecl / default ⇒ raw `GetDelegateFromContext<`); the BindingTests fixture `OptionalThrowingVoidClosures.swift` / `OptionalThrowingVoidClosureTests.cs` exercises the non-optional throwing setter end-to-end. **Verified:** `nuke binding-tests --compile-only` green (regenerated output: `validator_Set_value_*_Callback` reads `GetDelegateFromBoxedContext`, matching the boxed setter; all 5 boxed reads use the `SwiftSelf` `context.Value`, none use the cdecl `contextPtr`), `nuke binding-tests --device` **2589 pass / 33 skip / 0 fail / 0 crash** (was 2588 — the previously-crashing `SetValidator` test now passes), and Codex + Grok paired review confirmed read/write agree across every enumerated callback path post-fix (the lone flagged item — the escaping branch of `Marshaler/Projection/ClosureProjection.cs` — is **dead code**: its `CallbackDeclarations` has zero emission consumers and `GetParameterPlan`'s escaping branch is never reached, so the cdecl-vs-Swift-self skew Codex noted there is unreachable; logged below for if that path is ever wired up).
- **[OPEN — latent dead-code defect in `ClosureProjection`, surfaced by the box/unbox paired review above; NOT reachable today, logged so it isn't re-discovered]** **The escaping-parameter branch of `Marshaler/Projection/ClosureProjection.cs` emits a `[UnmanagedCallersOnly(CallConvCdecl)]` callback with a trailing `IntPtr context` (`CallbackDeclarations`, ~167–242) but wires it into a `SwiftClosureData` (`GetParameterPlan`, ~58–79) with no Swift-side cdecl adapter — so were Swift to invoke it as a thick closure, context would arrive in the Swift self/context register, not as a trailing cdecl arg (a cdecl-vs-Swift-self mismatch).** It is **inert today**: `MethodMarshalPlan.CallbackDeclarations` has **zero** read sites (no emitter iterates it), `MethodMarshalPlanBuilder` never populates it and never calls `GetParameterPlan` on a closure projection (closure *parameters* are emitted by the `ClosureHandler`/`ClosureEmitter`/`WrapperEmitter.Marshalling` path; the projection factory is used only for closure *return* marshalling, whose `GetReturnPlan` correctly uses `delegate* unmanaged[Swift]`), and `closureCallback`/`s_*Callback` from this path appears **0×** in generated `BindingTests` output. Both Codex (P1) and Grok (Medium) flagged it as a decoupled/latent concern and judged it safe under current routing. **Fix shape if ever wired:** match the escaping-parameter direction to the live `ClosureEmitter` contract — either make the callback `CallConvSwift` + `SwiftSelf` and box the context on the legacy path (mirroring `useBoxedContext`), or keep cdecl and emit the Swift-side cdecl-unbox adapter that the live path's `useCdecl=true` branch relies on. Logged, not queued (unreachable; no real-world lib or fixture hits it).
- **[OPEN — discovered while building the §6 regression fixture above; different shape (closure-return arg marshalling, not co-gater/wrapper-symbol); logged per the no-cascade rule, NOT fixed in the same pass]** **A returned/property *throwing* closure that takes a by-value argument generates an invoker whose function-pointer argument slot is hardcoded `void*` and is passed the raw managed argument → CS1503 (or, for a pointer-typed arg that happens to compile, an unmarshalled value).** The non-throwing returned-closure path emits a per-argument marshalling prologue — `TypeMetadata.GetTypeMetadataOrThrow<T>()` + `stackalloc`/`NativeMemory.Alloc` + `SwiftMarshal.MarshalToSwift` + bool/class/existential conversions — via `ClosureEmitter.StructParams.cs` (`EmitClosureReturnMarshallingWithStructParams` / `…WithNonFrozenParams`, dispatched by struct-arg detection) and passes the resulting `byte*`/converted expression. The **throwing** counterpart `ClosureEmitter.EmitThrowingClosureReturnMarshalling` (`ClosureEmitter.Throwing.cs:166`, fallback path ~203–308) has **no** struct-param variant: it builds invoke args via `GetSwiftInvokeArgExpression`, which for a struct arg returns the bare `_arg{i}`, and the func-ptr type from `GetPInvokeFunctionPointerTypeWithError` renders the arg slot as `void*` → `_fp(_arg0, &_error, _swiftSelf)` won't convert `RequestConfig` to `void*`. **Repro:** a gettable property or returned closure of type `(SomeStruct) throws -> Void` (or any non-pointer by-value arg). First surfaced by an earlier draft of `OptionalThrowingModifierHolder` with a `(RequestConfig) throws -> Void` *gettable* property (CS1503 at the two getter-invoker `_fp(_arg0, …)` sites); the committed fixture sidesteps it by using arg-free `() throws -> Void` on its properties and reserving the struct-arg shape for the parameter direction (which marshals correctly). **Fix shape:** give the throwing return-invoker the same per-arg marshalling prologue as the non-throwing path — either route it through the `StructParams` emitters (parameterized to append the `&_error` arg + error check) or factor the per-arg marshalling into a shared helper both call. Needs `--device` (NativeAOT) validation since it changes closure-arg struct marshalling. Only the throwing-closure-return-with-by-value-arg shape is affected; no current real-world validation lib is known to hit it (the §6 regression libs use the *parameter* direction), so it is logged here, not queued.
- **[OPEN — surfaced by S4's end-review (Codex r1 High / Grok r1 Low "masked"); pre-existing, explicitly acknowledged by P1-15, different shape from P1-15's size-clamp fix]** **`FrozenStructHandler.EmitIntPtrFields` rounds every frozen stored field up to whole 8-byte `IntPtr` words, so a frozen struct carrying ≥2 *sub-word* `Optional` fields is over-sized vs Swift's true layout → field offsets shift past the `[StructLayout(Size = VWT->Size)]` envelope → by-value call/return corruption.** `ClassifyFrozenStructField` routes every `Optional<T>` field to the `IntPtrFields` kind (`FrozenStructHandler.cs:521`), and `EmitIntPtrFields` (`:730–744`) computes `wordCount = (fieldSize + IntPtr.Size - 1) / IntPtr.Size` and emits one `IntPtr` per word. For sub-word optionals this rounds up: e.g. two adjacent `Bool?` fields (Swift `size=1` each; `MemoryLayout<struct{a,b:Bool?}>` = `size=2 stride=2 align=1`) emit 2×8 = 16 bytes of backing field vs Swift's 2 → the struct grows past its VWT size and any field after the first is mis-offset. A *single* sub-word optional next to a ref/word field is masked by 8-byte alignment (the rounding lands on the same boundary Swift's stride would), which is why no current fixture or real-world lib exercises the broken case. **Relationship to P1-15 (closed this session):** P1-15 fixed the orthogonal *size-computation* defect (`TryComputeOptionalInlineSize`/`TryGetOptionalPrimitiveInlineSize`, `:577`/`:648` — the historical one-word clamp that *under*-sized `Int?`-class 9-byte optionals) and, in the same pass, **explicitly documented this rounding limitation** at `FrozenStructHandler.cs:640–646` ("The sub-word Bool/Int8/…/Float distinctions are therefore presently masked by that rounding … this helper still reports the true Swift layout size … for any future precision-dependent consumer"). So this is acknowledged, pre-existing, and a *different shape* from P1-15 (a multi-field *packing* defect in `EmitIntPtrFields`, not the per-field size computation P1-15 corrected). Reviewers split: Codex rated High (latent corruption), Grok rated Low ("masked today"); both agree no live consumer hits it. **Fix shape:** teach `EmitIntPtrFields` (or a successor field-layout emitter) to *pack* sub-word optionals into shared words honoring the true Swift offsets `TryGetOptionalPrimitiveInlineSize` already reports, OR — fail-closed — detect a frozen struct with ≥2 sub-word optionals whose summed packed layout differs from the word-rounded layout and skip/error it rather than silently over-size. Needs a BindingTests fixture with adjacent sub-word optionals (`{ a: Bool?; b: Bool? }` / `{ a: Int8?; b: Int8?; c: Bool? }`) round-tripped by value + on `--device`. No current real-world validation lib is known to hit it, so it is logged here, not queued.
- **[OPEN — surfaced by S4's end-review (Grok r1) and already noted in-code; pre-existing deeper gap, NOT a regression introduced by P1-07/P1-08]** **Owned-return projection of a *nested* existential collection (`[[any P]]`, `[K: [any P]]`, `[[K: any P]]`) routes its inner existential elements through the *non-owning* element conversion, so the inner class-bound cells are not adopted at +1 → a leak (or, symmetrically, an over-release) of the inner existential payloads on a nested owned return.** P1-07/P1-08 routed the *top-level* owned-return element/value through `ExistentialProjection.GetOwnedReturnElementConversion` (`ExistentialProjection.cs:206`), which adopts the subscript-getter/move-out +1 (`new {Proxy}(e, ownsContainer: true)`); this is now **empirically proven balanced on both Mono and NativeAOT** by `ClassBoundExistentialCollectionLeakProbeTests` (the `[any Marker]` / `[String: any Marker]` LifetimeTracker probes added this session — live count returns to 0 across 50×5 materialize+dispose cycles, refuting the H1 over-release hypothesis). But `ArrayProjection.OwnedReturnElementConversion` (`ArrayProjection.cs:222`) only owns the *outer* element; an inner `[any P]` element is itself projected by the default (non-owning) `GetReturnElementConversion` (`ExistentialProjection.cs:186`) because the owned-ness is not threaded recursively through the nested collection projection — exactly as the in-code comment already records (`ArrayProjection.cs:218–220`: "Nested existential collections, e.g. `[[any P]]`, still project non-owning — a pre-existing deeper gap, not a regression introduced here"). **Fix shape:** thread the owned-return element conversion recursively so a nested collection's inner existential elements also adopt (`ArrayProjection`/`DictionaryProjection` owned-return paths build the inner projection with `GetOwnedReturnElementConversion` rather than the default), with a nested LifetimeTracker probe (`[[any Marker]]` owned return) as the durable gate. Unexercised by any current real-world lib or fixture (no top-level owned `[[any P]]` return is known in the validation set), so it is logged here, not queued.
- **[OPEN — surfaced by S4's end-review (Grok r3 H1 residual + composition extrapolation); same shape as P1-08's opaque sibling, one step past every reachable site; pre-existing, EMPIRICALLY UNREACHABLE in the current surface]** **Two residual carrier-conversion fall-throughs in `ExistentialProjection.GetArrayElementCarrierConversion` (`ExistentialProjection.cs:162`) route an `__owned` existential collection-element param/write through the *non-owning* alias (`GetParameterElementConversion`'s `((ISwiftExistentialConvertible<EC>)e).GetExistentialContainer()` tail) instead of minting/donating an owned carrier — the same over-release shape P1-08's opaque sibling fixed for the reachable arity-1 cases.** The owned-carrier dispatch has exactly two branches — `IsClassBoundArity1` → `CreateOwnedClassCarrier`, and `_proxyClassName != null && _containerType == "Swift.Runtime.ExistentialContainer1"` → `CreateOwnedExistential1` — so two sub-shapes fall through to the aliasing tail: **(a)** an EC1 single-protocol existential whose `_proxyClassName` is null (the `publicType` is `object` / `Swift.Runtime.ExistentialUnion` / a well-known protocol per `TypeProjectionFactory.ProjectExistential:510`); **(b)** a **composition** existential (`any P & Q`, arity ≥ 2 → `ExistentialContainer2`+), whose container type never equals EC1 so branch 2 is skipped. **Empirically unreachable today** (verified against generated `BindingTests/output/SwiftBindingsTestLib.cs`): `grep -cE "Select\(.*GetExistentialContainer\(\)"` = **0** across all arities (no collection param/write emits the aliasing tail at all), and `FromEnumerable<…ExistentialContainer[23]>` = **0** (no composition existential ever reaches a collection carrier). All **19** owned-carrier conversions actually emitted (6 `CreateOwnedClassCarrier`, 13 `CreateOwnedExistential1`) take the minting/donating path; the fall-through has zero emission sites. For (a) the null-proxy EC1 cases are *separately* safe by routing: `object`/bare-`Any` box a fresh +1 via `ExistentialContainer0.Box`, `ExistentialUnion` mints via try-cast, well-known protocols (`Swift.Error`→`AnyError`) carry their own owned-transfer — none alias a borrowed proxy +1, so even if reached the over-release would not manifest the same way. **Fix shape (if a composition or null-proxy existential collection param/write ever lands):** generalize the owned-carrier dispatch to be arity- and proxy-agnostic — an `ECn`-ownership-aware `GetOrCreate`/`CreateOwnedExistentialN` (mirroring the EC1 `MintOrDonateExistential1` no-fallback `ownsContainer` signal up to the `ExistentialContainerN` wire-buffer-retain copy) wired into `GetArrayElementCarrierConversion`'s branch set, with a composition-existential array/dict LifetimeTracker probe as the durable gate. Logged, not queued — no current real-world lib or fixture emits either sub-shape.
- **[OPEN — surfaced by S5's end-review (Codex r2 Medium); pre-existing + uniform across all async BoundType/BoundStruct params, different shape from S5's P0-04 Swift-side ABI fix; ergonomics-not-correctness, EMPIRICALLY UNEXERCISED]** **The async `CreateAsync` public C# API surfaces every flattened `BoundType`/`BoundStruct` init param as raw `IntPtr`, while the non-async factory path emits the typed wrapper conversion — so S5's P0-04 (which taught the *Swift* async `_Create` wrapper to read an ObjC-bridgeable `BoundStruct` as a bridged-object pointer) has drifted from the async C# Session-API contract.** The async param-type switch falls to the default `param.CSharpPInvokeType` (= `IntPtr`) at `SwiftUIBridgeEmitter.AsyncPattern.cs:1152`, null-checks at `:1192`, and forwards the raw `IntPtr` unchanged at `:1278`; the non-async factory at `SwiftUIBridgeEmitter.cs:3058` instead emits `param.Name.Handle` for ObjC-bridgeable structs / `param.Payload.DangerousGetHandle()` for class wrappers. P0-04 made the Swift async side expect a bridged-object pointer (`Unmanaged<AnyObject>.fromOpaque(ptr) as! URL`, `AsyncPattern.cs:752–757`), so the generated public signature reads `CreateAsync(IntPtr url, …)` instead of `CreateAsync(Foundation.NSUrl url, …)` and does not forward `url.Handle`. **Not a crash, not introduced by S5:** the `IntPtr` surface is pre-existing and *uniform* across every async BoundType/BoundStruct leaf param, and it functionally works because the consumer can pass `nsUrl.Handle` as the `IntPtr` and the Swift `as! URL` toll-free bridge accepts it. **Why not absorbed (per §1.4):** different shape from P0-04 (which is the Swift-side ABI *read*, now correct) — this is the C# public-API *type surface*; absorbing only the ObjC sub-case would create a new BoundStruct-vs-BoundType inconsistency in the async path (non-ObjC class params would remain `IntPtr`); and it is unexercised by any current fixture (no async-pattern View has a `Foundation.URL` leaf param). Convergent with the prior 'Grok High #3 async public-IntPtr surface' note from S5's earlier rounds. **Fix shape:** mirror the non-async typed conversion at `SwiftUIBridgeEmitter.cs:3058` onto the async param-type switch (`AsyncPattern.cs:1152`), null-check (`:1192`), and forward (`:1278`) so `CreateAsync` surfaces typed wrappers (`Foundation.NSUrl` / the class wrapper) and forwards `.Handle` / `.Payload.DangerousGetHandle()`; durable gate = an async-pattern View whose leaf init param is `Foundation.URL`. Logged, not queued.
- **[OPEN — surfaced by S5's end-review (Grok r2 Medium M3); same family as S5's P0-04 typed-closure ObjC-struct fix, one step past the reachable case; reachability UNCONFIRMED, safe fix needs investigation]** **The typed-closure C# trampoline decode routes `BoundStruct` + `IsObjCBridgeable` through `ObjCRuntime.Runtime.GetNSObject<T>(argN)!` (S5's P0-04 typed-closure fix at `SwiftUIBridgeEmitter.cs:3692`) but routes `BoundType` + `IsObjCBridgeable` (an ObjC-bridgeable *class* closure arg) through `SwiftMarshal.MarshalFromSwift<T>` like any other BoundType — a narrower ObjC split than the Result-closure branch, which special-cases `BoundType`+IsObjC to `GetNSObject` + `passUnretained`.** **Why not absorbed:** `GetNSObject<T>` requires `T : NSObject`; a genuine Swift-class (`ISwiftObject`) closure arg that is *also* tagged ObjC-bridgeable cannot be blindly routed through `GetNSObject` without breaking the `ISwiftObject` wrapper case — so mirroring the Result branch's `BoundType`+IsObjC → GetNSObject onto the typed path is not a safe mechanical copy. **Reachability unconfirmed:** the only S5 fixture (`UrlClosureView`) exercises a `BoundStruct` (URL→NSURL); no fixture exercises a typed closure with an ObjC-bridgeable *class* arg, and it is not established that any real-world shape produces `BoundType`+IsObjC as a typed-closure arg. **Disposition (per §1.4):** not a confirmed live defect (no reachable shape) and the safe fix requires distinguishing NSObject-derived ObjC classes from `ISwiftObject` Swift-class wrappers → logged, not absorbed. **Fix shape:** add a typed-closure decode branch routing `BoundType`+IsObjC through `GetNSObject<T>` *only when* `T` is NSObject-derived (not an `ISwiftObject` wrapper), with a typed-closure ObjC-class-arg BindingTests fixture as the durable gate. Logged, not queued.
- **[RESOLVED — S6 grep-sweep discovery; whole-category fix in one pass per the no-cascade rule. The *user-param == injected-synthetic-PARAM* direction of the synthetic-name collision theme. DISTINCT direction from the S2 synthetic-LOCAL bullet above — see "Relationship" below]** **A USER parameter spelled exactly like a synthetic binding the generator injects into the `@_cdecl`/`@_silgen_name` wrapper signature collides, producing a defect whose severity depends on the emission mechanism.** The generator mixes injected synthetics (`self_`/`_self`/`__self`/`selfObj` receiver pointer, `resultPtr`/`__resultPtr` indirect-result buffer, `errorOut`/`errorPtr` throwing out-param, `tag` simple-enum discriminator, `newValue` setter value, `_resultBuf` default-overload buffer, `completionFn`/`completionCtx` async trampoline, `cdecl`/`hasValuePtr`/`parentMetaPtr`/`_by` — the full set is `NameProvider.ReservedSwiftWrapperParamNames`) with user bindings in a flat param list. A user binding equal to one of these collides via **three** mechanisms, fixed at three chokepoints: **(#1, CS0100, loud)** the C# P/Invoke declaration declares the name twice → fixed by the idempotent positional-safe dedup `PInvokeEmitHelper.DeduplicateCSharpParamNames` (renames the later duplicate `{name}_{N}`) wired into the universal `FormatDeclarationLines` chokepoint (~30 callers), plus the raw-`csWriter.WriteLine` decl sites that bypass it — the four enum-decl joins in `EnumHandler.SimpleEnum.cs` (508/561/983/1470) and `ConformerKeyPathInitFactoryEmitter.cs:398`; safe because C# P/Invoke param names are positional-only at the call site. **(#2, SILENT → runtime crash, HIGHEST severity)** the Swift wrapper declares two params with the same internal binding name → `swiftc` rejects the function, the wrapper is silently dropped, the entry point is missing from the dylib, and the call crashes → fixed by escaping the USER binding via `NameProvider.EscapeReservedSwiftWrapperLabel` (delegates to `MakeNonCollidingSyntheticName` against `ReservedSwiftWrapperParamNames`), routed through the shared `CdeclParamMapper.Map` chokepoint (a `escapeReservedCollision` opt-out flag, plus direct application at 36 verified per-emitter computation sites — `MethodWrapperEmitter`, `OptionalPointerWrapperEmitter`, `CrossModuleExtensionEmitter.{Struct,Class}`, `MetatypeArrayBridgeEmitter`, `MarkerProtocolOverloadEmitter`, `KeyPathBagValueSpecializationEmitter`, `ConcreteProtocolSpecializationEmitter` + `.AsyncGenericParent`, `MethodGenericBridgeEmitter`, `ConstructorWrapperEmitter`, `ClosureEmitter.SwiftWrapper`, `ArraySliceNormalizationEmitter`, `ExistentialBypassEmitter`, `DefaultParameterOverloadEmitter`, `PInvokeEmitter`). The escape renames the *source-local* binding only; the *external* Swift call label is computed separately (`CallArgumentsString`/`argLabel`), so forwarding to the underlying Swift API is unchanged — and because Swift `@_cdecl` and C# P/Invoke params match by POSITION not name, the escaped Swift binding may freely differ from the consumer-visible C# name. **(#3, CS0136, loud)** a generated body-local shadows a user param → `NameProvider.SyntheticLocalNames.Resolve(MethodDecl)` resolves a per-method non-colliding local-name scope. **SKIP reasoning (deliberate non-escapes, output-safe):** (a) the three setter `newValue` sites in `PropertyWrapperEmitter`/`SubscriptWrapperEmitter`'s *value* param pass `escapeReservedCollision: false` because there `newValue` IS the synthetic itself, referenced by bare name in the setter body — escaping its decl would orphan the body reference ("cannot find 'newValue' in scope"); a subscript *index* named `newValue` is a genuine user collision and DOES escape (keeps `newValue` in the reserved set, per-call opt-out only); (b) the success/error callback methods and the metadata/free `@_cdecl` wrappers take fixed synthetic-only params with no user names, so they stay literal; (c) the four `_`-prefixed generic-bridge buffer locals and `ConstrainedExistentialBridge` sites already mint names outside the reserved set. **Coverage:** `SyntheticNameCollisions.swift` + 10 `CollisionTests` + 3 cross-module `CrossModuleTests`; +13 pass on BOTH sim (Mono JIT) and device (NativeAOT); compile gate strict/fail-closed proves 0 CS0100/CS0136 across the whole generated tree. **Relationship to the S2 synthetic-LOCAL bullet above:** that bullet is the INVERSE direction — a hardcoded synthetic *local* (loop index, `is`-pattern var, bare `cdecl`/`_self` buffer literal) shadowing a user name (CS0136), fixed by renaming the *synthetic* via `SyntheticNameScope`/`MakeNonCollidingSyntheticName`. S6 resolved the *user-side escape* direction (rename the user binding when it equals a reserved synthetic) comprehensively for the `@_cdecl`/P/Invoke param surface; the S2 bullet's synthetic-LOCAL bare-literal shadow shape in emitters not on S6's 36-site list remains as it describes (lower-reachability, defense-in-depth, S7's name/key theme). The two share the guard primitive (`MakeNonCollidingSyntheticName`) but are applied in opposite directions at different surfaces.
- **[RESOLVED — surfaced by S6's GRDB-regression fixture (`ItemBag`) once the setter-escape fix let the wrapper compile; DIFFERENT shape from S6's name-collision scope (optional-existential *return* marshalling, not an identifier collision), so logged here per §1.4 even though it lives in a file S6 already edits. Fixed in-pass: guaranteed-crash dead path, mirrors a proven sibling block, zero blast radius]** **A large optional-existential accessor return (`subscript -> (any P)?`, `var x: (any P)? { get }` on a non-frozen value type — `ItemBag.subscript`, GRDB's `PersistenceContainer.subscript -> (any DatabaseValueConvertible)?`) crashed the getter with `NotSupportedException: Cannot marshal type IProtocol from Swift`.** Root cause: `WrapperEmitter.Return.cs`'s `_requiresIndirectResult` block had no `IsAccessor && IsOptionalExistential` guard, so a 40-byte `ExistentialContainer1` accessor return fell through to the catch-all `MarshalFromSwift<IProtocol?>` (`SwiftMarshal.cs:1140` throws); the correct small-existential accessor block (`:549–583`) is unreachable once the return is large enough to require an indirect buffer. **Resolution:** added the missing guard immediately before the catch-all — reads `Swift.SwiftOptional<ExistentialContainer1>` from the indirect buffer, returns `null` on `.None`, else adopts via the well-known ctor or `…Proxy(some, ownsContainer: true)`. Ownership matches the decomposed-property path (Swift writes inner existential at +1 via `initializeMemory`; `MarshalFromSwift` bitwise-copies, no re-retain; buffer's plain `NativeMemory.Free` leaves +1 intact; proxy adopts and releases on `Dispose` — no leak, no double-free). Verified by `TestItemBagSubscriptGetterRoundTrip` on sim **and** device, plus GRDB `swift_compile`=ok in validate.
- **[RESOLVED — root-caused to TWO distinct defects (a build-harness strip cascade + a genuine generator emitter conformance cycle); the original "incomplete conformance" diagnosis below was WRONG. Fixed end-to-end with the `describeBag()`→`describe()` work-around reverted and a dedicated reverse-dispatch fixture as the durable gate.]** **When two emittable protocols each declare a method with the same Swift signature (e.g. both `func describe() -> String`), the catch-all `EveryProtocol` emits the shared witness body once on the lexicographically-smallest *owner* protocol and an EMPTY `extension EveryProtocol: <sibling> {}` for each peer.** Swift cross-extension witness inheritance legitimately satisfies the peer from the owner's body — the empty extension is **NOT** incomplete-conformance, contrary to the original note. The observed failures were two unrelated bugs the fixture happened to surface together:
  - **Bug #1 (build-harness — the `EntryPointNotFoundException` symptom): `SwiftSourceStripper` stripped the empty `extension EveryProtocol: P {}` for a non-preserved `P` but left its `@_cdecl("Get_EveryProtocol_P_WitnessTable")` getter — which binds `any P = instance` and only compiles while the conformance exists — orphaned.** No other strip pattern catches that getter (it is a plain `public func`, not an `SBW_`/`PInvoke_` symbol), so swiftc rejected the orphaned `any P` bind and the coarse line-based retry-strip then cascaded into UNRELATED symbols in the same file (observed: a stripped same-signature loser conformance taking out an unrelated `..._init` → runtime `EntryPointNotFoundException` on `SBW_…ItemBag_init`). **Fix:** a Pattern-1b pre-scan (`CollectStrippedExtensionProtocols` sharing the `ShouldStripEveryProtocolBlock` predicate with Pattern 1, so the two never diverge) strips the witness-table getter in lock-step with its extension; and `SiblingMethodOwner`/`SiblingMethodPeer` were added to `PreservedProtocols` so the durable fixture's conformances survive the harness.
  - **Bug #2 (generator emitter — a compile-time `EveryProtocol does not conform to specified type <sibling>`): the method fan-out body cast `self` to each *sibling* protocol type (`var selfProto: <sibling> = self`) before invoking that sibling's vtable slot.** This forced swiftc to eagerly resolve every sibling's conformance while typechecking the owner's body — but a sibling whose only witness is borrowed from this very body creates a resolution cycle, so the build failed with `EveryProtocol does not conform to specified type <sibling>` for every same-signature peer. The generated C# receiver (`Receive_<method>_…`) reads ONLY word 0 of the existential (the class reference: `var handle = *(IntPtr*)selfContainer;`) and never the witness table, so the sibling-typed box was gratuitous. **Fix:** box `self` as the *owner's* protocol type (`branches[0]`) for every branch — the word-0 class reference is identical and the owner always conforms self-containedly, breaking the cycle. Applied uniformly across all six fan-out emission sites (method / property getter+setter / subscript getter+setter / closure-property getter+setter) per the grep-all-instances rule; single-owner extension bodies (which box their own protocol) were verified safe and left unchanged.
  **Coverage:** the fixture work-around was reverted — `Properties/Subscripts.swift` `describeBag()`→`describe()` (now colliding with the new sibling protocols' `describe()`) is the end-to-end Bug #1 regression; a dedicated `Protocols/SiblingMethodDispatch.swift` (`SiblingMethodOwner`/`SiblingMethodPeer`, same `siblingMethodValue()`/`siblingMethodEcho(_:)` signatures) + `SiblingMethodDispatchTests.cs` (6 reverse-dispatch tests through owner-only, peer-only, and both-conforming C# impls) is the Bug #2 gate; 7 new `EveryProtocolEmitterTests` assert owner-typed boxing across the fan-out sites. Verified: `nuke binding-tests --compile-only` green (0 conformance errors), full sim **2669 pass / 0 fail / 53 skip / 0 crash**, unit **12411 pass / 0 fail** (`.validation-baseline.json` sim auto-updated 2655→2669). No library in the current validation-libraries set produces two emittable protocols sharing a method signature, so neither defect appeared in the real-world sweep; both are caught at build time (Bug #2 compile error / Bug #1 harness cascade) rather than shipped as a silent runtime fault. **End-review (Codex r1):** Codex surfaced one Medium — the plain-method fan-out *receiver* reused the owner protocol's C# method name for every sibling-interface call, so a sibling whose property set renames the method differently (a same-named property in one protocol but not the other) would emit a `siblingProxy.<owner-name>(…)` call to a method that interface never defined (CS1061/CS1955) — FIXED this pass by resolving the name per protocol (`ComputeReceiverPascalMethodName`, `ProtocolProxyEmitter.Receivers.cs`), with the `SiblingNameOwner`/`SiblingNamePeer` fixture (owner carries a `collidingTag` property + a `collidingTag()` method, peer only the method) as the durable gate. Codex r1's High (closure-shaped / async same-signature fan-out) is a different emission mechanism that fails loud and is logged as the next §6 entry below. **Verified post-fix (Codex r2 `019e9619-25f7-7dc1-a4c0-5e6885bdb8ba`, Grok r1 `019e9684-8f4e-7d31-83e6-b433a3346852`):** Codex r2 confirmed the per-protocol name fix landed (per-sibling `ComputeReceiverPascalMethodName` at `ProtocolProxyEmitter.Receivers.cs`) and re-raised ONLY the already-logged closure/async High — no new defect. Grok r1 found no correctness defect in the executed paths; its two Highs are (a) a *unit-coverage-depth* gap on the C# receiver sibling-glue — the behaviour IS covered end-to-end by the BindingTests reverse-dispatch + name-divergence cases plus the compile gate (a `GetPublicMethodName` drift surfaces as CS1061 at the compile gate, not a silent runtime fault), so coverage is correctly placed at the behavioural layer per the project's assert-behaviour-not-implementation rule; and (b) a *pre-existing* leaf-name collision risk logged separately below.
- **[OPEN — surfaced by the audit-item-1 end-review (Codex r1 High); SAME family as the resolved same-signature method fan-out above, one emission mechanism past the reachable case; pre-existing, fails LOUD, EMPIRICALLY UNEXERCISED]** **The same-signature method fan-out (owner emits the shared body + fans out across sibling vtables; receiver tries each sibling interface) covers only the PLAIN sync value/string/ObjC-return method shape.** A same-signature method whose shape is a dispatchable-closure parameter, a closure return, or async takes a DIFFERENT emit path (`EmitClosureMethodImplementation` / `EmitDispatchableClosureReturningMethodImplementation` / the async-closure receiver) that reads only the OWNER's own vtable and does **not** fan out — and the C# receiver deliberately disables the sibling fallback for exactly those shapes (the `!method.IsAsync && !hasDispatchableClosureParamForFallback` gate in `ProtocolProxyEmitter.Receivers.cs`, with the in-code comment "async Task returns and dispatchable-closure params take other emit paths that do not fan out, so applying the fallback there would mis-marshal"). So if two emittable protocols share a closure-shaped / async method full signature and a C# type implements ONLY the non-owner protocol, dispatch through the non-owner existential routes (via Swift cross-extension witness) into the owner's body, which force-unwraps its own **nil** vtable field (`{vtable}.{field}!(…)`, `EveryProtocolEmitter.cs:3732`) → a LOUD Swift trap ("unexpectedly found nil while unwrapping an Optional value"), **not** silent corruption. Both sides are intentionally consistent, so this is a known scope boundary of the plain-method fix, not an inconsistency. **Reachability:** requires two emittable protocols sharing a CLOSURE-shaped or async method full signature AND a non-owner-only C# impl AND dispatch through the non-owner existential — no library in the validation set produces it, and even the plain same-signature collision has zero real-world hits. **Fix shape:** thread the `methodPlan` owner-box + sibling-vtable fan-out (the plain `EmitMethodImplementation`/`EmitMethodFanOutBody` path) into `EmitClosureMethodImplementation` / `EmitDispatchableClosureReturningMethodImplementation` / the async-closure method emitter, and extend the receiver sibling-fallback past the `!hasDispatchableClosureParamForFallback`/`!IsAsync` guard with the matching (fnPtr,ctx) / TCS marshalling — needs `--device` (NativeAOT) validation since it changes closure/async ABI, plus BindingTests fixtures for closure-param, closure-return, and async same-signature collisions with a non-owner-only impl. Logged, not queued — different emission mechanism, loud failure, unexercised by any current lib or fixture.
- **[OPEN — surfaced by the audit-item-1 end-review (Grok r1 High); PRE-EXISTING, orthogonal to the same-*signature* fix; not reachable in the single-module BindingTests harness]** **The EveryProtocol witness-table getter symbol (`GetWitnessTableGetterMangledName`, `EveryProtocolEmitter.cs:5072` → `Get_EveryProtocol_{protocolDecl.Name}_WitnessTable`) keys on the protocol's LEAF name only, omitting the `GetCrossModulePrefix(...)` that the sibling `GetSetVtableMangledName` (`:5060`) DOES include — and the build-harness stripper's `WitnessTableGetterCdecl` regex + `PreservedProtocols`/`CollectStrippedExtensionProtocols` HashSet likewise match on the leaf name.** So two emittable protocols sharing a leaf name across a module boundary (or nested under different parents) would emit colliding `@_cdecl` getter symbols in the single wrapper source AND conflated keep/strip decisions — whereas the set-vtable symbols, the fan-out keys, and the C# interface qualification all use the full module+nesting chain. **Not introduced by audit item 1** (the getter has always been leaf-only; item 1 only made the Pattern-1b lock-step strip depend on it) and **not reachable in the current BindingTests library** (single module `SwiftBindingsTestLib`, no same-leaf nested protocols), so it produced no failure in this work. **Fix shape:** thread `GetCrossModulePrefix` into `GetWitnessTableGetterMangledName` (and its `Get*FunctionName` pair) to match the set-vtable disambiguation, mirror the prefix into the stripper's getter regex + preserved-name comparison so keep/strip stays lock-step, with a two-module same-leaf-protocol fixture as the gate. Logged, not queued — pre-existing, orthogonal, needs cross-module reproduction before fixing.
- **[OPEN — surfaced by S7's Kingfisher-regression sibling-dispatch consult (Codex `019e98d7-4989-7363-89a8-be2379e09a56` + Grok `019e98d7-6659-7be3-aa0e-aa4590cb49fc`, both r1); SAME shape as the cross-protocol async-key fix S7 landed, one mechanism over — the INTRA-protocol vtable-slot keys; spans two files OUTSIDE S7's edit set, so §6 per §1.2; pre-existing, EMPIRICALLY UNEXERCISED]** **A single protocol declaring both a sync and an async method that share name + params + return TYPE (`func m() -> Int` *and* `func m() async -> Int` — valid Swift, typechecked) collapses to ONE vtable dispatch slot, because the intra-protocol method-identity keys omit the `async` effect — so the async requirement's slot / receiver / UCO trampoline is dropped while the C# interface still declares BOTH `M(...)` and `MAsync(..., CancellationToken)` → a missing dispatch (or CS1061/CS1955 if a later layer references the dropped member).** S7 fixed the *cross-protocol* twin of this — Kingfisher's sync `ImageDownloadRequestModifier` refining async `AsyncImageDownloadRequestModifier`, both declaring `modified(for:)` — by adding the `async` effect to the two **sibling-fan-out** keys (`EveryProtocolEmitter.GetSwiftMethodFullSignature` ~`:1481`, `GetMethodSiblingMapKey` ~`:1150`); that resolved the CS1061 because the async/sync requirements live in *separate* protocols and thus separate vtable structs. The SAME `async`-omission survives in the **intra-protocol vtable-slot** keys, which an Explore enumeration confirmed: `EveryProtocolEmitter.GetMethodKey` (`:5095`, vtable index in `EmitProtocolVtableStruct`/`EmitProtocolExtension`, reached via `EnumerateProtocolMethodsForDispatch`), `WitnessDispatchEmitter.GetMethodKey` (`:2433`, P/Invoke accessor index), and `ProtocolSignatureHelper.GetMethodSignatureKey` (`:38`, C# vtable-struct first-layer slot dedup) — all three key only `name(labels:types)` with no `IsAsync`. These three index the SAME logical slot and must move together or dispatch desyncs; two of the three files (`WitnessDispatchEmitter.cs`, `ProtocolSignatureHelper.cs`) are **not** in S7's edit set, so per §1.2 ("absorb new same-shape defects only in files S7 edits; else one line in §6, do not fix") this whole category is logged, not fixed, rather than dragging a third- and fourth-file edit into S7. **Empirically unexercised:** no library in the validation set has an intra-protocol async/sync same-name overload (the only real-world hit, Kingfisher, was the cross-protocol shape, now fixed); the BindingTests catch-all EveryProtocol would surface it, but no current fixture declares the intra-protocol pair. **Fix shape (one pass, when queued):** add the `async` axis to all three vtable-slot keys consistently — index-preserving for every non-colliding protocol (the distinct-key set, count, and iteration order are unchanged; only a protocol that currently *collapses* an async/sync pair gains a second slot, and those are already broken), so zero `nuke validate` baseline churn is expected. `throws` stays OUT of these keys for the same reason it stays out of the sibling keys (a non-throwing witness satisfies a throwing requirement — the nonThrowingOverrides "share a witness" rule). Durable gate: a BindingTests fixture with one protocol declaring `func m()` + `func m() async` and a non-owner-only C# impl, validated on `--device` (NativeAOT) since it changes vtable-slot indexing. Logged, not queued — pre-existing, spans non-S7 files, no reachable real-world shape.
- **[RESOLVED — hardened defensively + RECLASSIFIED from "CONFIRMED reachable" to LATENT (not reachable by any compilable Swift shape today). The original Codex P1 trace was correct about the *branch* but wrong about its *reachability*: it assumed the Phase-1 closure/optbuf gates fire, but `MethodWrapperEmitter` preempts them for every ordinary method of this shape. Empirically verified — see below.]** **A small blittable-optional parameter (`Int32?`/`Int?`/`Double?`/`CGFloat?`/… — the set in `CdeclParamMapper.cs`'s `GetSwiftRawValueType`, Bool excluded) reaching the `else if (useCdecl)` branch of either FALLBACK Swift-wrapper emitter (`ClosureEmitter.SwiftWrapper.cs:1015`, `OptionalPointerWrapperEmitter.cs:169`) was mapped via `CdeclParamMapper.Map(…, omitLabels: true)` — the bare-pointer shape (`"_ {label}: UnsafeRawPointer", reconstruction: null, callArg: "{label}"`), correct ONLY for a `_dbw_init_*` dispatch target that decodes the Optional internally. These two wrappers forward to the *original* method, which expects the decoded `Int32?`, so the wrapper would emit `foo(n: n)` with `n: UnsafeRawPointer` — swiftc would reject it, the build would silently strip the wrapper, and the entry point would go missing.** **Reachability correction (the decisive finding):** in the live pipeline `MethodWrapperEmitter.ShouldEmitWrapper` (`MethodWrapperEmitter.cs:27`) claims every ordinary method of this shape FIRST and sets `UsesWrapperLibrary`; the Phase-1 gates that would invoke these two fallback emitters with `useCdecl: true` both require `!UsesWrapperLibrary` (`MethodHandler.cs` closure gate ~1083, optbuf gate ~1112), so they never fire when a method is already claimed. MethodWrapperEmitter emits `_sbw_method_<hash>` wrappers that ALWAYS map params with `omitLabels: false`, so the small blittable optional decodes correctly there. The Codex trace's premise — `ClosureEmitter.CanConvertToCdecl` true ⇒ `closureCdecl = true` ⇒ `useCdecl: true` — skipped the `!UsesWrapperLibrary` precondition on that Phase-1 gate; with MethodWrapperEmitter owning the method, the closure/optbuf fallback path is dead. **No compilable Swift shape reaches the buggy branch.** Empirical confirmation (two independent probes): (1) a regenerated probe with `@MainActor` and per-method custom-global-actor methods, each carrying a small `Int32?` alongside an `@escaping` closure / a large `BigPoint?`. `@MainActor` methods are claimed by MethodWrapperEmitter (wrapper annotated `@MainActor`, correct `omitLabels:false` decode). Per-method custom-global-actor methods are forced to `IsAsync = true` at parse time (`SwiftABIParser.cs:2107-2118`) and routed to the ASYNC wrapper path — the probe's `@ProbeActor func largeOptVariant(_ n: Int32?, _ big: BigPoint?)` emitted `SBW_…_largeOptVariant_…_async` (`await __self.largeOptVariant(…)`) + C# `Task<int> LargeOptVariantAsync`, never a sync `_cdecl` branch. Every sync wrapper path independently rejects the now-async method: the sync `MethodWrapperEmitter` via `CanEmitMember`'s async guard (`WrapperValidation.cs:183`), the closure fallback via `NeedsClosureCdeclWrapper` (`ClosureEmitter.SwiftWrapper.cs:1303` — `if (methodDecl.IsAsync) return false;`), and the optional-pointer fallback via its `!IsAsync` precondition (`MethodHandler.cs:1131`). So neither actor variant slips past MethodWrapperEmitter into the fallback `else if (useCdecl)` branch. **(Gate-citation precision — the controlling path is the async normalization above, NOT a per-method actor check inside the cdecl-shape gate: `HasCdeclCompatibleFunctionShape` — the `bool` gate `ClosureEmitter.CanConvertToCdecl` actually calls, `WrapperValidation.cs:990` — checks only a *type-level* `actor` parent at `:1034`, never per-method `IsActorIsolated`; the per-member `custom_actor_isolated` guard at `WrapperValidation.cs:1296` lives in the separate string-returning `GetRejectionReason` (`:1227`), which is NOT on the `CanConvertToCdecl` path. A frozen-struct method with a `@MyActor` annotation has a non-actor parent, so the `:1034` type-actor check does not fire either — the async normalization is the sole reason.)** **(This refutes the only concrete reachable-shape counterexample raised in end-of-task review — Codex's r1 claim that a per-method custom-actor-isolated frozen-struct method reaches the sync branch because `CanConvertToCdecl` tolerates the isolation where `ShouldEmitWrapper` rejects it. `CanConvertToCdecl` indeed does NOT reject it (per the gate-citation note above) — but that is moot: the parser forces the method to `IsAsync = true` first, so every sync wrapper path rejects it as async before `CanConvertToCdecl` is ever consulted, confirmed by the probe emitting an async `Task<T>` wrapper rather than a sync `_cdecl` branch. Grok's alternative `_const`-literal-sibling candidate yields a Swift-uncompilable wrapper — stripped, not a runtime crash. The actor probe also incidentally surfaced a SEPARATE, out-of-scope latent defect: the async wrapper for a custom-actor-isolated method with a large-optional param emits malformed C# — `CS1513 } expected` at the `…Async` Task method — a distinct async-emission bug, NOT the `omitLabels` shape; logged here only so it is not lost.)** (2) the in-repo BindingTests fixtures `addOptionalWithClosure(_ n: Int32?, _ onDone:)` and `addOptionalWithLargeOptional(_ n: Int32?, _ big: BigPoint?)` compile to `_sbw_method_<hash>` `@_cdecl` wrappers whose bodies read `let nOpt: Int32? = n.advanced(by: 4).load(as: UInt8.self) == 0 ? n.load(as: Int32.self) : nil` and forward `nOpt` — they pass with NO source change, confirming MethodWrapperEmitter ownership and a correct decode. **Defense-in-depth fix applied anyway** (the branch is latent, not provably-impossible-forever — a future routing change could expose it): `omitLabels: true → false` at both `else if (useCdecl)` branches, consuming the mapper's RETURNED `callArg` verbatim (and, in `OptionalPointerWrapperEmitter`, stripping the leading `label: ` for the bare-RHS `valueArgs` setter-assignment) — mirroring `PropertyWrapperEmitter`'s single-source-of-truth pattern (`PropertyWrapperEmitter.cs:629–638`) instead of the old `reconstruction != null ? {label}Val : csName` synthesis (which would also have mis-named the local: the blittable-primitive mapper declares `{label}Opt`, not `{label}Val`). Because the branches are dead in the live pipeline, the change produces **no reachable-output diff** → zero `nuke validate` baseline churn. **Pinning tests:** emitter-level unit tests in `OptionalPointerWrapperTests` ("Blittable-Optional @_cdecl Decode" region) drive both branches DIRECTLY with `useCdecl: true` (bypassing the preemption gate) and assert the `Int32?` is decoded (`let nOpt: Int32? =`, `.advanced(by: 4).load(as: UInt8.self)`, `load(as: Int32.self)`, declared-and-forwarded) — verified RED on `omitLabels:true`, GREEN on the fix. The BindingTests fixtures (`BlittableOptionalCdeclWrappers.swift` / `BlittableOptionalCdeclWrapperTests.cs`) are RETAINED and relabeled as genuine MethodWrapperEmitter decode coverage (round-trip non-nil + nil, sim + device) — their prior comments claiming fallback-emitter `@_cdecl` coverage were stale and have been corrected. **Why pre-existing / not an S6 regression:** S6 only added the reserved-name escape at these sites; the `omitLabels:true` argument and the `reconstruction != null ? {label}Val : csName` heuristic are older code S6 did not touch. Originally surfaced by S6's end-of-task Codex review (`Session: 019e950b-71ad-7ea0-b083-a7fa06db9925`) as its sole P1; Grok (`019e950b-92a2-7ee0-9ba0-19982ed9b891`) reviewed the same files for the reserved-name shape and correctly found it sound.
- **[OPEN — surfaced while implementing S9's P1-23 bridge-arch threading; different mechanism (Apple-framework second-slice compile is already its own staging path, not the third-party `WrapperXCFrameworkMerger` fat-fold S9 made transactional); logged per the no-cascade rule, NOT fixed in the same pass]** **The Apple-framework SwiftUI-bridge lipo path (`Sdk.targets` `_CompileAppleFrameworkSecondBridgeSlice`) folds the second simulator arch into the bridge xcframework through a *separate* already-staging MSBuild mechanism that S9's `WrapperXCFrameworkMerger` transactional rewrite (P1-25) does NOT cover.** S9 threaded `--target-architectures` into `_CompileSwiftUIBridge` (P1-23(b)) and made the third-party generator-side fat-fold atomic (P1-25), but the Apple-framework bridge second-slice fold lives entirely in `Sdk.targets` as its own target and was out of S9's single-owner file scope (the generator `Configuration/*` merger vs. the SDK `.targets`). It is not known to be broken — the Apple-framework path's existing staging differs from the third-party non-transactional fold P1-25 fixed, so it does not share P1-25's torn-tree window — but it was never audited for the same transactional/atomic-commit property under an interrupted build, and its arch-threading parity with the third-party bridge path was not exercised by the X64SimGate (whose Apple-framework leg uses StoreKit, which has no SwiftUI bridge). **Fix shape (if later confirmed needed):** audit `_CompileAppleFrameworkSecondBridgeSlice` for the same staging→atomic-swap + cross-run-recovery property `WrapperXCFrameworkMerger.MergeFatSlices` now carries, and add an Apple-framework-with-SwiftUI-bridge leg to X64SimGate so the second-slice fold is covered end-to-end. Logged here, not queued — no current gate reproduces a failure and the mechanism is distinct from every S9-owned file.
