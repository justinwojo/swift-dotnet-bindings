# 0.8.0 — Apple Framework Bindings

**Updated**: April 10, 2026

The headline of 0.8.0: bind Apple's Swift-first frameworks (StoreKit 2, WeatherKit, TipKit, etc.) and publish them as NuGet packages for .NET MAUI / .NET iOS apps. These APIs have no .NET binding today. `Microsoft.iOS` is fundamentally an ObjC binding generator and has no story for Swift-first frameworks.

---

## What's done

All generator/SDK/runtime work for the first Apple-framework package is complete:

- **Apple-framework target mode** in the generator — auto-detects when the target module matches a built-in dependency database and bypasses it. `swift-api-digester` ABI dumps are the metadata source, not wrapper packages.
- **`SwiftAppleFrameworkTarget` MSBuild item** in `SwiftBindings.Sdk` — drives ABI dump, generator invocation, wrapper compilation, and NuGet packaging for system frameworks.
- **Multi-TFM support** — single nupkg with `lib/`, `buildTransitive/`, and `runtimes/` for iOS, macOS, Mac Catalyst, and tvOS.
- **`NamespacePattern` metadata** — prevents namespace collision with Microsoft.iOS's ObjC types (e.g., `StoreKit` namespace → `StoreKit2`).
- **Second-slice wrapper compilation** — iOS/tvOS xcframeworks get both device and simulator slices merged via `xcodebuild -create-xcframework`.
- **StoreKit 2 validated end-to-end** — 68 types bound, wrapper compiles, C# compiles, runtime smoke tests pass on iOS Simulator (`AppStore.CanMakePayments`, `Transaction.unfinished` AsyncSequence iteration, `AppStore.DeviceVerificationID` UUID resolution).
- **Foreign value-type metadata** — `@_cdecl` accessors for UUID/Date/Decimal in `SwiftBindingsRuntime.swift`.
- **Co-gater cross-scope stripping fix** — resolved ~210 orphaned P/Invoke declarations across Transaction, Storefront, Product.
- **Mac Catalyst framework path fallback** — frameworks not in the iOSSupport overlay (WeatherKit, CryptoKit) now fall back to the regular macOS `System/Library/Frameworks/` path.
- **Foundation Unit type remaps** — 24 `Unit*` → `NSUnit*` mappings (UnitTemperature, UnitLength, etc.) so generated C# references the correct Microsoft.iOS types.
- **Enum helper `@available` annotations** — synthesized `@_cdecl` wrappers (InitWithRawValue, CaseByIndex) now inherit availability from the enum's parent type chain.
- **SoundAnalysis + CoreSpotlight validated** — build + pack across all 4 TFMs (iOS, macOS, Mac Catalyst, tvOS), ready for NuGet publish.

---

## Remaining work

### Ship 0.8.0 (must do)

- [x] **Validate additional Apple frameworks** — SoundAnalysis and CoreSpotlight build + pack across all 4 TFMs alongside StoreKit2. WeatherKit, CryptoKit, AuthenticationServices, and TipKit were attempted but hit pre-existing generator limitations (see below). Three bugs fixed during validation: Mac Catalyst framework path fallback, Foundation `Unit*` → `NSUnit*` type remaps, enum helper `@available` annotations.

- [ ] **Pack + publish to NuGet**
  - `nuke pack --version 0.8.0` → `SwiftBindings.Sdk`, `SwiftBindings.Runtime`, `SwiftBindings.Templates`
  - Copy nupkgs to `swift-dotnet-packages/local-packages/`
  - Build + pack `SwiftBindings.StoreKit2 26.2.0` via `dotnet pack` in `apple-frameworks/StoreKit2/`
  - Run regression validation against local feed (sim + device)
  - Publish all 4 packages to nuget.org
  - Commit swift-dotnet-packages (only after NuGet publish succeeds — per `feedback_no_commit_packages.md`)

- [ ] **Commit swift-bindings changes** — Sdk.props (no default TFM), Sdk.targets (Apple-framework target chain, multi-TFM pack, second-slice compilation, namespace-pattern support), SwiftWrapperCompiler.cs (macOS xcframework variant fix), unit test updates.

- [ ] **Wire `apple-frameworks/` into swift-dotnet-packages Nuke pipeline** — `Build.Xcframework.cs:DiscoverLibraries` only scans `libraries/*/library.json`. Apple-framework packages under `apple-frameworks/` are invisible to `BuildLibrary`, `Pack`, `PackValidate`, `BuildAndPackRelease`, and `ListChangedLibraries`. Need to extend discovery to handle SDK-sourced multi-TFM projects.

### Should do for 0.8.0

- [ ] **`dotnet new swift-binding --apple-framework` template** — add `--apple-framework <Name>` option to `SwiftBindings.Templates` that scaffolds a project with `<SwiftAppleFrameworkTarget>` instead of `<SwiftFramework>`.

- [ ] **`dotnet pack` regression test** — no unit test verifies that emit-and-pack produces consistent `lib/` and `buildTransitive/` TFM paths. The static-drift bug is fixed via `--platform-version`, but the regression gate is missing.

- [ ] **Complete availability annotation propagation** — the existing `@available` / `[SupportedOSPlatform]` pipeline works for most cases but has gaps that prevent frameworks like WeatherKit from compiling at deployment target 15.0. Two known issues:
  1. **Swiftinterface parser drops some platform annotations** — `DayWeather` has `@available(iOS 16.0, macOS 13.0, tvOS 16.0, watchOS 9.0, *)` in the swiftinterface, but some of its property wrappers end up missing the iOS annotation. Root cause not yet identified — likely in `ApplyAvailability` key matching or `CollectAvailabilityAnnotations` parsing.
  2. **Type-reference availability not inferred** — a wrapper function that references `UVIndex` (iOS 16+) in its body doesn't automatically inherit that type's availability. The existing pipeline only propagates from the member declaration and parent type chain, not from types used in signatures/bodies.
  Fixing these would unblock WeatherKit, AuthenticationServices (partially), and future Apple frameworks. The wrapper binary's deployment target becomes the hard `minos` in its Mach-O header — availability annotations are the correct way to gate newer APIs without raising the floor.

- [ ] **`UnsafeRawBufferPointer` in `@_cdecl` wrappers** — CryptoKit's `update` methods take `UnsafeRawBufferPointer`, which the Swift compiler rejects in `@_cdecl` functions (not representable in ObjC). Need an alternative marshalling strategy (e.g., `UnsafeRawPointer` + length pair).

- [ ] **Struct/class misclassification for newer ObjC types** — AuthenticationServices' `ASImportableEditableField` is treated as a struct but emitted with `.Handle` (class marshalling). The generator needs to correctly identify ObjC classes that aren't in the existing type database.

- [ ] **Duplicate member emission for protocol builder patterns** — TipKit's `Tips.ConfigurationOption` emits duplicate `CloudKitContainer`/`DatastoreLocation`/`DisplayFrequency` members, likely from `@resultBuilder` protocol extension handling. Also missing `Foundation.PredicateExpressions` type mapping and generic interface arity mismatches (`ITip<TSelf>` used without type arguments).

- [ ] **Runtime tests on non-iOS platforms** — at minimum macOS (most accessible). Architecture is validated; runtime proof is not.

### Stretch for 0.8.0

- [x] **Ship additional Apple frameworks** — SoundAnalysis and CoreSpotlight validated and ready to publish alongside StoreKit2.

- [ ] **Stripe hardening + publish** — probably already good enough to ship. Expand test coverage, NativeAOT validation, polish sample app.

---

## Locked-in decisions

### Versioning

- **Apple-framework packages**: `{SDK-major}.{SDK-minor}.{binding-rev}[-preview.N]`
  - Major.Minor mirrors the iPhoneOS SDK version (not Xcode)
  - Patch is our binding revision lane (bug fixes, generator fixes, runtime bumps)
  - Preview suffix for beta SDKs and pre-release cuts
  - Example: `26.2.0` → `26.2.1` (fix) → `26.3.0` (iOS 26.3 SDK) → `27.0.0-preview.1` (Xcode 27 beta)
- **Third-party packages**: unchanged — version mirrors upstream (e.g., `SwiftBindings.Nuke 12.8.x` = Nuke 12.8.x)

### SwiftBindings.Runtime pinning

Bounded `[0.8.0,0.9.0)` — patch releases must be ABI-compatible (no struct layout changes, no P/Invoke signature changes). Avoids republish tax across every Apple-framework package on every runtime patch.

### TFM in generator-emitted csprojs

Explicit `--platform-version` CLI flag (e.g., `--platform-version 26.2` → `net10.0-ios26.2` in both `<TargetFramework>` and `buildTransitive/`). Dynamic `$(TargetPlatformVersion)` resolution was rejected — .NET 10 library projects default to the oldest installed TPV, which produces stale TFMs on multi-workload machines.

### Package layout (multi-TFM, via SwiftBindings.Sdk)

```
lib/net10.0-ios26.2/SwiftBindings.StoreKit2.dll
lib/net10.0-macos26.2/SwiftBindings.StoreKit2.dll
lib/net10.0-maccatalyst26.2/SwiftBindings.StoreKit2.dll
lib/net10.0-tvos26.2/SwiftBindings.StoreKit2.dll
buildTransitive/net10.0-ios26.2/SwiftBindings.StoreKit2.targets
buildTransitive/net10.0-macos26.2/SwiftBindings.StoreKit2.targets
...
runtimes/ios-arm64/native/StoreKitSwiftBindings.xcframework/...
runtimes/osx-arm64/native/StoreKitSwiftBindings.xcframework/...
...
```

### Forward compatibility

Old nupkgs work on newer Apple workloads — Swift ABI stability since 5.0, .NET 10 TFM precedence rules select older-versioned `lib/` entries, `runtimes/` is RID-based not TFM-based. Verified with a `26.0 → 26.2` consumer repro.

### Naming

`SwiftBindings.StoreKit2` (not `SwiftBindings.Apple.StoreKit2`). Namespace is `StoreKit2` to avoid collision with Microsoft.iOS's ObjC `StoreKit` types.

---

## Apple framework shortlist (post-StoreKit 2)

| Framework | iOS | Value | .NET status |
|---|---|---|---|
| **SoundAnalysis** | 15+ | Audio classification | Partial — **validated, ready to publish** |
| **CoreSpotlight** | 16+ | On-device search indexing | Partial — **validated, ready to publish** |
| **WeatherKit** | 16+ | Apple's weather service | None — blocked on availability propagation gaps |
| **TipKit** | 17+ | Feature discovery / tips UI | None — blocked on duplicate members, generic arity |
| **App Intents** | 16+ | Siri / Shortcuts / Spotlight | Partial in Microsoft.iOS |
| **Translation** | 17.4+ | On-device translation | None |
| **Foundation Models** | 18.1+ | Apple Intelligence on-device LLM | None — huge strategic value |
| **CryptoKit** | 13+ | Modern Swift-first crypto | Partial — blocked on UnsafeRawBufferPointer in @_cdecl, generic associated types |
| **AuthenticationServices** | 12+ | Sign in with Apple, passkeys | Partial — blocked on ASImportableEditableField type misclassification |
| **HealthKit** (Swift APIs) | 17+ | Workout sessions, async queries | Old ObjC API only |

Not on shortlist: SwiftData/Observation (macro-based, needs verification), Combine (Rx.NET exists), SwiftUI (separate bridge work).

Validation projects for all attempted frameworks are in `swift-dotnet-packages/apple-frameworks/`.

---

## 1.0 thesis

**Bridge .NET MAUI to Apple's Swift-first platform APIs, plus select third-party Swift SDKs that fill real gaps.** Measured by what shipping packages exist and where they run, not by skip percentages.

The skip-rate metric is over-indexed at 15.4% — further reduction has diminishing returns. The headline is Apple framework coverage and multi-platform proof.

### What's shipping

| State | Packages |
|---|---|
| **Published to NuGet** | Nuke, Lottie |
| **Ready to publish** | StoreKit 2, SoundAnalysis, CoreSpotlight (0.8.0), Stripe (0.8.0 or 0.9.0) |
| **Validation only** | Alamofire, RxSwift, GRDB, CryptoSwift, etc. |
