{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Swift stdlib conformance facts",
  "description": "Facts feeding ConformanceOracle for Swift standard-library types that have no local TypeDecl. Generated by `nuke regen-stdlib-conformances`; do not hand-edit.",
  "type": "object",
  "required": ["schemaVersion", "conformances"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "type": "integer" },
    "comment": { "type": "string" },
    "conformances": {
      "type": "object",
      "description": "Map of module-qualified stdlib type name to the module-qualified protocols it conforms to.",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    }
  }
}
