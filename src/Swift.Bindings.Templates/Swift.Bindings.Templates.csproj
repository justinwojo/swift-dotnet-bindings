<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>Swift.Bindings.Templates</PackageId>
    <PackageVersion>0.1.0-preview.1</PackageVersion>
    <Description>dotnet new templates for Swift binding projects. Creates a project configured with the Swift.Bindings.Sdk MSBuild SDK.</Description>
    <Authors>Justin Wojciechowski</Authors>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageTags>swift;dotnet;bindings;template;ios</PackageTags>
    <PackageType>Template</PackageType>
    <TargetFramework>netstandard2.0</TargetFramework>

    <!-- Template packages don't build or include their own assembly -->
    <NoBuild>true</NoBuild>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <IncludeContentInPack>true</IncludeContentInPack>

    <!-- Suppress warning about no lib/ref assemblies -->
    <NoWarn>$(NoWarn);NU5128</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="content/**/*" Exclude="content/**/bin/**;content/**/obj/**" />
  </ItemGroup>
</Project>
